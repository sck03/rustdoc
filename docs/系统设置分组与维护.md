# 系统设置分组与维护

系统设置按任务分类。桌面、浏览器和容器共用界面；分类由产品能力过滤，配置仍由管理员统一维护。

| 分类 | 内容 |
| --- | --- |
| 运行与数据库 | 软件名称、通用导出目录、当前数据库连接；桌面端另有更新来源 |
| 单据设置 | 录入默认值、发票表头／商品明细／付款报销的字段名称、申报默认值 |
| Excel 导入 | 缺省出口商中文名、导入方案、单元格和列映射 |
| 汇率与币制 | 汇率来源、缓存和常用币种 |
| 邮件设置 | SMTP、发件人、单据邮件默认内容和收件人规则 |
| 备份与恢复 | 保留策略、PostgreSQL 自动备份、WebDAV、数据备份还原、团队库恢复与完整迁移 |
| AI 服务 | 模型地址、名称、密钥及提示词 |
| 维护工具 | 日志管理、数据归属、发票清理、运行检查和问题诊断 |

报表模板、文件命名、PDF 合并和 ZIP 默认值继续在“报表模板管理”中维护。

日常业务邮件模板在“客户与供应链 → 邮件中心 → 邮件模板”维护。设置分类、字段分组及维护子页会更新地址，可刷新或从功能搜索直接打开；未保存配置仍只保存在页面内存。

## 单据设置

- 默认打开“录入默认值”，配置前 0—10 个备用列和明细预留空白行数。已有内容的列自动显示，每张发票可临时调整显示列。
- “字段名称”一次显示一组十项，桌面两列、窄屏一列。切换分组或设置分类保留当前草稿。
- “申报默认值”维护申报员、签证机构、领证地点等单一窗口默认项，与 AI 服务分开配置。
- 名称最多 40 字、同组不能重复，留空使用“备用 N”。保存后连接同一服务的账号共用；其他客户端重新进入或刷新页面读取。数据字段键保持稳定，已有模板的文字标题仍在设计器中修改。
- 发票表头、明细显示列和付款备用字段提供管理员快捷入口，打开对应分组；有未保存单据时先确认是否离开。销售版、行政版不显示单据设置。

## 保存和敏感字段

顶部“保存全部修改”统一提交各分类草稿。标题区列出有修改的分类，左侧分类同步标记“未保存”；撤销到原值后取消标记。重新加载、保存失败和多人版本冲突沿用统一草稿保护。

“恢复本分类默认”只更新当前分类中有正式默认值的设置，确认后先写入草稿，不会改变其他分类或切换数据库类型。保存和校验继续使用现有 `Revision` 并发保护。

密码更新开关只出现在数据库连接、邮件、备份和 AI 等相关分类。勾选后只替换填写了新值的密码／密钥，留空保留已有值；更新邮件密码不会清空 PostgreSQL、WebDAV 或 AI 的凭据。

## 运行、备份和日志

- 健康接口的 `databaseProviderKey` 返回服务当前使用的规范数据库标识。界面据此展示 SQLite 文件名或 PostgreSQL 连接参数，状态读取失败时显示错误及重试入口。
- 软件更新地址仅桌面端显示；默认导出目录继续供 Excel、报表和审计等功能共用。
- “备份与恢复”分别提供“计划与保留”“WebDAV 云备份”“数据备份与还原”；PostgreSQL 另显示“团队库与完整迁移”。仅在每周计划下显示星期选择。
- 数据备份保留天数为 0 时不自动清理。WebDAV 连接测试使用已保存配置，上传和下载在数据备份页操作。
- 原数据库恢复、完整迁移、后台任务和下载票据继续使用现有服务、确认及权限边界。
- “维护工具 → 日志管理”集中维护日志和审计保留规则。清理旧日志前需先保存草稿，清理操作不隐式保存其他修改；审计记录的查询及清理仍在“系统管理 → 审计日志”中维护。

配置继续保存在现有受管设置文件，数据库基线仍为 v19。分类和分组选项通过受控地址参数定位，未保存草稿只在页面内存中维护，不增加浏览器持久化或新的默认输出目录。
