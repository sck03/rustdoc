# 程序改进重构进度文档

## 2026-09-25 独立商品字段、字体选择与磁盘清理

- 默认发票不再使用整块高级明细表，货名、PO、款号、箱数/单位、数量/单位、单价及金额各自可拖动、缩放；表头与线条独立保留。TOTAL 和总箱数、总数量、总金额同行，合计与明细同列，在末页跟随正文。普通字段不参与商品循环。
- 日常编辑分开普通字段与商品字段，画布只设计一行。点击新增商品字段寻找同行空位；已有字段直接定位，避免重复堆叠。自动排位不锁定，随后仍可自由拖动、微调、缩放和撤销。对应合计可一键对齐，但不建立永久绑定。低频组件放在高级排版，已有用户组合模板保留读取和输出。
- 字段、文本和页码提供 Noto 黑体常规、黑体粗体、宋体常规三份随包字面选择，分别写入既有 fontFamily/bold；没有新增字体或依赖。字体、字号和对齐优先显示，避免切换多个属性页。三份字体的许可及文件校验通过。
- Rust 自由字段输出复用金额格式化，处理同列文字换行后的顺延及底部对齐，并保留设计时的相对纵向偏移。跟随合计预留真实空间；线条中心与文字垂直对齐和画布保持一致。
- 已通过 fmt、workspace locked tests **255 passed / 0 failed / 3 ignored**、workspace all-features locked check、Windows GNU Debug Tauri build、React build、设计器结构合同及 **34 项**浏览器交互回归。覆盖自动排位后的手动拖动/撤销、独立字段拖动、三种字体草稿回读及合计对齐。3 项实库测试本轮未执行，不计通过。
- 实际 Windows Tauri 使用隔离 DataRoot，默认发票显示独立字段，单独移动箱数后保存用户副本并刷新回读通过；三种字体选项和宋体应用已实际检查。krilla 单页及 36 件商品四页发票已渲染检查，明细无遗漏，合计仅末页，箱数/数量/金额对齐。未执行其它 OS/架构或实体打印。
- 用户要求腾出磁盘空间，先执行清理盘点，再用 Cargo 定向清理仓库内旧 `.codex-runtime/cargo-target-native` 和本轮 `target`（命令统计 18.9 GiB 与 79.2 GiB）。可运行验证文件及记录另存后才清理；E 盘可用空间由约 6.4 GiB 升至约 98.5 GiB。业务数据、模板、相邻只读 C# 工作树、既有程序包、Browsers、Cargo/npm 下载缓存与 node_modules 保留。源码仅移除本次不再使用的重复预览行及相关样式/辅助定义，没有通过删除未验收功能腾空间。
- 现有门禁阻断单列：无障碍脚本依赖 TypeScript 旧的 `lib/typescript.js`，当前 TypeScript 7 安装不含该接口；样式治理的两条既有 `!important` 超过旧基线（HEAD 已存在）。本轮新增样式已改用设计变量，未放宽门禁基线。结构合同、源码规模、文档链接及差异检查通过。
- 依赖治理 **871 components，unresolved=0 / disallowed=0**、脚本套件通过。公开源码守卫发现前一提交 `61b634a` 修改容器镜像后未同步部署文件摘要；仅重新核验并同步现有 compose 文件的 SHA-256，未改镜像或依赖版本，公开源码守卫随后通过。

## 2026-09-25 GitHub 发布入口、版本与审计诊断

- 依赖/运行时稳定版本复核：React 19.3.0、Tauri 2.11.6、Rust 1.98.1 已是当前稳定版；Web 更新到 React Query 5.103.2、lucide-react 1.48.0、three 0.186.1、TypeScript 7.0.2、Vite 8.3.1、Node 类型 26.6.2。Actions、Dockerfile 和容器基线统一到 Node 26.10.0；PostgreSQL 客户端/镜像从 18.4 升到 18.6，Windows 二进制 SHA-256 已重新下载核验为 `fbe23da234ee31547bf8a36d29dfd81e82b849df2d2b78d2eecb43d360252f8c`。Cargo/npm 锁文件同步，未修改数据库 schema 或业务契约版本。
- 基于远端 `7c0305b` 建立 `codex/release-workflow-entries`，保留本地未跟踪 Browsers。按原 C# 操作方式提供 Windows/Linux/macOS 三个桌面入口，另有 Web 系统/架构选择和 Docker 入口；用户确认 macOS 继续仅 ARM64。Windows/Linux 桌面可选 x64/ARM64/all；Web 维持四个既定目标，拒绝没有受管 PostgreSQL 客户端的 Windows ARM64 组合。
- 共用版本校验和打包工作流；CI 输入同步 Rust workspace、npm/Tauri、独立 OCR/Excel 及各自锁文件，修复旧同步脚本访问已不存在的 Tauri 独立锁文件和遗漏主 workspace 的问题。当前内部包版本统一为既有产品版本 0.1.1；未升级第三方依赖，未修改 C# 对照属性。桌面/Web 归档和包内标记包含版本，附 SHA-256；GitHub Release 上传拒绝异源码标签和异内容同名附件，首次创建先 draft 再公开。
- Docker 每架构只构建一次，实际生命周期验证同一镜像后推送唯一临时标签，全部所选目标通过再按 digest 提升版本索引。单架构/预发布不能更新完整 latest；版本内容不可覆盖。未发布时提供 docker load 归档；本地入口增加 Image/SkipBuild，Compose 初始化、业务和恢复共用同一镜像引用。
- 读取 GitHub run `36044362655` 的真实日志并本地复现：主 Cargo 锁图因 glib 0.18.5 / RUSTSEC-2024-0429 被 `--deny unsound` 阻断；OCR、Excel 审计分别通过。保留严格门禁，并在失败后保存三份审计 JSON、生成 SBOM。单项例外守卫已准备，检查 Tauri/glib 精确版本及其它依赖/项目的受影响符号引用；当前仍未启用例外，等待用户风险决定。源码扫描不是上游漏洞已修复的证据。
- 已通过版本/架构/锁图同步/发布冲突/镜像索引回归、脚本套件、Actionlint 1.7.12、45 个 Action 引用、Tauri 更新信任合同、文档链接、公开源码和差异检查。许可治理 851 项，unresolved=0 / disallowed=0。发布回归测试计入现有交付自动化统计，审阅后基线更新为 1402 行，单文件 350 行和后续增长 75 行保持；业务源码阈值未调整。
- Windows x64 GNU 服务端 locked Release 构建通过；Full 网页包实际组装并 ZIP 归档校验通过，196 个文件 / 114.9 MiB，包含版本 0.1.1、OCR、PDFium、PostgreSQL 客户端与许可，SHA-256 一致。fmt 与服务端 HTTP 库回归 4/4 通过。桌面依赖边界 441 个运行包符合 Tauri/SQLite 与排除 PostgreSQL 的要求。Docker Image/SkipBuild 参数和真实非零退出码传播使用隔离命令替身验证；本机没有 Docker daemon，未将替身检查写成真实容器通过。
- Windows x64 GNU 桌面 `cargo build --locked --release -p export-doc-tauri --features custom-protocol` 通过，Full 便携包实际组装完成；EXE 产品版本和包标记均为 0.1.1，OCR、ONNX、PDFium、WebView2Loader 与受管 WebView2 安装资源检查通过。此项为 GNU Release 证据，不替代 MSVC/NSIS 或桌面交互验收。
- 新增轻量 release-script-validation 工作流，在相关 push/PR 执行发布合同、脚本和 workflow 语法检查。尚未推送 main、创建远端 Release 或发布 GHCR；MSVC 正式包、Linux/macOS/ARM64 真机、真实 Docker 与签名更新仍待对应 runner/设备验收。

## 2026-09-25 设计器、恢复、权限与发布链路集中收口

- 接续此前未提交的六份 `.dtpl` 版式、目录身份/草稿恢复、模板文件事务与资源权限改动。文件与数据库模板、图片归属和源业务域分别校验；清理未调用的 HTML 格式化入口及重复票面绘制代码。
- 商品字段可从中文字段列表直接拖到 A4 第一行，自动归入主体并逐件重复；可按列独立拖动/缩放、设置文字和边框、统一最小行距，长内容自动撑高和分页。单据信息不参与商品循环。高级表格、合并/斜线表头、普通线条、图片和印章保留，已有复杂票面不自动改写；高级列编辑一次只显示一列，分组与组合设置默认收起。
- 修复灾备任务空输出、备份结果重复入库、清单错误计算自身、服务器迁移误作 dump、上传/下载参数不符合 OpenAPI、恢复标记没有启动执行等问题。加密包共用有界校验与暂存逻辑，包含数据库/主密钥/用户模板；SQLite 在实例锁内恢复，PostgreSQL 使用独立维护命令、数据库实例锁、恢复前安全 dump、事务还原和存储层角色授权修复。Windows 暂存/密钥目录复用私有 ACL；Unix 使用 0700。下载同时核验当前会话、权限及文件摘要，PostgreSQL 浏览器票据绑定 HttpOnly Cookie。
- 补齐 `requiresLicense` 后端门禁，无效注册码不再回落试用，试用到期边界和过期注册码注册结果修正；团队身份不随容器 HOSTNAME 变化。登录/敏感操作保留密码原文，备份上传头支持 UTF-8 文件名和密码。Tauri 更新检查、安装、取消及安装前阶段均复核管理员会话；生命周期使用弱引用，不让权限适配器延长数据库锁寿命。
- 发布工作流覆盖既定五个桌面目标和四个网页服务目标；Unix 产物先打 tar.gz 保留执行位/应用包结构。容器两架构使用各自原生 runner，再合并 GHCR 索引。网页/容器加入 PostgreSQL 客户端；中央清单沿用核验后的 18.4，许可为 PostgreSQL，附带来源原文和动态库许可。Cargo/npm 版本未升级；Windows 系统签名、Developer ID 和公证不执行。
- 已通过集中门禁：workspace locked tests **253 passed / 0 failed / 3 ignored**；3 个实库用例另在隔离 PostgreSQL 18 执行通过并停止集群。覆盖备份/迁移恢复、运行实例拒绝维护、独立业务账号、组织权限和并发业务契约。全 feature check、fmt、OpenAPI **435 operations / 481 schemas** 一致性通过。
- React 生产构建、设计器浏览器回归 **31 项**、模板草稿 **17 项**、无障碍、缩放和视觉基线 **84 场景**通过；真实 Windows Tauri 打开六类模板，中文修改、保存为用户副本及刷新回读通过。工作流 **35 个** Action 引用、IPC **33/33**、更新合同、脚本套件及公开源码检查通过。依赖治理 **851 项，unresolved=0 / disallowed=0**。
- Windows x64 PostgreSQL 18.4 客户端实际下载、SHA-256、版本与许可检查通过；`artifacts/review-web-server` 生成包含 OCR、PDFium、字体、模板与 PG 客户端的 Windows GNU Debug 服务包。它是本机打包证据，不替代 MSVC Release 或其他 OS 构建。
- 新增的恢复/分页回归保留，测试与示例规模基线复核为 **7346 行**，单文件及后续增长上限保持；生产代码规模阈值未提高。8 份已被事实源覆盖的旧专项文档删除，历史保留在 Git，当前索引和本地链接检查通过。
- 收尾定向验证：Unicode 备份头与 HTTP 文件/会话回归 **7 项**、Tauri 平台/更新回归 **51 项**通过；缩放门禁 **198 场景**通过。实际 krilla PDF 样本为 **A4 三页、30 件商品**，逐页渲染检查同列对齐、完整分页与中文字形；提取确认 30 件各出现一次，发票号和唛头各一次。样本与截图仅在忽略的 artifacts，临时样本程序未进入源码提交。Windows Full 网页服务包已成功 ZIP 归档。
- 尚待独立验收：Linux/macOS/ARM64 真机、MSVC 正式包、真实 Docker 生命周期、完整签名更新升级、实体打印和客户样本逐页签字。本机没有 Docker 命令；没有把脚本静态通过或旧 .NET 测试写成上述项目已通过。查询下推/分页、大规模并发吞吐及 Full 之外的产品裁剪仍按差距台账推进。

## 2026-09-23 默认模板 `.dtpl` 可视化链路收口

- 六份默认模板继续使用同一 V3 AST，并以带 magic、版本、长度和 SHA-256 校验的 `.dtpl` 容器随包发布；默认路径、Builtin 查找和文件模板目录都已切换到唯一的 `.dtpl`。
- 文件模板读取、复制、重命名、显示名称回读、上传、单文件导入、模板包导入、下载和保存统一经过容器解码/封装边界。API 返回可直接交给 React V3 设计器的 JSON 文本，磁盘不会把二进制容器误当 HTML；旧 HTML、V2 和未知文件类型全部拒绝。
- `Design::validate` 放宽了流式明细与页脚之间的错误重叠判断，并允许发票唛头字段绑定；用户模板记录、资源校验和模板预览也接受直接 V3 JSON。报表不再提供高级 HTML 运行时或转换入口。
- 定向回归：`cargo test --locked -p export-doc-engine report_template_files -- --nocapture` 通过（13 项），覆盖六份内置 `.dtpl` 的可视化读取、保存后的容器回写、修订冲突、上传/下载、模板包和事务回滚。完整 workspace 门禁与逐份 PDF 视觉签字仍需后续集中执行。
- 本仓库通过 `rust-toolchain.toml` 固定 Rust `1.98.1`；系统全局 `stable` 版本不参与仓库命令解析，只有需要让其它项目也默认使用新版本时才调整 `rustup default`。

## 2026-09-22 V3 斜线表头、网格溢出与明细混排

- 普通表格新增可选 `diagonalHeader`(斜线上方文字/斜线下方文字),React 类型、属性面板、正式 HTML、Rust Domain、SVG/PDF 共同使用同一字段;斜线由渲染器矢量绘制,不依赖模板名或浏览器 DOM 截图。
- 普通表格单元格在排版前按真实字体测量换行并检查可用高度,内容放不下时明确失败,不再与相邻单元格静默重叠;明细表前后的普通 Flow 现可在同一页按坐标输出,越过页脚或与明细内容重叠时显式拒绝。
- 新增回归覆盖斜线表头 HTML 往返与 SVG 输出、网格溢出失败、明细前后普通 Flow 混排及重叠拒绝;六份默认模板逐份转换、真实设计器最终 PDF 对照、PostgreSQL/Docker 与跨平台打印仍按统一 V3 验收方案单独验收。
- 本批定向验证:`cargo fmt --all --check`、`cargo test --locked -p export-doc-domain -p export-doc-report`、`node scripts/test_report_designer_v3_contract.mjs`、`npm --prefix apps/export-doc-web run build`。

## 2026-09-22 V3 通用打印策略与受控字体测量

- Rust SVG/PDF 现按随包 Noto 字体实际测量中英文换行,移除了普通布局中的字符宽度估算路径;字体测量通过受控 `RuntimePaths.font_path` 初始化,失败时保留明确错误/兼容回退,不引入系统字体。
- V3 通用打印能力补齐:图层 `minHeightHundredthMm` / `pinToPageBottom` 参与页眉页脚预留和贴底,普通网格支持 `verticalAlign`,明细表支持 `firstPageRows` / `continuationPageRows`,并与 `repeatHeaderOnPageBreak` / `keepRowsTogether` 一起进入 SVG/PDF 分页。
- 设计器补齐首页/续页明细行数及垂直对齐入口,HTML 导出同步执行对应分页/对齐语义;Rust 领域校验、前端归一化、V3 合同测试与报告回归共用同一字段,不新增第二套 schema。
- 本批验证:`cargo fmt --all --check`、`cargo test --locked -p export-doc-domain -p export-doc-report -p export-doc-engine --tests`、Web 生产构建、V3 合同、源码规模、字体许可/哈希和依赖治理(`unresolved=0 / disallowed=0`)通过。六份默认模板逐页保真、真实设计器最终 PDF 对照、PostgreSQL/Docker 与跨平台打印仍按统一 V3 方案单独验收。

## 2026-09-21 V3 普通 Flow 顺序与分页符执行

- Rust SVG/PDF 布局现在对主体普通 `Row/Grid/Conditional/PageBreak` 按设计坐标顺序排版;显式 `PageBreak` 会收束当前页并开始新页,后续块触达页脚保留区时自动换页,不再只跳过 Flow 或仅在导出 HTML 中标记分页。
- 普通 Flow 分页保持图层语义:重复页眉与页脚按既有规则绘制,主体静态元素仍按现状输出,动态流由 Rust 统一测量和定位;明细表继续走现有复合分页路径,未把两类分页混成模板名分支。
- 新增回归覆盖显式分页符页序和接近页脚自动换页;本批定向验证使用 `cargo +1.98.1 fmt --all --check` 与 `cargo +1.98.1 test --locked --manifest-path Cargo.toml -p export-doc-report --test designer`(6 项)通过。
- 清理不再保存可重建的根 `target/`、`artifacts/` 验证输出和 `.codex-runtime` 一次性工作区,包括 `cargo-target-native`;源码、锁文件、模板、字体、数据库与交付资源保留。清理后仓库约 1.4 GB。
- 真实设计器复杂块测量、明细表与普通 Flow 混排、六份默认模板转换及最终 PDF 保真仍按统一 V3 验收方案逐项验证,不从现有单元回归推导为已完成。

## 2026-09-21 V3 普通网格占用校验统一

- 新增 `reportDesignerGridPlacement.ts`,前端 V3 保存/导出阶段按行、列、跨行和跨列计算真实占用,拒绝合并范围越界、已占用格重叠,以及未被跨行合并完整覆盖的空行。
- 普通 Grid 的 `colSpan/rowSpan` 改为显式整数校验,不再把越界值静默夹紧成合法值;合法的 rowSpan 覆盖空行继续允许,Rust `grid::placements` 与前端共用同一语义。
- 扩展现有 V3 契约回归,覆盖合法跨行覆盖空行、列跨度越界和真实重叠三类场景。
- 本批验证:`npm --prefix apps/export-doc-web run build`、`node scripts/test_report_designer_v3_contract.mjs`、`node scripts/check_source_size_governance.mjs`、`cargo fmt --all --check`、`cargo test --locked -p export-doc-domain -p export-doc-report -p export-doc-hs`(23+15+8 项)和 `cargo check --locked -p export-doc-engine --all-features` 通过。Chrome for Testing 未安装,V3 打印回归未在本机执行;这不影响本次 Grid 占用契约测试结果。

## 2026-09-21 模板字体统一为现有随包三文件

- 用户要求模板使用可免费商用的既有字体，核实 manifest 和 SIL OFL 1.1 原文：`NotoSansCJKsc-Regular.otf`、`NotoSansCJKsc-Bold.otf`、`NotoSerifCJKsc-Regular.otf`，两个家族、三份资源，允许商业使用、随应用分发及 PDF 嵌入。来源、文件哈希和字体二进制未变。
- 六份默认 HTML 删除系统字体回退，宋体风格模板的加粗标题/表头/字段使用已有 Sans Bold；React 字体常量、新建 V3、结构模板默认值与无效字体回退统一为 Noto Sans CJK SC。已有用户草稿不批量改写，六份默认 V3 转换仍未完成。
- 接续复核补齐 V3 页面/组件的随包字体范围校验，移除旧的未使用字体正则与打印样例中的 Arial。画布组件、结构块 HTML、V3 HTML 和 Rust SVG 的粗体显式使用 Sans Bold；不依赖不存在的 Serif Bold 或系统伪粗体。
- 统一 V3/PDF 样本方案、字体许可记录、当前事实、架构与总览同步约定：原单据布局/字段继续保留，字体替换为已批准资源，不要求复刻样本的微软雅黑等字体；没有随包的宋体粗体不作为交付要求。
- 本批验证：`cargo fmt --all --check`、Domain/Report/HS 定向测试 46 项（23+15+8）、engine all-features check、Web TypeScript/Vite 构建（2394 modules）、V3/导出契约、字体许可/哈希 `--require-files`、源码规模和 `git diff --check` 均通过；依赖治理 850 项，`unresolved=0 / disallowed=0`。
- 使用现有 Rust `render_builtin_reports` 样例生成六类单据和 36 行三页发票，共 7 份 PDF/9 页；检查字体资源及嵌入流，只包含 Sans Regular/Bold 和 Serif Regular，衬线单据粗体已选用 Sans Bold，未检出 U+FFFD。Poppler 渲染后检查九页概览；结果在忽略目录 `artifacts/report-font-verification/`，`font-audit.json` 保存逐文件字体清单。这是 Builtin 的字体验证，不代表默认模板已转 V3，也不是用户五份 PDF 的逐页等价验收。
- 完整 PDF 版式、浏览器画布/快速 SVG 的包内字体加载、跨平台文字测量与打印结果仍须按迁移方案逐项验收，不把字体名称替换当作已完成保真。

## 2026-09-21 HS 三关键词修复与统一 V3 版式核查

- **明确路线与完成边界。** 用户选择轻量原生、默认模板逐份统一 V3、拒绝未支持的高级 HTML。已逐页查看用户提供五份 PDF（共七页），记录发票每页 12 行、横向报关单、合并/竖排/斜线表头、条款/签章及字体要求，形成[《Rust统一V3模板与原版PDF验收方案》](./Rust统一V3模板与原版PDF验收方案.md)。六份默认模板尚未转换，不把样本评审或现有 Builtin 输出写成统一 V3 已保真。原 PDF 与客户内容未提交；检查页图只在忽略目录。
- **HS 实际根因。** “男裤”的产品 User-Agent 收到 403，使用原 C# 请求头可正常读取静态页；恢复原请求头。按“男裤、男T恤衫、T恤衫”保存公开源页的必要结构作为回归夹具，修复标准结果已存在时仍扩展历史实例、推荐分支被提前终止、返回顺序/证据优先级变化、内联高亮插入词中空格、包装层表格漏读及无效的详情根节点评分阈值。推荐同时支持链接和纯文本，并保留来源顺序。
- **HS 分层与动作。** 有界追踪移入 `export-doc-hs::lookup`，纯详情解析拆至 `parser/detail.rs`；engine 保留生成 DTO 投影、权限及候选事务。保持最多 12 次自动详情、3 层推荐、去重与取消检查。作废详情也捕获待审核实例，替代项补全详情；普通搜索不写候选或有效税则。未增加浏览器运行依赖，交互验证兜底仍未实现。
- **报表通用修复。** Domain 共用网格占位校验拒绝越界/重叠合并；`report::layout::flow` 处理行/网格/条件，支持行列跨度、样式继承、四边/虚线、标签/回退文本、基础竖排及隐藏/禁用。修复明细 SVG 误用表格宽度作为页面宽度，保留 A4 实际纸张尺寸。普通 Flow 完整分页、字体真实测量、默认模板转换与逐页保真仍属未完成。
- **已完成定向验证。** `cargo fmt --all --check`、`cargo check --locked -p export-doc-engine --all-features` 通过；`cargo test --locked -p export-doc-domain -p export-doc-report -p export-doc-hs` 通过 46 项(23+15+8)。真实联网核对得到“男裤”2 个当前参考编码/15 条实例，“男T恤衫”3 个当前参考编码/15 条原始实例，“T恤衫”3 个当前参考编码/0 条实例；条数仅为本日公开源采样，不能硬编码为业务规则，也不是海关有效税则认证。原版预期来自只读 C# 源码及同一公开页，未启动原 C# 应用完整联调。
- **文档与治理。** 当前事实、架构选型、功能台账和 PDFium 平台文档修正过时的“手写绑定/HS 无证据模型/文件事务未接通/WebDAV 无 HTTPS”等描述。依赖门禁发现此前 PDFium 封装迁移未同步 notices，按现有锁图补齐 9 项 Cargo 声明，未升级 Cargo/npm 或原生包；治理 `unresolved=0 / disallowed=0`。测试规模基线更新为实际 6186 行，保留单文件 1200 行、后续增长 120 行上限；生产代码门禁阈值不变，保留真实行为回归而不删断言规避规模检查。
- **未宣称通过。** 全部页面操作等价、六份 V3 默认模板、真实 Tauri/React HS 全链、完整 workspace 门禁、PostgreSQL/Docker、跨平台字体/打印与 PDFium 原生运行仍须分别完成。当前分支 `codex/report-hs-parity`，没有推送或替换发布包。

## 2026-09-20 B2 续二:复杂明细与 PDF 控制语义

- **复杂明细已接通。** 原生明细布局现支持分组头、分组小计、总计行和侧栏,小计按小计单元格自身的 `fieldPath` 聚合,分组支持 `pageBreakBefore`;模板 HTML 预览与 SVG/PDF 输出共用同一结构语义。
- **PDF 控制语义修正。** 移除原生布局无条件追加的页码,页码只在模板显式配置 `PageNumber` 时输出;Dashed 边框中矩形和线条均写入 `stroke-dasharray`,不再被静默渲染为实线。
- **本批定向验证。** `cargo fmt --all --check`、all-features check、领域测试 `22/22`、报表测试 `5/5 + 内置 6/6`、引擎报表工作流 `6/6`、源码规模门禁均通过。新增回归覆盖分组/小计/总计/侧栏、跨页重复表头、显式页码和无条件页码移除、虚线边框。尚未执行真实 Tauri/React 设计器联调和跨平台真机打印验收。

## 2026-09-20 B2 续:V3 流块联合结构与基础输出

- **V3 流块契约。** Rust 领域模型已从单一 `DetailTable` 扩展为原版 `Row / Grid / Conditional / DetailTable / PageBreak` 联合结构,并按原版 `type` 与字段形状序列化回读;`DetailTable` 同步承接 `output / detailWidthMm / repeatMode / headerStyle / bodyStyle / border / grouping / summaryRow / sideBand` 等原版合法字段,不再因未知字段直接拒绝保存。
- **校验与输出。** 模板校验按图层角色、流类型、字段域、ID 唯一性和嵌套块类型执行;`Row`、基础 `Grid`、`Conditional`、`PageBreak` 已接入模板 HTML、SVG 与 PDF 基础输出。复杂明细分组/小计/侧栏仍明确拒绝原生排版并保留原稿,不静默降级为简化表格。
- **本批定向验证。** `cargo fmt --all --check`、`cargo check -p export-doc-domain -p export-doc-report -p export-doc-engine --all-features`、领域测试 `21/21`、报表测试 `2/2 + 内置 6/6`、引擎报表工作流 `6/6` 和源码规模门禁均通过。新增回归覆盖原版流块回读/导出、字段域校验和未完成分组的显式拒绝;尚未执行真实 React/Tauri 设计器联调、完整 workspace 门禁、真实 PostgreSQL/Docker 或跨平台 PDF 验收。

## 2026-09-20 B2 起步:模板身份/目录/文件事务与 PDFium 封装

- **模板身份统一。** 新增受管模板解析入口,正式报表渲染现在能解析 `builtin:`、`user:`、`Templates/...` 和受管绝对路径,文件模板不再只出现在管理目录里却在预览/输出时被拒绝。目录查询同时合并内置模板、受管文件模板和已发布数据库模板,并返回文件目录中的显示名称与 `withSealDefault`。
- **模板文件事务。** 新建、保存、重命名、删除和 `.edtpl` 导入共用 `FileTransaction`:只快照本次受影响的文件,目录配置和数据库设置提交失败时按逆序恢复;成功或回滚后清理快照。故障注入回归已验证“已修改文件恢复原内容、已创建文件被删除”。
- **修订错误分类。** 模板正文不存在时仍按修订冲突处理;目录读取、权限或其它 I/O 错误改为明确的基础设施错误,不再被折叠成 409“别人已修改”。
- **PDFium 封装。** engine 的 `pdf/native.rs` 已从 `libloading` 手写 C ABI 迁移到 `pdfium-render 0.9.4`,显式关闭默认 feature 并启用 `pdfium_7881 + image_025`;继续使用受管绝对路径、worker 子进程、输入/页数/像素/输出上限和原错误分类。原生包仍为 `152.0.7961`,未在本条实施 `155.0.8057` 升级。
- **本批定向验证。** `cargo check -p export-doc-engine --all-features` 和模板文件测试 `12/12` 通过;新增目录贯通、受管文件预览、事务回滚和修订错误分类回归。未提交 PDFium 二进制,因此本机没有执行真实 PDFium 加载/预览/文字/OCR/合并回环,也没有执行完整 workspace 门禁、Tauri/React 联调、真实 PostgreSQL/Docker 或跨平台真机验收。

## 2026-09-20 B1:HS 联网语义、WebDAV 与凭证错误分类

- **HS 证据分层。** `export-doc-hs` 改为返回 `SearchBundle` / `DetailBundle` 和显式 `RemoteRecordKind`,不再让解析器直接拼装混合 DTO。标准表与申报实例表按语义表头评分选择,规格进入 `Description`,详情补齐行邮税号、CIQ、分类章节、实例集合和推荐关键词;engine 只负责聚合、DTO 投影和候选写入,搜索响应只含未作废标准编码,避免把实例/作废码混入 `items`。
- **联网追踪边界。** Rust 搜索增加原版同类的推荐链追踪,保留查询去重、详情预算 12、推荐深度 3 和标准编码优先短路;详情补全把实例写入待审核候选,不自动写当前有效税则,作废详情按推荐链寻找可用替代。
- **WebDAV 传输。** 删除只支持 `http://`、自实现分块头解析的 TCP 客户端,改用 `export-doc-network::controlled_agent` 的受控 `ureq`/Native TLS 传输,支持 HTTPS、平台证书校验、标准 chunked 响应和解析地址策略;上传/下载继续保留 4 GiB 上限、2 MiB 列表/64 KiB 错误边界、受管暂存与取消清理。PROPFIND XML 改用 `quick-xml` 解析命名空间化 DAV 响应。
- **凭证 fail-closed。** 设置读取不再把 credential 读取错误折叠为空字符串;密文损坏、主密钥不匹配或记录类型异常会明确返回基础设施错误,邮件/WebDAV/PostgreSQL 等调用方不会继续使用空密码发起请求。
- **本批定向验证。** `cargo fmt --all --check`、`cargo test -p export-doc-hs`(3/3)、`cargo check -p export-doc-engine --features hs-remote`、WebDAV 端到端单元回归(HTTPS 路径、命名空间 DAV XML、响应上限)、邮件损坏凭据回归均通过;源码规模治理通过。尚未执行真实 i5a6 往返、真实 HTTPS WebDAV 服务、Tauri/React 联调、完整 workspace 门禁、真实 PostgreSQL、Docker 和 PDFium 版本迁移;这些保留为后续批次/集中验收。

## 2026-09-20 Firecrawl PDFium 绑定补充评估

- 核对 `firecrawl/pdfium-rs` 及其 crate `firecrawl-pdfium 0.1.0`（2026-08-11，MIT/Apache-2.0 双许可）发布源码，补充[绑定方案第 8 节](./PDFium跨平台绑定与验收方案.md)：运行时绝对路径加载、进程级串行化、自有像素缓冲、字符位置和 OCR 坐标映射适合预览/提取，但明确不实现 PDF 编辑/保存，函数表缺少合并/保存入口。
- 发布包全部 49 个绑定入口在本地 Windows/Linux x64 现有资源静态导出表中均存在；没有执行该封装或跨平台调用。上游固定测试 PDFium 7988，Windows ARM64 不在其声明的 CI-tested 矩阵中；不把符号存在或上游声明当作本项目功能通过。
- 将其列为预览/OCR 有力候选，整套 PDF 功能仍优先验证 `pdfium-render`；不同时引入两套独立 PDFium 初始化、锁和生命周期。krilla 与 React 设计器职责保持，依赖及原生包未修改。

## 2026-09-20 pdfium-sys 与可视化设计器适配评估

- 核查 crates.io 的 `pdfium-sys` 最新发布 `0.1.1`（2021-03-31）及完整源归档：README 仅声明 Windows 测试，默认 bindgen `0.54`，wrapper 仅包含 `fpdfview.h`，构建采用链接式动态库依赖；随包声明只覆盖现有 19 个入口中的 12 个，缺少文本提取与合并保存的 7 个入口。没有编译或运行该包，不把源码检查写成平台支持结果。
- 在[《PDFium 跨平台绑定与验收方案》](./PDFium跨平台绑定与验收方案.md)第 7 节补充具体包、自建 sys 层、`pdfium-render` 的比较。继续优先验证 `pdfium-render + krilla`；自建最小绑定为有明确阻碍时的备选，不直接引入旧 sys 包或保留双实现。
- 明确设计器现状为 React V3 画布编辑、HTML/SVG 预览，PDFium worker 存在不代表设计器已使用最终 PDF 页图。建议保留即时编辑，后端统一测量/分页，由 krilla 生成实际 PDF，再由 PDFium 提供最终输出预览；缺失的 Grid/条件/分组/高级 HTML 仍须在报表模型与布局层补齐。
- 本轮仅补充分析文档，未变更业务实现、依赖、原生包或运行数据；未跑性能、平台加载或完整功能门禁。

## 2026-09-20 PDFium 跨平台 ABI 与封装方案复核

- 核对 PDFium 7961/8057 官方 `fpdfview.h`、`fpdf_save.h`、Rust ABI 规则和 Apple `dlsym` 规则，新增[《PDFium 跨平台绑定与验收方案》](./PDFium跨平台绑定与验收方案.md)。区分调用约定与符号名；当前 64 位 Windows/Linux/macOS 不能套用 Win32 x86 的 `_name@N` 修饰，公共函数和无 `FPDF_CALLCONV` 的保存回调分别声明，整数宽度按 C 类型处理。
- 从当前手写适配器提取 19 个不同入口，本地 Windows x64 DLL 的 PE 导出与 Linux x64 SO 的 ELF 动态符号均为 `19/19` 存在。这是静态检查，未运行 Linux/macOS/ARM64 调用，也没有复现现有程序的符号缺失故障。
- 直接核查 `pdfium-render 0.9.4` 发布源归档：默认 `pdfium_latest` 对应 `pdfium_7881`，动态绑定为 C ABI，提供显式库路径入口。长期优先建议用封装减少 unsafe/生命周期维护，但必须匹配全部启用符号、保留受控 worker、受限 writer 与 AppRoot 路径；不能使用会搜索当前目录/系统库的 `Pdfium::default()`。这项建议不更换 krilla，也未实施依赖迁移。
- 文档类型宏通过本机 `rustc 1.98.1` / `x86_64-pc-windows-gnu` metadata 编译检查，两份方案共 67 个本地链接及差异检查通过；该检查不加载 PDFium，也不作为其它目标的编译/运行证据。
- 本轮仅修改文档，保留此前缺口清单；业务源码、Cargo 清单/锁文件、原生包和原版项目未改。封装源归档保存在忽略的 `.codex-runtime/pdfium-abi-audit-20260920/` 供复核；没有重跑完整 Rust/Web/实库或平台功能门禁。

## 2026-09-20 原版后端缺口盘点与报表方案确认

- 对照 Rust `106efd4` 与相邻只读 C#/.NET 10 基线 `ba9dbdc`，生成[《Rust 与 C# 后端功能差距及修正方案》](./Rust与CSharp后端功能差距及修正方案.md)，列出报表、HS、WebDAV、查询/文件事务、逐页流程、模块边界和分批验收条件。528 个 React 源文件统一换行后与原版全部一致；不把页面复用或路由存在当作功能等价。
- 用户进一步确认继续采用 **krilla + PDFium**。源码核实报表由 krilla 生成，PDFium 经手写 C ABI 用于预览/提取/OCR/合并，未使用 `pdfium-render`。本次官方注册表/发布源查询：封装最新稳定 `0.9.4`，原生包最新非预发布 `155.0.8057`；仓库原生清单仍是 `152.0.7961`，krilla/krilla-svg `0.8.2/0.8.1` 与查询结果一致。没有升级依赖或宣称新版本已验收。
- 确认 HS 搜索混入案例/作废码、规格字段映射不同、详情/推荐证据缺失；报表文件目录与渲染入口未贯通，文件变更缺少原版完整回滚；WebDAV 仅支持 HTTP、部分查询使用整类正文加载后分页。这些是源码证据，未实跑用户当时的联网查询，不把源站变化当作唯一根因。
- 本轮只修改文档和索引，原版、业务源码、锁文件、运行数据及发布输出保持。验证为文档本地链接检查和差异检查；没有重跑 Rust/.NET/Web/实库或 UI 完整门禁，功能修复按新台账后续批次实施。

## 2026-09-20 主支交付审查与退役源码清理

- 用户明确要求将本次 Tauri + React + Rust 改动提交并推送 `sck03/rustdoc` 的 `main`。提交前 fetch 确认 `origin/main` 与本轮起点 `f0ed463` 一致，没有未审查漂移；交付采用普通快进，不重写远端历史。
- 清理共 153 个已退役文件：Slint 客户端 134 个、egui 客户端 16 个、Slint 专用许可 1 个，以及已经无调用的 `export-doc-engine::validation` 和 `reference_runtime` 2 个模块。移除其导出，不保留由 Rust 桌面启动旧 C# API 的无用宿主链路。
- 原 C# 业务／测试和契约生成器继续作为未完成报表等功能的行为对照；旧部署文件仍被对照测试与公开源码守卫引用，暂不删除。业务数据库、模板、OCR 模型、发布包和可复用缓存未清理。只提交任务源码、精确锁文件、资源校验清单和文档，生成产物不进入 Git。
- 清理后验证：`cargo fmt --all --check`、全 workspace／all-features locked check、引擎库 locked tests `48/48`、Actionlint、34 个 Actions 引用、OpenAPI `435/481` 一致性及源码规模通过；公开源码守卫检查 `2227` 文件通过，依赖治理 `841` 组件且 `unresolved=0 / disallowed=0`。暂存区逐项检查没有运行库、缓存、大二进制或业务数据。

## 2026-09-20 Tauri + React + Rust 路线切换（实施中）

- 按用户最新要求修改 AGENTS 与架构事实：原版 React 是三端唯一界面，Tauri 2 是桌面宿主，后端共用 Rust；逐页批次实施、集中联调、最终完整门禁。
- 从只读 ExportDocManager_CS 恢复原版桌面 bridge、窗口、文件对话框、数据根和更新平台适配器；桌面改为进程内 Rust HTTP + SQLite，不恢复 .NET sidecar。
- 2026-09-20 官方稳定版本查询：Tauri 2.11.6、tauri-build 2.6.3、single-instance 2.4.5、updater 2.12.0、CLI 2.11.5、API 2.11.1。
- 盘点后删除 134 个 Slint 文件、16 个 egui 文件及 Slint 专用许可引用；共用 Rust 模块、原版对照、业务数据及缓存保留。本条下方记录本次真实结果，不引用旧 Slint／C# 通过数字。
- OCR 已在完整 Tauri 包实测：资源诊断返回 `ready`，Rust `exportdoc-ocr` 使用 PP-OCRv6 + ONNX Runtime 识别中文／英文图片并返回文字、置信度和定位框；预览端点只在桌面令牌下读取用户选择的本机图片，网络版拒绝该操作。示例输出为“宁波测试公司 出口发票 INV-1001／数量 120 PCS／金额 USD 1234.50”。
- 报表模板截图问题已修复：Rust 新建用户模板现在与原 C# 一样将空正文转换为 V3 A4 starter，管理页可打开 `/reports/templates` 可视化设计器；添加文本、保存草稿、版本递增、复制和 PDF 输出均已实测。Rust 原生渲染目前只支持 V3 基础组件与一个 `Invoice.Items` 明细表，普通 Grid、Conditional、复杂分组汇总和高级 HTML 仍明确列为未完成，不宣称与 C# 设计器完全等价。
- Rust 交付不使用 NPOI、NuGet 或 .NET 运行时；Excel 使用 `export-doc-excel`，PDF 使用 `export-doc-report`。NPOI `2.7.6` 只在保留 C# 对照治理中检查；Rust 原生依赖治理为 `841` 个组件，`unresolved=0 / disallowed=0`。
- 第一批真实 Tauri 页面巡检 27 个入口无 HTTP 错误／JavaScript 异常／整页横向溢出，但不将空页面巡检等同完整业务验收。发票中文合同号修改保存回读（版本递增）、付款创建、Rust PDF 任务（有效 `%PDF-1.7`）、桌面登录／退出和 OCR 预览识别已实际运行。修复全局 Decimal 序列化为字符串导致的概览 NaN，JSON 输出精确数值且不经过二进制浮点转换。
- 集中门禁：workspace locked tests `197 passed / 0 failed / 2 ignored`，两个忽略的实库用例另以隔离 PostgreSQL 18 执行 `2/2` 并正常停止集群。随后截图问题定向回归通过引擎库 `49/49`、报表闭环 `6/6`、HTTP 库 `3/3`；无可选 feature 引擎库另 `48/48`。全 feature check、fmt、官方 OpenAPI `435 operations / 481 schemas` 一致性、Web 生产构建、请求取消／登录／日志脱敏／并发提示／无障碍、脚本套件、34 个 Action 引用、33 个 IPC 权限、更新合同、源码规模和公开源码守卫均通过。已通过且未受后续修改影响的全量测试未重复。
- 交付：本地 `artifacts/native-desktop/ExportDocManager.Tauri` 为包含 OCR 的 Windows x64 GNU Debug 联调包，另保留 `.Full` 测试包。包携带 WebView2Loader、核验后的 WebView2 离线安装器、PDFium、Rust OCR／ONNX、四个 app-local CRT、模型、字体、模板和 notices；不含 C# 后端／NPOI／.NET Runtime。正式本地与远端发布默认含 OCR，非全功能验收才可显式 `-WithoutOcr`。
- 脚本根据原版入口与平台代码恢复；共享资源抽取使用经原 NuGet 签名验证的原生归档 SHA-512 清单，不再把 NuGet 内容哈希误当完整归档哈希。Linux x64 OCR／PDF 原生资产实际校验解包通过，Docker 构建层补齐编译所需 Templates。原模板文件服务按目录身份、包事务拆分，应用构造／健康与分发分离；源文件上限未放宽，交付脚本批次基线经复核调整为 1181，后续增长额度仍为 75。
- 交付资源收尾：共享 PowerShell 资源清单同时用于 Tauri 与网页服务端，已生成 `artifacts/native-web-server-tauri-migration` 的 Debug 服务包；重复整理不再产生 Web/dist 嵌套目录。Docker 的原生资源准备实际解包 ONNX／PDFium 并保留 ONNX LICENSE／ThirdPartyNotices；桌面默认完整包已同步同一许可文件。最终公开源码守卫 PASS，未提交运行数据与大体积二进制。
- 限制：没有触发 GitHub 发布、没有推送 main，没有执行 MSVC／ARM64／Linux／macOS 真机或真实 Docker、干净 Windows 缺 WebView2 安装分支和完整视觉／缩放门禁。报表 Grid／Conditional／复杂分组与高级 HTML 尚未迁完，四产品版和签名更新仍需独立验收。OCR 沿用现有 `ort 2.0.0-rc.12`；官方当前未提供稳定 2.x，未在本次升级到新的 RC，不把它写成最新稳定库。



2026-09-15 及以前的 C# 实施记录已从当前 Rust 进度文件移除，可通过 Git 历史追溯；其测试结果不作为 Rust 验收证据。
