# 字体许可与跨平台报表审查记录

> 当前约定更新：2026-09-21。第 1 节及下列 Rust 约定适用于当前交付；第 2—5 节保留 2026-07-23 的 C#/Chromium 实施与验收历史，不作为 Rust 已通过的证据。
>
> 当前范围：默认 HTML/V3 模板、Rust PDF、最终预览和桌面/服务器发布包。

## 当前 Rust 模板字体约定

按用户明确要求只使用项目已有三份随包字体：Noto Sans CJK SC 常规 400、Noto Sans CJK SC 粗体 700、Noto Serif CJK SC 常规 400。发票/装箱单/合同采用无衬线正文，报关/付款/报销采用衬线正文，需要加粗的标题/表头/字段使用 Sans Bold；没有 Serif Bold 文件，不增加其它字重或系统字体依赖。参考 PDF 的原字体不再作为复刻要求，使用批准字体后仍须校准列宽、行高、换行和分页。

三份字体均采用 SIL OFL 1.1，允许免费商业使用、与应用一起分发及嵌入输出文档；保留随包许可原文。默认 HTML 字体声明、新建 V3 和字体无效时的默认值已统一；已有自定义草稿不因本次调整被批量改写。V3 页面和组件只接受两个随包字体家族，前端读取不支持的字体时明确提示并使用 Sans，服务端拒绝提交的其它字体。设计器、结构块 HTML 和 Rust 向量输出中的粗体均选择 Sans Bold，避免衬线字体被伪加粗或被替换为常规字重。

当前 Rust PDF 编码器从资源目录加载字体。浏览器画布/快速 SVG 预览中的同名字体资源加载、精确测量与最终输出一致性仍需独立验收；只写入 CSS 字体名称不能证明浏览器已经加载包内字体。完整迁移边界见[《Rust统一V3模板与原版PDF验收方案》](./Rust统一V3模板与原版PDF验收方案.md)。

2026-09-21 本机字体验证：六类 Builtin 单据与 36 行三页发票共 7 份 PDF/9 页，字体资源及嵌入流仅使用上述三份字体，Poppler 已渲染检查。字体许可/哈希、Web 构建、设计器/导出契约、Rust Domain/Report/HS 46 项定向测试及 engine all-features check 通过。匿名产物与逐文件字体清单位于忽略的 `artifacts/report-font-verification/`。这不代替六份 V3 转换、原版版式或其它平台验收。

## 1. 字体许可口径

正式报表只允许使用 `Resources/Fonts/OpenSource/font-manifest.json` 中登记的三个 Noto CJK 文件：

| 文件 | 用途 | 许可证 |
| --- | --- | --- |
| `NotoSansCJKsc-Regular.otf` | 正文、表格和无衬线标题 | SIL Open Font License 1.1 |
| `NotoSansCJKsc-Bold.otf` | 表头、重点金额和标题 | SIL Open Font License 1.1 |
| `NotoSerifCJKsc-Regular.otf` | 报关单、付款单、报销单的宋体风格正文 | SIL Open Font License 1.1 |

字体版本固定到 Noto CJK 上游 commit，并在 manifest 中记录 SHA-256。`.otf` 文件不提交 Git，由构建阶段下载、校验并放入程序根 `Resources/Fonts/OpenSource`；许可证、manifest 和 README 随包保留。

模板和正式输出不依赖微软雅黑、Segoe UI、宋体、Arial、SF Pro、PingFang 等系统字体，禁止复制未登记字体二进制进安装包。应用界面字体与模板字体职责分开，`verify-font-license-policy.mjs`、发布包 payload 审计和 CI 检查随包文件与登记哈希。

## 2. 报表输出策略

- `ReportFontPolicy` 为无头 Chromium/PDF 导出注入本地 `@font-face` 和统一字体变量；字体路径来自程序资源根，不读取系统字体目录。
- 发票、箱单、合同、报关单统一使用 Noto Sans CJK；中文付款单正文使用 Noto Serif CJK，标题和表头使用 Noto Sans CJK，消除对 Arial/宋体自动回退的依赖。
- 浏览器 HTML 预览和打印预览继续复用同一份已渲染模板，等待 `document.fonts.ready` 与图片加载后再打印。浏览器直接打印属于快速打印入口；需要跨平台分页、换行和归档一致性的正式输出应使用“生成 PDF”入口，该入口走上述本地字体注入的 Chromium 链路。
- 技术文本统一使用前端 `--edm-font-mono` 等宽变量；唛头 Canvas/图片和报表模板分别保留明确的跨平台回退栈，不复制系统字体。

## 3. 版式基准与箱单规则

- 默认箱单保持每 12 条商品为逻辑页，长品名允许在“货品说明 / DESCRIPTION OF GOODS”列内自然换行，不再把品名锁死在固定单行。
- 品名独立一行，款号和订单号在其下各占一行；模板不擅自添加 `Style:` 或 `Order No.:` 前缀，用户在商品明细中录入的原文会原样显示。
- 混装行的箱数为零或空时不输出 `0CTN`，留空表示承接上一箱；最后一页显示按单位汇总的箱数、数量、重量和体积。
- 当前列名采用行业更易理解的双语名称：唛头、货品说明、包装、数量、毛重、净重、体积。列名不是数据契约，后续自定义模板可以按企业习惯改名。

## 4. 验证矩阵

| 检查项 | Windows 本机 | macOS | Linux |
| --- | --- | --- | --- |
| Web TypeScript/Vite 构建 | 已通过 | 由 CI 工作流执行 | 由 CI 工作流执行 |
| 字体许可证/哈希 | 已通过 | CI 同一脚本 | CI 同一脚本 |
| 发票、箱单、合同、报关单、付款单 PDF | 已用真实长中英文与特殊符号通过 | `.github/workflows/cross-platform-typography.yml` | `.github/workflows/cross-platform-typography.yml` |
| 分页、方向、媒体盒和 PDF 规模比较 | 已通过 | 工作流比较任务 | 工作流比较任务 |
| 视觉、无障碍、缩放/密度 | 80 场景、72 场景已通过 | 浏览器 runner 执行 | 浏览器 runner 执行 |

Windows 本机当前已完成真实验证：全解决方案 .NET 8 Release 测试 `568/568`、Web 生产构建 `2143 modules`、视觉基准 `80` 场景、缩放/密度 `72` 场景、字体许可门禁和箱单多页 PDF 回归均通过。macOS/Linux 在 CI 工作流真正成功前，不将“已通过”写成生产设备验收结论。

## 5. 发布前门禁

1. 执行 `node scripts/provision-report-fonts.mjs`。
2. 执行 `node scripts/verify-font-license-policy.mjs --require-files`。
3. 执行 Web 构建、无障碍、视觉和缩放契约。
4. 执行 .NET 报表 PDF 回归并确认没有 `U+FFFD`、空字符、截断或跨页重叠。
5. 用 `scripts/verify-package-payload.ps1` 检查发布包只含 manifest 登记字体，并确认浏览器开发 UI 字体已从正式包移除。

许可证全文见 `Resources/Fonts/OpenSource/OFL-Noto-CJK.txt`；关于页同步展示字体家族和许可证信息。
