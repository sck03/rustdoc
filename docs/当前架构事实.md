# 当前架构事实

> 事实基线：2026-08-07。本文是部署、开发和审核时唯一需要先阅读的现状说明；按日期排列的旧文档只记录当时的决策与证据，不可用来推断当前路径、数据库版本或接口行为。

## 产品形态

- 桌面端、浏览器服务器版和容器版共用同一套领域模型、应用服务、API 契约和 React Web 界面。
- 后端项目统一目标框架为 `net10.0`，开发工具链固定官方 .NET 10 LTS SDK `10.0.302`；框架依赖使用运行时 `10.0.10`，EF Core/SQLite 使用 `10.0.10`，Npgsql EF Core Provider 使用 `10.0.3`，ONNX Runtime 使用最新稳定版 `1.28.0`。
- 容器采用按职责选择的轻量基线：API 运行层使用 `debian:trixie-slim`，通过 Microsoft 官方 Debian 13 仓库配置包安装构建时可用的最新稳定 .NET/ASP.NET Core Runtime `10.0.x`，随官方配置同步仓库签名密钥轮换，并校验两个共享框架使用同一补丁版本；不硬编码尚未进入 Debian 13 仓库的包修订号。浏览器使用 Debian 原生 `chromium`，并在 `--no-install-recommends` 下显式安装配套 `chromium-sandbox`；API 固定以 UID/GID `10001` 运行，两份 Compose 只增加 Chromium 官方容器沙箱所需的 `SYS_ADMIN` capability，使 PID/Network namespace 沙箱可用，仍禁止 `--no-sandbox`，且 API `5188` 不向宿主发布。Web 只承载静态资源和反向代理，使用 `nginx:1.30.4-alpine3.24`。PostgreSQL 客户端来自 `trixie-pgdg` 的 PGDG 18，数据库服务固定 `postgres:18.4-trixie`。API 容器分配 512 MB `/dev/shm` 并显式允许 Chromium 使用该内存文件系统，避免把渲染临时负载转移到磁盘。容器构建按 BuildKit `TARGETARCH` 将 .NET restore/publish 映射到 `linux-x64` 或 `linux-arm64`，把对应的 `libonnxruntime.so` 带入镜像，确保 Rust OCR sidecar 的构建期真实识别探针与运行时使用同一原生依赖。Microsoft Container Registry 当前没有稳定版 .NET 10 Trixie SDK/ASP.NET 标签，因此构建层继续使用官方固定 SDK `10.0.302-noble`，不使用不存在的 `bookworm-slim` 标签，也不自行伪造基础镜像。
- 桌面单机使用 SQLite；办公室/浏览器/容器团队版使用 PostgreSQL 18。项目尚未投产，不为 v1—v5 数据增加兼容迁移层；当前空库基线为 `v6`，不带有效版本标记的开发库直接拒绝启动。
- PostgreSQL 团队版的后台任务快照存入业务库，但全新部署允许 API 在首次管理员登录建表之前先通过进程就绪探针。后台任务存储初始化只把 PostgreSQL `42P01`（任务表尚不存在）识别为“等待首次建表”并从空内存目录启动；连接失败、权限错误和其它数据库异常仍阻止启动，不会把基础设施故障伪装成空任务列表。
- 浏览器安装器和容器安装器把 PostgreSQL 客户端、OCR、浏览器等工具放在程序运行目录或镜像层；业务数据、配置、日志、缓存、备份、模板和密钥均位于 `DataRoot` 或管理员指定的数据盘。

## 运行目录

`DataRoot` 下的主要目录是 `Database`、`Config`、`Security`、`Files`、`Templates`、`SingleWindow`、`Marks`、`Backups`、`Exports/Browser`、`SupportPackages`、`Logs` 和 `Cache/BackgroundJobs`。路径提供器只解析路径，不在属性访问时创建目录；API/Tauri 启动先检查绝对路径、文件系统根和全部现存祖先中的符号链接/联接点，通过后才创建运行目录并做写入探测。路径字段只保存相对路径，写入、读取和迁移前后继续检查目录边界。只有系统盘时可以使用系统盘上的专用可写目录，不强制要求第二块磁盘。

桌面 DataRoot 迁移使用确定性目录树清单，清单包含相对路径、空目录、文件大小和逐文件内容 SHA-256 的汇总摘要；源目录、同盘 staging 和激活后的目标目录摘要一致后才切换配置并删除旧目录。仅比较文件数和总字节数不再视为有效迁移校验。

## 服务器迁移

服务器迁移包为 schema `2` 的 `.edmmigration` 加密包。职责由以下组件隔离：

1. `ServerMigrationPackageGenerator`：收集受控运行数据、生成 manifest、压缩和加密。
2. `ServerMigrationPackageValidator`：限制大小/路径/ZIP 条目、校验清单和 SHA-256。
3. `ServerMigrationDatabaseRestorer`：调用随运行目录提供的 `pg_dump`/`pg_restore`/`psql`，验证产品库、创建安全备份并恢复数据库。
4. `ServerMigrationFileSwitcher`：准备安全副本、原子切换配置与业务文件并支持回滚。
5. `ServerMigrationRecoveryStateMachine`：在建立数据库连接前按持久化阶段执行验证、备份、数据库恢复、文件切换、完成或回滚。
6. `ServerMigrationDeploymentCertificateAdapter`：明确排除 TLS/Certbot 证书，证书由目标部署重新签发。

恢复状态为 `pending → validating → safety-backup → applying-database → applying-files → completed`；任一中断进入回滚并记录可操作的失败状态。迁移包不包含许可证、机器绑定状态、日志缓存或部署证书。

## API 错误契约

应用服务使用分类异常：

| 分类 | HTTP |
| --- | ---: |
| `ServiceValidationException` / 无效输入 | 400 |
| `ResourceNotFoundException`、受管文件/目录不存在 | 404 |
| `PermissionDeniedException` | 403 |
| `ServiceBusyException` | 429 |
| `ResourceConflictException` / `ServiceConcurrencyException` | 409 |
| `InfrastructureServiceException`、数据库/网络/文件依赖不可用 | 503 |
| `ServiceTimeoutException` | 504 |
| 未分类异常（带 `X-Correlation-ID`） | 500 |

数据库离线、文件系统故障和 WebDAV/外部工具故障不得再包装为普通业务冲突。API 端点和全局安全中间件都复用同一个映射器；上传超限单独返回 413，客户端取消返回 499，受控 OCR/浏览器/Excel 队列繁忙返回 429，明确的内部任务超时返回 504，其他依赖取消或不可用返回 503。

## 请求热路径

- .NET 10 源码层只采用有明确收益的现代基础库能力：私有同步闸门使用 `System.Threading.Lock`，静态只读目录使用 `FrozenDictionary/FrozenSet`，无自定义配置的 Web JSON 序列化复用 `JsonSerializerOptions.Web`。这些改动不增加 NuGet 包，也不改变公开 API 或持久化格式。
- HS 知识搜索把查询 n-gram 每次请求只生成一次，同义词按预计算最长匹配执行且归一化保持幂等；候选评分不再为每个候选重复排序同义词、重复生成查询特征或通过 LINQ 分配交集迭代器。跨平台文件名非法字符由 Domain 共享策略统一判断，普通合法名称直接返回原字符串，避免各层重复获取平台字符数组和生成临时字符数组。
- 授权服务使用 30 秒线程安全状态快照；普通受保护请求不重复读取平台锚点、计算机器身份或改写授权文件。同一自然日内不更新 `LastRunDate`，机器种子和 Windows 本机密封镜像仅在内容变化时写入。
- PostgreSQL 网络模式的会话验证使用最多 30 秒、最多 10000 项的进程内快照；注销、用户停用、改密和权限修改通过会话撤销立即清理本实例缓存。后台清理按会话 ID 游标分轮续扫，达到 10000 项批次上限后会从上次位置继续，扫描到末尾才归零，避免低 ID 有效会话长期挡住高 ID 过期记录。多实例部署仍以数据库为事实源，缓存最长一致性窗口为 30 秒。
- Web 后台任务默认从 1 秒开始轮询并退避到 5 秒；页面隐藏时暂停请求，重新可见后继续。不依赖 Redis 或消息队列。
- 客户、出口商和收款对象的业务选择器使用带取消、300 ms 防抖和服务端分页的远程检索，不再因旧非分页接口的 200 条安全上限漏掉第 201 条以后的档案；当前已选记录按 ID 单独读取。港口等管理列表继续服务端分页；计量单位数据量受业务约束且表格需要完整候选，明确请求受权限约束的完整集合。
- 自定义候选项按类型只返回最近 500 项，再恢复为时间正序供界面使用；重复判断由数据库执行 `AnyAsync`，不会为一次输入把同类型全部历史记录物化到进程内存。

## Web 维护页与样式层

“系统设置 → 维护 → 团队库”由编排组件和四个独立面板组成：数据库备份、数据库恢复、完整迁移、后台任务状态。页面只在进入对应分类时加载相应 chunk，长任务使用后台状态和短期 Range 下载票据，不把大文件整体读入浏览器内存。

基础样式入口 `src/styles.css` 仅保留按主题排列的 `@import` 清单；基础、仪表盘、主数据、单一窗口、审计查询、工具、邮件、任务、装柜、单一窗口文档和产地审查分别位于 `src/styles/*.css`。原先 1580—2655 行的 `theme.css`、`businessFeatures.css`、`reportWorkspace.css`、`responsiveOverrides.css` 已变为有序导入清单，实际规则按主题/业务/报表/响应式职责拆入 `src/styles/theme`、`business`、`report`、`responsive`，单个模块不超过 800 行并保持原级联顺序。源文件行数由 `test:source-size-governance` 门禁限制。

## 发布边界

优先使用免费开源库，当前本轮不新增 Redis、消息队列或数据库依赖。`SQLitePCLRaw.bundle_e_sqlite3 3.0.5` 随包 `LICENSE.txt` 按 SQLite Public Domain 识别为 `LicenseRef-SQLite-Public-Domain`，SPDX 提供提取文本，人类可读 notices 同步说明，不再误报为未解析许可证。运行时统一使用 .NET 10 LTS，并由 `global.json`、GitHub Actions 和容器 SDK 镜像共同锁定；不再保留 .NET 8/9 多目标或兼容分支。容器工作流会真实验证 Debian 13 原生 Chromium 在非 root、未关闭沙箱的条件下生成 PDF，并验证 PGDG 18 客户端和 PostgreSQL 18.4 Trixie 生命周期。ONNX Runtime `1.28.0` 官方包不再提供 Intel macOS native，因此 macOS 桌面交付明确只支持 Apple Silicon ARM64，工作流不再生成会缺失 OCR 运行库的 `osx-x64` 包；Chrome for Testing 供给脚本也会拒绝非 ARM64 的 macOS 平台，跨平台字体工作流固定 `macos-15` 与 `mac-arm64`。系统级 Windows/macOS 签名和公证明确不执行；Updater 包签名契约仍单独保留。真实 Docker、PostgreSQL、Nginx、多机恢复以及 Linux/macOS/ARM64 真机验收由 CI 和部署现场完成，不能用本机缺少 Docker 推断为已验收。

## 文档导航

- [产品架构与文档总览](./产品架构与文档总览.md)：按日期保留的架构证据和完整审计索引。
- [程序改进重构进度文档](./程序改进重构进度文档.md)：实施进度与测试证据，历史条目不覆盖本文。
- [运行目录与路径存储审查清单](./运行目录与路径存储审查清单.md)：逐路径审查和例外说明。
- [架构决策记录索引](./架构决策记录/README.md)：将历史事实、取舍和后续变更与当前事实分开。
