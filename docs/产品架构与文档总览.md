# 产品架构与文档总览

> 构建空间治理：`scripts/clean-generated-artifacts.ps1` 默认删除 `artifacts/` 下除交付物和复用下载缓存外的全部生成目录，以及源码树 `bin/obj/dist/target`、`TestResults/` 和仓库 `tmp/`；`-IncludeCodexRuntimeWorkspaces` 只追加清理 `.codex-runtime/` 中的一次性测试工作区，继续保留仓库内 .NET SDK、NuGet/npm 缓存和工具。便携包、安装器及内部注册机输出只有显式使用 `-IncludeReleaseOutputs` 才删除。2026-07-31 实际整理将工作区由约 `52.6 GiB` 降至 `12.21 GiB`，默认交付物、依赖缓存、浏览器、`App_Data` 和 Git 数据均保留。

> Windows 便携构建目录治理：`artifacts/windows-desktop-run/` 固定视为一次性构建输出。`build-windows-desktop-run.ps1` 与 `build-windows-editions.ps1` 不预检、不询问、也不在最终验收中扫描 `App_Data/` 或 `logs/`；整理阶段直接删除这两个运行目录并覆盖程序文件。需要保留的开发数据由使用者在构建前自行备份，真实业务数据不得在该构建目录中长期使用。普通用户入口见 `scripts/README.md`：四个根 CMD 均为 4 行薄转发，共用唯一宿主显示真实退出码并暂停；自动化使用 `-NoPause` 或环境变量。

> 更新日期：2026-08-12

> **现状优先提示：** 部署和开发请先阅读[《当前架构事实》](./当前架构事实.md)。本页按日期保留历史实施证据；旧 v1—v5、旧路径和旧拓扑只用于审计，不能作为当前实现依据。

> 阅读规则：本页全部日期章节都只保留当时的实施证据，不再承担“当前事实源”职责。数据库版本、目录拓扑、接口状态和部署规则必须以《当前架构事实》为准；本页出现的数据库 `v1/v2/v3/v4/v5`、单一窗口 schema `3.0`、付款字段必填、共享协同工单、工作站/租约、默认客户端档案和旧单一窗口目录描述均不得直接用于当前实施。

> 2026-07-28 商业化、解耦、运行目录和跨平台发布复核见[《整体项目商业化与架构复核规划-2026-07-28》](./整体项目商业化与架构复核规划-2026-07-28.md)。该文记录当日实施快照；数据库版本和单一窗口拓扑已经由本页现行章节继续推进，现行基线为数据库 `v6`，不能再按该文中的早期版本号或共享协同设想实施。

> 本轮整体交叉审查、实施结果与后续路线图见[《整体项目核查与程序改进重构规划-2026-07-23》](./整体项目核查与程序改进重构规划-2026-07-23.md)：该文把源码事实、已实现/CI 契约/待验收状态、运行目录例外、依赖许可证、前端跨平台风险和发布前门禁集中列出，并记录手机导航、登录页、仪表盘、HS、单一窗口、报表布局及自动门禁的实际落地结果。

> 桌面自动更新的管理员地址、三产品版独立密钥配置、GitHub Variables/Secrets、签名产物、轮换和丢失处置见[《Tauri正式更新签名与发布配置》](./Tauri正式更新签名与发布配置.md)。地址可在 GitHub、自建 HTTPS 和可信内网 HTTP 之间切换，公钥仍固定在安装包；项目所有者尚未生成正式密钥或完成真实升级验收。Updater 包签名继续强制，Windows/macOS 系统级签名暂缓到正式商业发布。

> 容器部署模式和 15 个 GitHub Actions 的触发、产物、权限与排障入口见[《GitHub Actions工作流用途与运行手册》](./GitHub%20Actions工作流用途与运行手册.md)。已发布 GHCR 镜像可由 `deploy/container/install-container.sh` 在 Linux x64/ARM64 VPS 一键部署：公司内网使用基础 HTTP；公网域名使用 Nginx + Certbot 自动申请和续期证书。Nginx 始终是唯一的 React 静态文件服务和 Web/API 同源代理，Certbot 不承载业务流量；非 Docker 浏览器服务器包则由 ASP.NET Core 单进程托管 Web/API，不需要 Nginx。

## 2026-08-12 资源占用、产品版裁剪、大文件解耦与发布链路收口

- **运行资源继续按需释放并设置硬边界。** 本地 Playwright 浏览器在最后一次使用后默认空闲 120 秒回收，远程容器 CDP 不受影响；Excel 导入把分析和真实工作簿读取放入同一有界并发闸门，并统一限制原包、ZIP 展开、工作表、行列和总单元格数量。CRM/供应商联系人及供应商商品关联查询增加服务端结果上限，汇率无配置时不再访问网络。
- **桌面离线和三产品版载荷按能力解耦。** Tauri 查询与写入在系统离线状态仍可访问本机随机回环 API；Document/Full 保留报表浏览器、OCR、模板、业务资源和 Excel Analyzer，Sales 由同一 `product-editions.json` 能力清单裁掉这些无用资源。真实 Windows 构建结果为 Document 789 个文件/688,968,624 字节、Sales 397 个文件/193,429,034 字节、Full 789 个文件/688,968,809 字节，三版均完成启动、API、SQLite `admin` 空密码登录和产品端点探针。
- **重新积累的前端与 Rust 大文件按职责拆分。** 装柜主工作区降至约 183 行，项目、参数、工具栏、货物表、结果、投影数学、渲染块和可视化模型分层维护，并以独立模型测试阻止不同优先组/装载区域错误合并；COO 页面抽出文档编辑 hook，报表模板设计器抽出管理侧栏，OCR 进程入口拆出图像预处理与识别模块。源码规模门禁以全仓默认上限、聚合基线和专项组合共同阻止回流。
- **GitHub 发布权限和供应链边界收紧。** 54 个 Action 引用全部固定到完整提交 SHA；普通桌面打包只读源码，Updater 私钥仅进入签名步骤，独立发布 job 才获得 `actions: read` 与 `contents: write` 并下载已验证 Artifact。Linux AppImage、macOS `.app` 和 Windows 便携包都执行原生启动 smoke；系统级签名和 Apple 公证仍不执行。
- **容器发布改为完整 revision 闭环。** API、Browser、Web 先生成互不混淆的 `sha-<40位提交>-<版本>` 多架构候选镜像，三者 digest、版本与 OCI revision 全部一致后才提升版本标签并生成不可变发布清单；Linux 一键安装器要求版本与完整 revision 成对输入，拉取后逐镜像复核 `org.opencontainers.image.revision`，避免混装不同构建。
- **CI 冷启动与 macOS 资源边界修复。** 普通 OCR 请求继续保持 90 秒，容器发布验收单独使用程序集内部 180 秒上限且内部自定义上限不得超过 5 分钟。API 镜像构建阶段只检查 Sidecar、ONNX Runtime 和模型载荷，完整 OCR 推理移到全部镜像构建结束后，在无网络、只读根文件系统和独立临时数据根的容器中执行，避免与 Rust/Web 并行编译争抢 CPU。跨平台报表工作流把两项长 PDF 测试拆到独立 testhost，macOS 每 4 次渲染回收 Chromium，并增加 4 分钟 hang watchdog。空闲回收取消不再在调度器内部锁中触发；主机退出会先停止空闲 worker，再关闭浏览器，避免延迟回收与 macOS 进程树清理竞争。
- **最终本机门禁。** .NET 10 Release Domain `46/46`、Application `156/156`、Infrastructure `473/473`、API `343/343`，合计 `1018/1018`；Web 构建 `2223 modules`，缩放 `198/198`、视觉 `84/84`、Firefox/WebKit `4/4`，Tauri/OCR/Excel Rust 为 `60/60`、`8/8`、`13/13`。Excel 公司名识别改为线性 token/HashSet 判断，长文本不再触发正则超时。825 个依赖组件、15 份 workflow、54 个 Action 引用、PowerShell `27`/CMD `6`/MJS `83` 脚本和 1624 个公开源码文件/61.02 MiB 边界均通过治理；Linux x64 API 与 macOS ARM64 Infrastructure 测试项目交叉编译为 0 警告、0 错误，根 README 仍严格只有 `TESTING`。

## 2026-08-11 可空契约、OpenAPI 与导入体验收口

- Domain 已启用完整可空引用类型分析；Application、Infrastructure 和 API 采用 `annotations` 分阶段迁移，可能查不到记录的服务契约返回 `T?`，不以批量空值抑制符制造虚假安全。
- `/openapi/v1.json` 已切换为 .NET 10 官方 Minimal API 元数据生成；手写 Swagger/客户端文档作为迁移期受控子集保留，并由集成测试保证其中每条路径都存在于官方文档。新增端点以 Minimal API 元数据为先，逐步缩小手写 OpenAPI 面积。
- Excel 往来单位分类同时修复 Rust 与 .NET 路径，实际 `2026YH027备货单.xls` 能正确得到 `Reason Brand Inc` 及独立英文地址。发票往来单位界面重新分区，专属 CSS 与供应商评价 CSS 均移出主题层，避免业务规则污染全局级联。
- `lazyNamed` 保留懒加载组件必需 Props；单一窗口托单页拆出独立面板文件。源码规模治理从专项白名单扩展到 .NET、Web、Rust、测试、自动化和 workflow 的全仓默认上限与增量趋势门禁。
- 本轮最终验证为 .NET `1009/1009`、Web `2213 modules`、缩放 `198/198`、视觉 `84/84`、Firefox/WebKit `4/4`、Tauri/OCR/Excel Rust `60/60`、`8/8`、`13/13`；81 个 NuGet 精确版本和 825 个依赖组件通过漏洞/许可证治理。Windows Document、Sales、Full 三版各 789 个文件并通过真实启动，Full 绿色 ZIP SHA-256 为 `94853a2fdbf502f9faa18ad93b7c559f389f4265561662373df3da84b7b30bb3`。
- 根 README 继续只保留 `TESTING`；系统级 Windows/macOS 签名与 Apple 公证仍未执行。真实容器、PostgreSQL、多机恢复及 Linux/macOS/ARM64 真机结果继续以 GitHub runner 和目标现场为准。

## 2026-08-10 资源边界、取消传播、检索索引与前端协调器收口

- **单一窗口、邮件和 PDF 大文件边界统一。** COO 附件改为 `XmlWriter` 分块 Base64 和有界流式复制，限制 20 个/单个 8 MiB/合计 20 MiB；邮件限制 10 个/单个 10 MiB/合计 18 MiB，并为发送、连接测试设置可取消的 60 秒/30 秒超时；网页与桌面 PDF 合并统一为 2—50 个、单个 50 MiB、合计 100 MiB，禁止输出覆盖输入，后台任务执行前再次校验。
- **发票、付款和 SMTP 取消令牌贯穿。** 发票 CRUD、状态流转、复制、商品查询、EF Core 事务与保存，付款查询/保存及对应 API 都传播请求令牌；报表和托单后台任务传播任务令牌。业务验证、权限、未找到、冲突、取消、超时和基础设施故障继续由全局异常契约分别返回 400/403/404/409/499/504/503，不再由端点统一包装成 409。
- **汇率协议失败和 PostgreSQL 检索性能修正。** 中国银行页面 HTTP 200 但缺少预期表格，或目标币种无可解析数值时返回基础设施故障；PostgreSQL 的 HS、商品、发票、付款、客户和出口商模糊检索改用与 `upper(column) LIKE` 一致的表达式 trigram 索引，SQLite 保持无需新扩展的回退。
- **打印、离线体验与窄屏交互修复。** 报表预览用单航班锁避免 load 与兜底计时器重复触发打印，并通过 `afterprint`、焦点恢复和最终超时清理窗口；普通浏览器版登录页不预填账号，Tauri SQLite 单机桌面版继续预填 `admin` 且密码为空，离线提示显示当前会话最近缓存时间，单一窗口桌面工具栏固定但在移动端恢复普通流式布局。
- **重新积累的前端协调器再次拆分。** 单一窗口参考词典把工具栏、Excel 导入、编辑表格、右键菜单和别名对话框拆到独立视图；发票明细编辑器把选区、焦点、键盘导航、虚拟滚动和列隐藏清理抽入独立 hook，主文件由 886 行降至约 618 行。两组组件以及报表设计器、Tauri 路径模块加入更严格的源码规模门禁。
- **公共工作区样式装载缺陷关闭。** 发票及其它普通页面的表单网格曾依赖报表、单一窗口或装柜懒加载路由中的公共选择器，未访问对应路由时就退化为一个输入框占一行。表单/筛选/详情/模态框原语现固定进入应用壳全局样式，设置诊断和单一窗口专属规则各自留在按需样式；真实全局样式入口和静态门禁会在不访问懒加载页面时验证布局。桌面 125%/150%/200%、4K、macOS Retina、Linux、320/375/390 手机及软键盘场景累计 `198/198` 通过。
- **Windows GNU 三版本便携构建恢复稳定。** 本机 `run-tauri-local.ps1` 默认将 Cargo 构建并发限制为 1，避免 GNU/UCRT64 的 LLVM 链接阶段耗尽内存；GitHub Windows 继续使用 MSVC 并显式保留 2 个构建作业。API 端点握手文件以无 BOM UTF-8 原子发布，Tauri 兼容读取 BOM；桌面启动探针按产品版选择业务端点，Sales 验证 CRM 客户分页，Document/Full 验证发票分页，不再把正确的 403 权限边界误判为启动失败。WebView2 退出后短暂占用 `BrowserMetrics` 时，脚本会有界重试清理一次性烟测数据，不再把业务烟测成功误报为整个构建失败。
- **依赖继续以免费商用为前提。** `axe-core` 升至 MPL-2.0 的 `4.13.0`；NPOI `2.8.0` 官方预编译包新增维护费协议，故测试依赖保留最后一个纯 Apache-2.0 的 `2.7.6`。根第三方依赖和 notices 已由统一生成器同步，README 仍只保留 `TESTING`，系统级签名和公证仍不执行。
- **最终本机门禁。** .NET 10 Release 全解决方案 Domain `46/46`、Application `156/156`、Infrastructure `459/459`、API `340/340`，合计 `1001/1001`；全方案及 `win-arm64`、`linux-x64`、`linux-arm64`、`osx-arm64` 额外 RID 均为 0 警告、0 错误。Web 构建 `2204 modules`，视觉 `84/84`、缩放 `198/198`、Firefox/WebKit `4/4`、模板视觉 `9/9`、PDF 结构 `11/11`、PDF 页面像素 `18/18`、打印像素 `11/11`；Tauri/OCR/Excel Rust 为 `57/57`、`8/8`、`11/11`。npm、NuGet/OSV 与 RustSec 审计、822 组件许可证、47 个 Action 引用、1584 个公开源码文件、PowerShell `27`/CMD `6`/MJS `81` 脚本、Actionlint 和源码规模治理通过。原始 `build-windows-editions.cmd -NoPause` 从头构建 Document、Sales、Full，三版均为 787 个文件，大小分别为 688,167,829、688,168,145、688,167,904 字节；API、空密码 `admin` 登录和版本对应分页探针真实成功。Full 绿色 ZIP 的 791 个载荷逐一校验，SHA-256 为 `526de4310dada2c0ca0c4381ff1901da037cbc23f91f8470dbc8f0bc79af697b`；系统级签名和公证未执行。

## 2026-08-09 .NET 10 迁移后稳定性、原子性与大文件再拆分

- **服务器迁移改为显式提交点和 fail-closed 恢复。** 数据库与文件切换成功后先持久化提交点，再写成功审计；提交后的审计故障不会回滚已经一致的数据。提交前失败仍自动回滚，回滚不完整或人工恢复 marker 会阻止 API 启动，避免以数据库/文件混合状态继续服务。
- **错误契约和健康探针按商用边界收口。** 验证、未找到、权限、冲突、并发、繁忙、超时和基础设施异常具有独立分类；数据库、网络、磁盘和外部工具故障不再误报 409。`/livez` 只检查进程，`/readyz` 在两秒内检查数据库、核心目录和远程 CDP，失败返回脱敏 503。报表、浏览器设置和后台任务响应不再向 Web 暴露服务器绝对路径。
- **后台任务与分页热路径修正。** 任务持久化失败会原子撤销内存快照、部分输出和队列配额；分页 offset 使用 `long` 计算，拒绝页码乘法溢出。自动创建发票链路保留 `PermissionDeniedException` 的 403 语义，不再误包装为依赖不可用。
- **OpenAPI、发票表单和 Excel 分析器继续按职责拆分。** OpenAPI Factory 抽出通用 helper 与单一窗口 Schema，Single Window Schema 根文件只保留目录合并；发票表单拆为基础信息、往来单位、扩展字段、贸易条款和唛头明细五个组件；Rust Excel 分析器把工作簿读取、单据字段、表格分析和测试移出 `main.rs`。全部组合加入源码规模门禁。
- **重量级 CSS 改为路由级加载。** 报表、单一窗口和装柜规则各自随 lazy route 加载，初始 CSS 降为 `174.43 KB`；视觉基线通过独立组合入口保持原生产级联。Firefox/WebKit 的桌面和手机回归会真实进入三个路由，等待确定性的 UI/CSS 就绪条件并检查横向溢出。
- **GitHub 容器与跨平台验证修复。** 容器生命周期使用明确 Compose env file、清理临时 `.env` 并正确校验 root 目录；macOS ARM64 Headless Shell locales 合同按实际载荷判断，避免 Windows 失败和 macOS 无界等待。系统级签名和 Apple 公证未执行。
- **最终本机门禁。** .NET 10 Release 全解决方案 `970/970`，五个 RID 均 0 警告、0 错误；Web 构建 `2201 modules`，视觉 `80/80`、缩放 `140/140`、Firefox/WebKit `4/4`、PDF 页面像素 `18/18`、打印像素 `11/11`；Tauri/OCR/Excel Rust 为 `55/55`、`8/8`、`11/11`。NuGet/npm/RustSec、822 组件许可证、46 个 Action 引用、1564 个公开源码文件和脚本治理通过。本机没有 Docker CLI，真实容器/PostgreSQL/多机恢复继续由 GitHub runner 与部署现场验收。

## 2026-08-08 .NET 10 多端、安全边界与大文件最终收口

- **服务器迁移恢复职责再次变薄。** 恢复状态机只保留启动判断、阶段推进、数据库/文件恢复与回滚编排；迁移清单、逐文件 SHA-256 和主密钥兼容性校验移入独立 partial。主文件由 541 行降至 446 行，源码体积门禁恢复通过，未增加旧迁移包兼容分支。
- **PostgreSQL 权限从单账号收口为四角色。** 容器首次初始化管理员只负责引导；不可登录的 owner 持有数据库与 schema；普通 app 账号只保留 DML、序列和例程权限，明确无 DDL、无 `CREATEDB`；独立 maintenance 账号具备 `CREATEDB` 并可切换 owner，专门执行首次建表、结构初始化和数据库恢复。API 普通请求不再使用管理员或维护凭据，服务器迁移也不再要求业务 app 账号具备建库能力。
- **OpenAPI 与前端协调器继续拆分。** Documents/System 的路径和 Schema 根文件均缩为约 15 行目录壳，具体契约按单据和系统子域维护；应用壳、设置页、发票编辑器、报表模板设计器分别拆出路由、会话存储、面板目录、默认值、持久化和编辑动作模块。单文件与组合规模由源码治理基线持续限制，不允许再次累积为千行以上单体协调器。
- **下载票据按浏览器与桌面双通道收口。** 浏览器票据绑定同源 `HttpOnly`、`SameSite=Strict` 会话 Cookie，Tauri 受信任桌面令牌通道不依赖跨源 Cookie；登录、退出和会话撤销会清理旧票据。用途、资源、用户、有效期和受管路径仍逐次校验，不增加 LocalStorage、第二数据库或磁盘票据缓存。
- **容器浏览器从 API 进程彻底隔离。** 正式拓扑扩展为 Nginx、API、Browser、PostgreSQL 四服务；Debian 13 原生 Chromium、setuid sandbox、Noto CJK 和 512 MiB `/dev/shm` 只存在于 Browser 镜像，API 镜像不再携带浏览器并执行 `cap_drop: ALL + no-new-privileges`。只有 Browser 获得 `SYS_ADMIN`，且不接入数据库 backend 网络、不接收 PostgreSQL/维护/主密钥/首次启用 secrets；API 通过专用内部网络和 CDP `9222` 连接。Browser 只读共享 `Cache/ReportPdf`，自身 profile/缓存写入 `runtime/browser`，四服务均有 CPU、内存、PID 和停止宽限边界。GHCR 工作流同步发布 amd64/arm64 API、Browser、Web 三镜像，生命周期工作流验证真实 PDF、capability、凭据与网络隔离。
- **外部汇率与装柜计算资源边界补齐。** 汇率源只允许公开 HTTPS，逐跳校验重定向，并由生产套接字连接回调把实际连接固定到已验证公网 IP，关闭代理和自动重定向，阻断 DNS 重绑定、私网和元数据地址；响应限制 5 MiB/30 秒/5 次跳转，取消、超时和基础设施故障分别映射 499/504/503。装柜分析限制 200 行、100 万件、5000 个模拟单元，默认并行 2、排队 5 秒、计算 10 秒，候选/碰撞/承重/重心链路统一响应取消。
- **API 与桌面大文件继续拆分。** HS API 原单体路由拆为导入、联网、CRUD 和共享 helper，入口只负责注册；Tauri 桌面命令拆为文件选择、PDF 和打开路径，运行路径拆出配置、Sidecar 路径和测试。源码规模治理新增对应单文件与聚合上限，防止后续重新积累。桌面退出触发 API 维护后等待托管服务正常停止，超时才终止进程树；Sidecar stdout/stderr 在长时间运行中按 20 MiB 实时轮转并保留 3 份历史。
- **最终本机门禁（2026-08-09 复核）。** .NET 10 Release 全解决方案 Domain `46/46`、Application `156/156`、Infrastructure `431/431`、API `337/337`，合计 `970/970`；全方案和五个支持 RID 均为 0 警告、0 错误。Web 生产构建 `2201 modules`，浏览器会话、样式、源码规模、无障碍、缩放及业务模型契约全部通过，既有视觉 `80/80`、Firefox/WebKit `4/4`、模板视觉 `9/9`、PDF `11/11`、PDF 页面像素 `18/18`、打印像素 `11/11` 基线保持有效。OpenAPI 客户端重新生成并通过类型检查；Tauri、OCR、Excel Rust 测试为 `55/55`、`8/8`、`11/11`，三个工程格式检查与严格 Clippy 通过；NuGet/npm/RustSec、822 组件许可证、46 个 Action 引用、1564 个公开源码文件、PowerShell `26`/CMD `6`/MJS `81` 脚本和公共源码规模门禁通过。
- **部署边界据实保留。** 根 README 仍只有 `TESTING`，未执行 Windows/macOS 系统级签名或公证。本机没有 Docker CLI，因此真实 Compose、Debian Chromium 沙箱、PGDG/PostgreSQL 18 生命周期、多机迁移恢复以及 Linux/macOS/ARM64 真机结论仍以推送后的 GitHub runner 和部署现场为准。

## 2026-08-07 大文件解耦、资源上限与迁移安全最终收口

- **API 与数据库容器基线切换到 Debian 13/Trixie。** 原工作流使用了 MCR 不存在的 `dotnet/sdk:10.0-bookworm-slim` 与 `dotnet/aspnet:10.0-bookworm-slim`，导致 `container-runtime-validation.yml` 在拉取基础镜像时直接失败。API 最终运行层现为 `debian:trixie-slim`，通过 Microsoft 官方 Debian 13 仓库配置包同步仓库地址和当前签名密钥，再安装构建时可用的最新稳定 .NET/ASP.NET Core Runtime `10.0.x`，并验证两个共享框架补丁版本一致；构建层因 MCR 当前没有稳定 Trixie 标签，继续使用官方 SDK `10.0.302-noble`。Web 最终层只承担静态资源与反向代理，使用轻量 `nginx:1.30.4-alpine3.24`；Node、Rust 和 PostgreSQL 服务使用 Trixie 变体，数据库固定 `postgres:18.4-trixie`。
- **原生 Chromium 与 PGDG 18 使用 Trixie 正式源。** Chromium 继续使用 Debian 原生 `/usr/bin/chromium`，不引入 Google 私有源；PG 客户端仓库从 `bookworm-pgdg` 改为 `trixie-pgdg`，仍固定主版本 18。容器 CI 新增非 root、未关闭沙箱的真实 PDF 生成探针，并把 Dockerfile 实际依赖的 `global.json`、构建属性、字体、模板、OCR 模型、Shared 和第三方声明纳入工作流路径触发范围。
- **后台任务停机与资源释放修复。** `ApiBackgroundJobRunner` 纳入宿主 `IHostedService` 生命周期，停机先原子阻止新任务、取消宿主令牌并等待已登记任务；登记与停机快照使用同一生命周期锁，关闭竞态不再遗漏刚入队任务。并发停机回归确认全部任务最终进入终态，受控输出仍按成功、失败、取消路径清理。
- **超过 200 条的主数据选择缺陷关闭。** 发票客户/出口商、付款对象和查询筛选改为服务端分页远程检索，已选记录按 ID 保留；计量单位明确请求完整受权限约束集合。HS 知识包导出改为有界流写入 DataRoot Cache 临时文件并复用响应结束清理，不再构造 `MemoryStream + byte[]` 两份完整包。
- **四个历史 CSS 单体与授权服务继续拆分。** `theme.css`、`businessFeatures.css`、`reportWorkspace.css`、`responsiveOverrides.css` 只保留有序导入，实际规则拆为 14 个 189—748 行的主题、业务、报表和响应式模块，选择器、声明、媒体上下文与级联顺序保持不变；`RuntimeLicenseService` 按状态、注册、机器身份/锚点、持久化拆为 5 个 partial，最大 363 行。两类拆分均加入源码规模门禁。
- **三个高维护成本模块完成职责拆分。** `HsCodeKnowledgeService` 从约 2036 行单文件拆为查询、搜索、远程证据、历史、示例、知识包、模型和共享辅助 8 个 partial 文件，最大文件约 504 行；`SharedDatabaseMaintenanceService` 从约 1276 行拆为 PostgreSQL、工具进程、归属改派、支持包、诊断和通用辅助 7 个职责文件，最大约 392 行；维护设置页从约 1231 行拆为团队库、数据库恢复/完整迁移、数据归属、发票清理和支持包面板，最大约 506 行，并按当前分类懒加载。拆分不增加第二套服务、状态或 API 契约，也不为预发布旧数据保留兼容分支。
- **外部进程输出统一有界。** PostgreSQL 备份/恢复、迁移验证、客户端版本检查以及授权平台/安全锚点命令均并发读取 stdout/stderr，单流最多保留 1 MiB；超时或取消会结束整个进程树、有限等待退出并观察输出任务，避免管道填满、内存无限增长或后台残留进程。服务器迁移安全备份先写同目录随机临时文件，成功且非空后原子替换，失败不覆盖既有安全备份。
- **支持包按商用长期运行边界收集。** 单日志最多取尾部 8 MiB、全部日志最多 32 MiB，轮转/删除竞态只跳过失效文件；配置摘要最多读取 4 MiB并递归脱敏连接字符串、密码、token、credential、access/signing/encryption key 等字段。可选数据库备份使用无压缩流式写入并受约 4 GiB 恢复包上限约束，生成期间继续执行字节上限；日志、备份、配置和受管目录在读取前后拒绝符号链接/重解析点。
- **迁移包和数据库恢复入口继续加固。** manifest 拒绝空、NULL、重复、越界、绝对、盘符、控制字符、重复分隔符、`.`/`..` 和超过 12 层的路径，也拒绝 ZIP 目录条目和清单之外的文件；恢复 marker 对 NULL 文件列表和损坏路径安全失败。PostgreSQL 标识符按 UTF-8 63 字节限制，旧 owner 角色最多 100 个，业务归属筛选字段最多 50 字符；还原计划写入失败清理半成品。
- **下载与前端资源保持可控。** 五分钟下载票据最多保留 4096 张，签发时清理过期票据并按到期时间淘汰旧票据。维护分类只有进入时才加载对应 chunk；本轮不新增 npm、NuGet、Cargo、数据库、Redis、消息队列或系统级安装依赖，桌面/Web/容器继续复用同一 React、API 与 DataRoot 契约。
- **最终本机门禁。** Release .NET 全解决方案 Domain `42/42`、Application `150/150`、Infrastructure `349/349`、API `298/298`，合计 `839/839`，失败/跳过均为 `0`；Release 构建 `0` 警告、`0` 错误。Web TypeScript/Vite 生产构建 `2168 modules`，视觉 `80/80`、缩放/高 DPI/移动软键盘 `140/140`，Firefox/WebKit 桌面与手机 `4/4`，无严重无障碍、页面或服务器错误；production audit 在已记录的 React Router 声明式客户端例外下通过。Tauri、OCR、Excel Rust 三工程 `cargo fmt --check` 与 `cargo check` 通过，脚本门禁为 PowerShell `26`、CMD `6`、MJS `76`。
- **部署边界未伪造。** 系统级 Windows/macOS 签名和公证仍按要求不执行；本机没有 Docker CLI，因此真实 Compose/Nginx/PostgreSQL 生命周期、volume 权限、Range 下载、多机完整恢复和 Linux/macOS/ARM64 真机仍由 GitHub runner 与目标部署现场验收。当前代码与自动化完成度继续据实记为约 `99%`，不写成生产验收 `100%`。

## 2026-08-06 服务器迁移、运行资源边界与最终跨端复核

- **PostgreSQL 备份与支持包不再占用长连接。** 手工物理备份和技术支持包统一进入后台任务；任务完成后由用途绑定、五分钟有效的短期下载票据提供同源 Range 流式下载。数据库备份票据、任务产物票据不能串用，票据签发后每次消费仍会复核 HTTPS/显式可信 HTTP、受管目录边界和符号链接状态；前端不再把大文件整体读入 `Blob`。一小时级任务改为 2 秒轮询，队列满载统一返回 HTTP 429，页面离开时会取消仍在进行的创建、上传和恢复请求。
- **容器与浏览器服务器提供可回滚的一键安装。** `deploy/container/install-container.sh` 先把 Compose、Nginx 和安装器自身下载到 staging，完成 shell 与 HTTP/HTTPS Compose 双模式校验后才替换正式资产；pull、证书、启动或 readiness 任一步失败都会恢复旧 `.env`、部署文件和受影响的 Let's Encrypt 状态，并用 `--force-recreate` 重建旧服务以重新挂载证书和配置。安装根、运行根和关键子目录在 root 权限操作前拒绝祖先符号链接并归一化为真实非根路径，锁、环境更新、资产激活和令牌标记也不使用可预测的可跟随临时路径。浏览器服务器包增加 Windows `setup-windows.cmd` / `start-windows.cmd`，Linux/Windows 初始化均使用隐藏密码输入、CSPRNG 首次令牌、复杂字符安全保留和明确的一次性显示。
- **DataRoot 与浏览器服务器运行依赖继续受控。** 初始化和启动脚本拒绝磁盘根、文件系统根、普通文件，以及路径任一现存祖先中的符号链接、Windows 联接点和重解析点；`Config`、`Security`、环境定位文件和 OCR 标记也不能借链接改写边界。Windows DataRoot/配置 ACL 关闭继承并显式授予当前管理员、Administrators 与 LocalSystem，Linux 对应目录固定为 `700`，配置文件通过同目录临时文件原子替换。Linux OCR 真识别成功标记只在版本和架构均有效时缓存。发布包根目录携带 `README.md` 与 `version.json`，并自带 PostgreSQL 客户端、OCR 和浏览器运行资源；PostgreSQL 服务本身仍由目标服务器或远端数据库提供。
- **HTTP 灾备必须显式授权。** 容器 HTTP 一键模式默认只绑定 `127.0.0.1`，并设置 `EXPORTDOCMANAGER_ALLOW_INSECURE_DISASTER_RECOVERY=false`；需要向可信办公网/VPN开放时必须显式指定非回环 IPv4，敏感恢复还必须另外传入灾备授权开关。HTTPS 模式默认绑定 `0.0.0.0` 且保持该环境值为 `false`，由 TLS 通道满足安全要求；独立浏览器服务器同样只有管理员显式开启才允许 HTTP 恢复。Nginx 已为 PostgreSQL 备份流式下载设置独立 location，并为数据库恢复保留有界长超时；公网部署必须使用 HTTPS。
- **PostgreSQL 运行依赖随程序/镜像提供。** 浏览器服务器包携带固定 PostgreSQL `18.4` 的 `pg_dump`、`pg_restore`、`psql`；Windows ZIP 校验固定 SHA-256，Linux 正式验证边界为 Debian 13/Trixie 与兼容 glibc 环境，容器 API 镜像使用 PGDG PostgreSQL 18 客户端。默认不回退全局 PATH；工具只位于程序根 `Tools/PostgreSQL/` 或管理员显式指定的运行目录。
- **数据库 secret 契约收口。** 解析优先级为 `EXPORTDOCMANAGER_POSTGRES_PASSWORD_FILE`、`EXPORTDOCMANAGER_POSTGRES_PASSWORD`、`appsettings.json` 中的 AES-GCM 受保护载荷；配置文件禁止明文密码，密码文件相对路径只允许位于 DataRoot `Security/`。服务器迁移的临时验证数据库需要 `CREATEDB`；当前该权限只属于上方独立 maintenance 角色，普通 app 账号保持无建库能力。
- **服务器迁移包边界。** schema `2` 加密包包含 PostgreSQL 业务库、运行配置、`Files/`、`Templates/`、`SingleWindow/`、`Marks/`、印章、唛头图片、本地主密钥和必要持卡机标识；不包含日志、缓存、历史备份、许可证、试用绑定数据或 TLS/Certbot 证书。manifest 与 ZIP 必须完全一致，受管根和归档恢复均拒绝符号链接/重解析点；数据库和文件采用安全备份、阶段状态、原子替换与自动回滚。
- **恢复与下载面向商用大文件。** 灾难恢复需要当前管理员密码重新认证、动作绑定的一次性票据和显式 `RESTORE DATABASE`/`MIGRATE` 确认；后台迁移包和其它任务通过短期下载票据、同源原生 Range 流式下载，避免把大文件读入浏览器内存。Nginx 对迁移上传和任务下载保持独立大文件 location。
- **后台任务状态、取消与清理闭环。** PostgreSQL 团队版把任务状态写入业务库，SQLite 单机版原子写入 `DataRoot/Cache/BackgroundJobs/jobs.json`；单调 `UpdatedAt` 防止迟到快照覆盖新状态，进程重启会把未正常结束的任务收口为可理解的失败/重试状态。取消、删除、过期清理和失败恢复只删除 `DataRoot/Exports/Browser/` 下的受控输出，前端页面卸载时统一中止仍在进行的请求，避免后台继续占用网络、CPU 和内存。
- **空 PostgreSQL 首次启动不再被后台任务表抢跑。** 容器和浏览器团队版允许进程先启动、再由首次管理员登录完成 v6 建表；后台任务数据库存储只在初始读取遇到 PostgreSQL `42P01` 时使用空内存目录等待建表，其它连接、权限或查询错误继续直接失败。真实 PostgreSQL 集成测试会在 schema 为空时先构造后台任务服务，再执行初始化和后续持久化。
- **HTTP、WebDAV、PDF、OCR 与 Excel 均有硬资源上限。** 外部 HTTP/AI/汇率/WebDAV 改为流式读取并统一传递取消、超时和响应大小限制；WebDAV 下载上限 4 GiB，列表与错误正文分别限制 2 MiB/64 KiB。OCR 输入受上传上限约束，排队最多 30 秒、单次识别 90 秒、Sidecar 单行响应最多 4 Mi 字符；信用证 PDF 限 25 MiB、50 页和 50 万提取字符。OpenXML 在交给解析库前统一检查 25 MiB 压缩包、2048 条目、单条目 64 MiB、总展开 256 MiB、500 倍压缩比以及重复/越界条目，覆盖发票、CRM/供应商、HS、单一窗口参考词典和 Excel 转托单入口。
- **高频编辑与 3D 输出资源收口。** 发票明细用同步最新快照消除同一批次连续编辑、快速撤销/重做的丢更新竞态，并按明细规模把不可变历史深度自适应为 30/20/10。Three.js 装柜场景在页面不可见时停止帧循环，静止场景按需渲染且继续保持 `preserveDrawingBuffer=false`；导出 PDF 时通过正式截图事件取得真实 WebGL 帧并替换伪 3D 图，交互场景不会被克隆进打印 DOM。
- **桌面退出、日志与容器权限按长期运行设计。** Tauri 关闭窗口或进程退出统一进入一次优雅退出流程，先隐藏窗口并等待 API 退出维护和 Sidecar 回收，避免双重维护或残留进程；桌面诊断日志超过 20 MiB 自动轮转并最多保留 3 份历史。API 容器固定使用非 root `10001:10001`，`/runtime-data` 和宿主 `api-data` 预置最小权限，Chromium 恢复沙箱；PostgreSQL volume 继续由独立 UID 管理。本轮未新增 NuGet、npm、Cargo 依赖、Redis 或其它常驻服务。
- **运行路径与产品签名边界。** 印章和唛头数据库字段只保存 DataRoot 相对路径（分别为 `Files/Seals/...` 与 `Marks/...`）；启动探针在创建和写入前后检查 DataRoot、Database、Templates、Files、Exports/Browser、Backups/PostgreSQL、SingleWindow、Logs、Cache/BackgroundJobs、Config、Security、WebView、Marks 与 SupportPackages，拒绝链接并验证可写。系统级 Windows/macOS 签名和公证仍按要求暂不执行；Updater 包签名契约不变。
- **最终本机复核。** Release .NET 全量 `826/826`（Domain `42`、Application `150`、API `297`、Infrastructure `337`），构建 `0` 警告、`0` 错误；Web API 可重复生成、`2163 modules` 生产构建、两套 npm production audit、无障碍/样式/安全/路由和业务模型契约通过，视觉 `80/80`、缩放 `140/140`、Firefox/WebKit 桌面与手机 `4/4`；报表视觉 `9/9`、PDF 结构 `11/11`、PDF 页面像素 `18/18`、打印像素 `11/11`。Tauri `cargo fmt/check` 与 `50/50`、OCR `8/8`、Excel `11/11`、脚本门禁 `26/6/76`；NuGet 与三份 Rust 锁文件漏洞审计通过，公开源码守卫检查 `1383` 个候选文件，依赖治理仍为 `831` 组件且 unresolved/disallowed 均为 `0`。系统级签名/公证未执行；本机没有 Docker CLI，真实 Docker/PostgreSQL/Nginx、多机恢复及 macOS/Linux/ARM64 目标设备仍需推送后的 GitHub runner 和部署现场验收。

## 2026-08-05 多用户主数据与 HS 权限增量

- **客户与出口商按所有者隔离。** PostgreSQL 团队版的客户、出口商新增 `OwnerUserId / DepartmentId / CompanyScope` 归属字段。普通单证人员只可列出、读取、新建、修改、删除和引用自己的客户/出口商，也不能给其他账号的出口商上传印章；管理员保留全局查看和管理能力。发票录入、Excel/单据包导入和名称去重均使用同一归属范围，不能通过引用 ID 或导入预览绕过隔离。SQLite 单机版继续保持单用户、不额外过滤。
- **主数据权限与数据范围分层。** 内置单证人员继续拥有“主数据维护”管理权限，但该权限只决定能否操作页面和接口，客户/出口商的实际数据范围仍由服务端所有者过滤强制执行。共享数据库的账号改派工具把客户与出口商纳入“其他业务数据”归属移交，避免停用账号后形成无人可管的数据。
- **HS 编码权限独立。** 新增 `document.hs-knowledge` 模块，管理员可管理，内置单证人员默认只读查询。年度税则导入、换机迁移、HS 编码增删改、示例与联网候选确认等高级维护同时由 API 和前端要求 `manage`；普通单证人员看不到高级维护入口，直接访问路由或调用接口也会被拒绝。账号与权限页面可独立配置该模块。
- **数据库和依赖。** 当前一次性数据库基线提升为 `v6`，空 SQLite/PostgreSQL 直接建立归属字段和索引；无标记或非 v6 开发库明确拒绝，不加入旧结构迁移代码。本轮不新增 NuGet/npm/Rust 依赖、缓存服务或运行目录，归属信息只写当前 DataRoot 数据库，出口商印章继续位于 `DataRoot/Files/Seals/Exporters/{id}/`。
- **验证。** 权限目录测试 `11/11`、相关 Infrastructure 测试 `42/42`、API 启动/用户权限/主数据集成测试 `123/123`、Web 导航模型和生产构建通过；集成测试覆盖单证人员 A/B 的列表、详情、更新、删除、发票引用和印章上传隔离，以及管理员全局视图。

## 2026-07-31 当前正式架构事实源

- **数据库与登录。** SQLite 只用于回环单机、单进程和持卡机；PostgreSQL 只用于办公室/浏览器团队版。SQLite 首次 `admin` 可空密码登录，PostgreSQL 空库只能由 `admin` 使用 8—128 字符密码初始化；这些部署规则不再常驻显示在普通登录页。当前数据库一次性基线已由上方增量提升为 `v6`，无标记或非 v6 开发库直接拒绝，不增加旧库兼容胶水。
- **付款/报销宽松契约。** 付款方、收款方、付款日期和金额均可留空或为零；`PaymentDate` 在实体、数据库、API 与生成客户端中为可空字段。系统只在用户实际填写后检查日期范围、文本长度和金额非负，并继续执行并发版本控制。内置付款/报销模板对空字段输出空白；付款域仍只使用 `Payment.* / Payee` 且没有印章，报关单证域才可使用 `Exporter.*` 与报关印章。
- **普通界面文案。** 登录、文件选择、单一窗口、权限和运行状态页面使用业务语言，不向普通用户展示 `Tauri`、桌面桥接、`SQLite` 探测、API 状态、容器部署或服务端接口等实现说明。数据库类型、备份、运行目录和更新地址只保留在管理员功能中；需要技术人员处理的依赖缺失统一引导联系系统管理员或软件服务商。
- **单系统盘。** DataRoot 没有“必须使用非系统盘”或“必须存在第二块磁盘”的限制。只有 C 盘的电脑可选择 C 盘上有权限的专用目录并通过创建/写入/刷新/删除探针；禁止直接选择磁盘根、相对路径、链接或不可写目录。便携版继续使用程序旁 `App_Data`，安装版后续仍可整体迁移 DataRoot。
- **其余现行架构不变。** 单一窗口继续使用 schema `4.0`、`SWAC1` 预分派、`HMAC-SHA256` 包认证、SQLite 持卡机原子派发和单调回执状态机；一机支持任意多抬头/卡且只有一个活动档案，多台持卡机各自独立。Document/Sales/Full 继续使用独立 identifier、更新清单和 Release 资产；Updater 包签名强制，Authenticode、Developer ID 与公证当前暂缓。
- **发布依赖与容器构建。** 根 `THIRD_PARTY_NOTICES.md` / `THIRD_PARTY_DEPENDENCIES.md` 只随依赖图变化，不再把手工发布时临时同步的程序版本当成仓库漂移；精确构建版本继续写入同包 SPDX/CycloneDX。API 多架构镜像的 NuGet/Cargo 编译缓存使用 BuildKit cache mount，不把完整 Rust `target` 树写入镜像层；GitHub runner 仅在 API 镜像任务中清理明确无关的预装 Android、宿主 .NET SDK、GHC/Haskell 和 Swift 工具目录，并使用最小 GHA 镜像缓存，降低 amd64/arm64 同时构建的磁盘峰值。桌面包摘要使用 PowerShell 字面量 here-string，Action 门禁拒绝会把 Markdown 行尾反引号解释为续行的双引号写法。
- **最终自动门禁。** .NET Release 全解决方案 Domain `42/42`、Application `146/146`、Infrastructure `295/295`、API `281/281`，合计 `764/764`，`-warnaserror` 为 `0` 警告、`0` 错误；Web 生产构建 `2160 modules`，连续两轮视觉基线均为 `80/80`；Tauri Rust `47/47`、OCR Rust `6/6`、Excel 分析器 Rust `11/11`；PowerShell `25`、CMD `6`、MJS `76` 个脚本门禁通过。

## 2026-07-30 交付安全与多端架构快照

- **运行拓扑。** SQLite 只用于回环单机、单进程和持卡机；PostgreSQL 只用于办公室/浏览器团队版。SQLite 首次 `admin` 可空密码登录，PostgreSQL 空库只能由 `admin` 使用 8—128 字符密码初始化。当前数据库一次性基线为 `v4`，无标记或非 v4 开发库直接拒绝，不增加旧库兼容胶水。
- **单一窗口交接。** 办公室通过持卡机档案导出的 `SWAC1.*` 授权码，在生成提交包时完成全局预分派；`.swpkg` schema `4.0` 使用 `HMAC-SHA256` 认证标签、内容/文件摘要和 `StationKey + ProfileKey + CompanyScope + CardIdentifier + AssignmentNonce` 绑定。持卡机只导入属于当前机器、抬头、档案和卡的包，先事务预留，再经受管 staging 和 `.pending-*` 原子改名送入官方客户端 `OutBox`；失败清理本轮文件并进入可重试状态。回执按原包摘要、业务编号和单调状态机归档。
- **多抬头和多持卡机。** 一台 Windows 持卡机可保存任意多个抬头/操作卡档案，也可换卡操作多个公司，但同一时刻只有一个活动档案；多台持卡机各自使用独立 SQLite、`station.id`、认证密钥和目录。最终输出仍由持卡机送给官方单一窗口导入客户端，本程序不保存卡密码、不替代实体卡认证或官方提交动作。
- **DataRoot 与恢复。** 安装版首次启动支持选择 DataRoot 并执行真实写入探针；迁移使用非嵌套目标、链接拒绝、staging、文件数/字节校验、完成 marker 和原子配置切换。SQLite 持卡机可生成带密码加密的完整 `.edmrecovery`；恢复由管理员和可信桌面令牌双门禁，校验包头、版本、摘要、密码和路径后在重启前离线完成。PostgreSQL 继续使用独立备份/还原链。
- **业务域。** `ExportDocument` 报关单证域可以读取 `Exporter.*` 与报关印章；`PaymentVoucher` 付款/报销域只读取 `Payment.* / Payee`，抬头使用 `Payment.PayerName`，不接受任何 seal/stamp/印章字段。两类模板只复用编辑器、Scriban、Chromium、字体和 PDF 基础设施，不复用业务 DTO、字段目录、校验、模板包或数据源。
- **三产品版与更新。** Document、Sales、Full 分别使用独立 Tauri identifier、独立稳定/预发布更新通道、独立 `latest-*.json` 和独立版本 Release 资产；更新通道拒绝版本回退和不完整平台项。当前阶段不强制 Windows Authenticode、macOS Developer ID 或 Apple 公证；正式商业发布前再启用。Tauri updater 包签名仍是 `publish_release=true` 的强制条件，系统代码签名暂缓不等于允许未认证更新。
- **多端体验和供应链。** React 业务端继续共用 Windows/macOS/Linux/Web 代码；Firefox 与 WebKit 已在桌面和 390px 手机宽度完成真实响应式及 axe 验收，严重无障碍、页面异常、HTTP 500 和横向溢出均为 0。统一第三方清单包含 831 个组件，未解析/禁用许可证为 0；桌面和服务器 payload 带 notices、依赖清单、SPDX、CycloneDX，并拒绝运行数据库、日志、密钥、浏览器缓存和开发 UI。
- **当前验证。** .NET Release `762/762` 且 `-warnaserror` 为 0 警告/0 错误；Web `2160 modules`；Tauri Rust `46/46`、OCR `6/6`、Excel 分析器 `11/11`；脚本门禁为 PowerShell 25、CMD 6、MJS 76，15 个工作流共 45 个 Action 引用通过治理。最新真实 Tauri 单一窗口烟测为 COO=`Approved`、ACD=`Accepted`、双抬头双卡、活动档案唯一且 `allBusinessesSucceeded=true`。
- **仍需外部验收。** 真实 PostgreSQL/Docker 服务器、官方单一窗口客户端、实体卡、真实公司抬头、多台持卡机并行、macOS/Linux/ARM64 真机、触屏/读屏、安装升级回滚和正式系统级代码签名仍需目标环境完成，不能由本机模拟 XML 或 CI 编译契约替代。

## 2026-07-29 单一窗口持卡机、多抬头与交接架构收口

- **办公室与持卡机职责分离。** PostgreSQL 网络版负责多人制单、业务权限、提交包生成和回执归档，不直接读取官方客户端目录；插卡操作机固定使用 Tauri + SQLite 单机版，导入 `.swpkg`、把 XML 输出到官方客户端 `OutBox`、收集 `InBox` 回执并导出回执包。
- **一台持卡机支持任意多个公司抬头和操作卡。** 每个档案包含公司抬头、非秘密卡标识、COO/ACD 能力及独立业务目录；没有“两张卡”上限。同一抬头可配置不同卡，同一卡也可按不同抬头建立独立档案。同一时刻只允许一个活动档案，换卡前必须显式切换。系统不保存卡密码。
- **多台持卡机彼此独立。** 每台机器使用自己的 SQLite、`DataRoot/Security/SingleWindow/station.id` 和档案集合。批次与回执包绑定 `StationKey + ProfileKey + CompanyScope + CardIdentifier`；不属于当前机器、当前抬头或当前卡的包不能发送或回传。
- **目录与数据域严格隔离。** 同一档案的 COO 与报关代理委托目录、不同档案目录均不得相同或互相包含；外部官方客户端目录必须是本机绝对路径，网络共享和相对路径直接拒绝。报关单证模板与付款/报销模板继续使用不同模型、目录和渲染数据源，单一窗口链路不读取付款/报销数据。
- **付款/报销域执行零印章契约。** 付款实体、API 请求/响应、设置、模板清单、模板包、用户模板存储、样例预览、真实 HTML/PDF 和前端设计器均不提供 `ShowSeal`、`withSeal`、`doc_seal_path`、`customs_seal_path` 或其它印章数据；携带这些字段的付款请求、设置或模板包会在写入前拒绝。付款抬头统一使用 `Payment.PayerName`，不再依赖 `Exporter.*`。只有 `ExportDocument` 报关单证域可以使用出口商资料与报关印章。
- **删除旧协同模型。** PostgreSQL 抢单、租约、工位心跳、协同看板、默认客户端档案端点及对应实体/服务已移除；内部机器绑定术语统一为 `StationKey`。普通页面不显示工位键、机器名、服务器绝对路径、工作目录或开发阶段规划说明。
- **版本基线（当日快照）。** 2026-07-29 当日数据库为 `v3`、交接包为 schema `3.0`；两者已由上方 2026-07-30 的 `v4 / 4.0` 认证交接基线取代。项目尚未投产，仍不为开发期旧数据库、旧 `.swpkg` 或旧端点增加兼容代码。
- **前端与桌面验收。** 操作中心按“持卡机本地模式 / 办公室归档模式”展示业务动作；普通页面不再用数据库实现术语解释业务角色。档案面板只在查询确认无档案后进入新建态，已有档案时自动选中当前活动档案，避免加载竞态造成误建或空表单。CRM 客户新建、修改、删除和导入后会同时刷新客户目录与销售概览缓存，避免保存成功但目录仍显示旧数据。响应式、视觉、无障碍和多平台缩放门禁继续覆盖 Windows、macOS、Linux 与移动宽度；官方导入客户端和插卡操作只作为 Windows 持卡机生产验收项。
- **自动闭环证据。** 独立 Tauri 冒烟在全新运行数据根中依次创建两个不同公司抬头和两张卡，保留两个档案且每次仅一个活动档案；COO 与 ACD 均完成提交包导入、XML 输出、回执收集、回执包导出和办公室归档，最终状态分别为 `Approved` 与 `Accepted`，`allBusinessesSucceeded=true`。最终组合桌面烟测数据根为 `artifacts/cargo-target-exportdoc/debug/App_Data/Smoke-Final3-20260729-205233-38736`，继续覆盖真实备份创建/还原/API 重启、销售工作区、发票明细、装柜、报表模板和单一窗口操作中心。服务层测试覆盖三个顺序档案和六个并发保存档案，确认实现没有数量上限且始终只有一个活动档案；本轮最终 .NET `741/741`、Tauri Rust `43/43`、OCR Rust `6/6`、Web 生产构建 `2159 modules`、视觉 `80/80`、缩放/跨平台 `140/140` 通过。
- **交接包与回执防串单已收紧。** 包校验会拒绝 manifest 未声明的额外文件、跨大小写重复路径、空载荷、无效业务类型、异常 manifest 大小和压缩比；派发只复制 manifest 明确声明的 XML，复制或入库失败时清理本轮已投递文件。重复导入不能改绑其它持卡机、档案或操作卡，也不能把已派发/回执阶段批次退回“已导入”；临时解压目录清理后仍可从 SQLite 保存的原提交包归档恢复。回执收集同时限定目录边界、批次绑定和派发时间，业务终态优先于较新的初步回执。
- **仍需真实生产验收。** 自动化只证明本程序的包、目录、绑定和回执闭环；正式上线前仍需用真实认证卡、真实官方导入客户端和目标公司抬头验证文件命名、导入动作、签名/口令、回执落盘，以及多台持卡机并行交接。

## 2026-07-29 最终完整优化与审核补充

- **备份恢复是独立基础设施边界。** SQLite 使用在线一致性备份、摘要与 `quick_check` 校验、还原前安全备份和重启前离线替换；PostgreSQL 使用版本预检、互斥/超时、原子备份、漏跑补跑和平台对应的安全还原脚本。备份服务只依赖路径、进程和数据库维护接口，不把数据库实现细节扩散到业务页面。
- **单一窗口采用“办公室交接 + Windows 持卡机最终输出”。** 办公室 PostgreSQL 端不直接触碰官方客户端目录；Windows Tauri/SQLite 持卡机导入交接包并将 XML 放入当前活动档案的官方客户端待导入目录。一台持卡机可维护任意多个抬头/卡档案并显式切换，多台持卡机各自独立；操作员在官方客户端完成认证、导入和提交。本程序不保存卡密码，也不代替官方客户端。
- **模板模型按业务含义而不是技术组件复用。** 报关单证可使用 `Exporter.*` 和报关印章；付款/报销只使用 `Payment.*`、收款方和金额信息，不提供出口商字段、发票域字段或任何印章。两类模板可以复用编辑器、Scriban、Chromium 和 PDF 服务，但 DTO、字段目录、校验、设置、模板包、预览与渲染数据源不能互换。
- **商用数据维护强调留存和防误操作。** 有供货关系/评价的供应商执行停用而不是硬删除；CRM/供应商主要联系人由数据库唯一索引和事务保证唯一。CRM、商机、供应商、联系人、供货关系和评价表单统一保护未保存草稿，保存后的版本变化会重新读取服务端规范结果。
- **普通用户界面继续降技术噪声。** 报表设计器使用“可视化设计/高级 HTML”等可理解名称；邮件正文和模板提供富文本主路径，把 HTML 放入高级折叠区并由服务端白名单清理；单一窗口移动端以卡片承载超宽表格，弹窗、页签、键盘焦点和后台 `inert` 隔离遵守统一无障碍契约。部署路径、机器键、数据库实现和开发阶段说明留在管理员诊断与文档，不常驻普通业务页面。
- **最终稳定性复核补齐边界竞态。** API 的 DbContext 配置显式使用本次服务容器注入的路径提供器，不再受全局静态路径提供器被并行测试或其它宿主切换影响；无待还原任务时只检查受管 marker，不会为了构建内存服务图而创建 SQLite 数据库目录。客户跟进联系人改为受控回填，联系人异步加载不会通过重挂载清空摘要草稿，也能在编辑既有记录时自动选回原联系人。发票和付款默认日期使用本地业务日期，不再因 UTC 截取在中国等正时区提前一天；持卡机能力确认完成前不显示办公室或持卡机专属操作。
- **依赖和多平台策略不扩张。** 本轮复用现有 React、TypeScript、Tauri、Rust、.NET、SQLite/PostgreSQL 与浏览器/PDF 基础设施，没有引入收费 UI 套件、Redis、消息队列、第二 ORM 或系统级安装依赖。官方单一窗口客户端仍是 Windows 专属外部能力；Web 业务端继续面向 Windows、macOS、Linux、平板和手机的响应式使用。

最终本机门禁为 .NET Release `741/741`（Infrastructure `274`、Application `146`、Domain `42`、API `279`，含真实 Chromium PDF），Web 构建 `2159 modules`、视觉 `80/80`、缩放/跨平台 `140/140`，Tauri Rust `43/43`、OCR Rust `6/6`；OpenAPI 生成与类型、无障碍、样式、浏览器会话/路由、设置/邮件/报表/供应商模型、日期文本模型与 production audit 均通过。当前代码与自动化完成度约 `99%`，不能替代真实 PostgreSQL 服务、真实认证卡/官方客户端、多台持卡机、正式签名更新及三平台真机的生产验收。

## 2026-07-28 一次性商业化与架构重构收口

- **程序根只读、数据根可写。** `AppRoot` 只保存可整体替换的程序、内置模板、字体、OCR、浏览器和工具资源；`appsettings.json`、`local-preferences.json`、SQLite、日志、缓存、备份、安全材料、WebView 数据和用户模板统一进入 `DataRoot`。Tauri 的 `runtime-paths.json` 改存平台应用配置目录，可用 `EXPORTDOCMANAGER_RUNTIME_CONFIG_ROOT` 定向到运行包或数据盘；NSIS 不再写安装目录配置文件。
- **模板形成双层目录。** `AppRoot/Templates` 是只读内置模板，`DataRoot/Templates` 是可写用户模板；编辑内置模板时创建用户副本，新建、重命名、删除和模板包导入只操作用户层，同路径用户模板覆盖内置模板。前端和桌面桥接已删除任意外部模板文件选择与模板路径手工输入，渲染只接受受管目录或数据库用户模板。
- **报关单证与付款报销不是通用数据模型。** `Templates/Export` 固定属于 `ExportDocument`，只接收发票、客户、出口商和商品明细等出口单证数据；`Templates/Internal` 固定属于 `PaymentVoucher`，只接收付款、收款方、金额换算和报销信息。目录决定业务域，配置中的 `Type` 不能伪造重分类；保存、重命名、删除、模板包导入导出、预览、HTML/PDF 渲染均按业务域分开，跨类别模板直接拒绝。
- **数据库冻结为正式 v1 基线。** 空 SQLite/PostgreSQL 按当前 EF 模型创建并写入 `__ExportDocManagerSchema` 版本 `1`；无版本标记或非 v1 数据库明确拒绝。项目尚未投产，因此删除了约 900 行旧字段探测、旧索引识别、补列和历史回填兼容代码，不为开发期旧库增加迁移胶水。
- **普通界面完成商业化降噪。** 关于页、登录页、更新页、发票/付款预览、任务与设置文案不再常驻展示 API 地址、数据库模式、User-Agent、内部字体路径、`storagePolicy`、运行数据根政策或原始扩展 JSON。开发者和部署说明保留在管理员诊断、支持包、OpenAPI 和文档中；第三方许可入口继续保留。
- **平台能力不再假装可用。** OCR 页面读取结构化运行能力，组件缺失、关闭或不完整时禁用载入与识别并显示用户可理解的说明；Web/Tauri 继续共用 React 业务源码，桌面文件能力通过桥接提供，浏览器端使用下载/上传语义。
- **容器配置与桌面路径契约一致。** 容器只把宿主 `runtime/api-data` 挂到 `/runtime-data`，可编辑配置位于 `runtime/api-data/Config/appsettings.json`。不再嵌套只读挂载单个配置文件，管理员保存设置所需的同目录临时文件和原子替换可以正常工作；生命周期工作流增加配置目录可写与原子改名探针。
- **本轮最终验证。** .NET Release 构建 `0` 警告、`0` 错误，测试 `637/637`；模板业务域定向测试 `9/9`；Tauri Rust `38/38` 与本地 `cargo check`；Web 生产构建、API 类型、可访问性、样式治理和生产依赖审计通过；页面视觉 `80/80`、缩放/响应式 `140/140`、模板 HTML `9/9`、PDF `11/11`、PDF 像素 `18/18`、打印像素 `11/11`；脚本门禁 `25` 个 PowerShell、`6` 个 CMD、`75` 个 MJS，`17` 个 YAML 可解析，`42` 个 Action 引用通过治理；Windows Desktop 资源包 `778` 个文件通过载荷审计。

本轮计划内可代码化重构项已完成；生产发布就绪度仍受目标环境约束。当前代码/自动化完成度约 `99%`，路径治理约 `97%`，前端商用化约 `94%`，整体生产发布就绪度约 `86%`。剩余主要是本机无法执行的真实 Docker 生命周期、真实 PostgreSQL 多岗位与容量、Windows/macOS/Linux/ARM64 真机、正式签名密钥、升级回滚、触控和读屏验收。

## 2026-07-28 客户跟进、更新页、默认模板与文档治理收口

- SQLite 单机版客户跟进分页已修复 `DateTimeOffset` 数据库排序不受支持导致的 500 错误：跟进时间在 SQLite 模型中统一保存为 UTC ticks，分页查询使用“完成状态、是否有提醒、提醒时间、跟进时间、ID”的稳定数据库排序；PostgreSQL 多用户版继续使用原生时间类型，不引入 Redis、旁路缓存或内存全量分页。
- 软件更新主页面只保留检查更新、版本状态、下载安装和更新日志，不再向普通使用流程展示当前地址、地址来源、HTTP/HTTPS、签名信任或系统设置跳转。管理员仍在系统设置中维护地址；固定公钥、可信内网 HTTP 和发布私钥规则只放在管理员设置与专项文档。
- 默认发票模板不再自动输出 `REMARKS:`。特殊条款按用户填写原文生成；需要英文标题时由使用者直接写入特殊条款，避免模板强加业务文字。
- 文档真源已收敛：删除旧更新机制说明、早期程序改进稿、旧设计规划、旧模块审查、已完成的 Tauri 阶段方案和重复的报表设计器方案；仍有效的单一窗口代码设计与字段映射移入 `docs`。仓库根目录只保留 README，文档导航、架构原则、改进进度和路径审查分别由本文及三份核心文档维护。

## 2026-07-27 单机登录与版本工作区启动边界

- Tauri 单机版由桌面壳层提供随机回环 API 地址并使用运行目录内 SQLite，普通用户不需要也不应修改服务器地址；登录页在桌面运行时隐藏“服务器连接设置”和“首次部署令牌”。浏览器/PostgreSQL 网络版仍保留该高级入口。
- “首次部署令牌”只是网络版空数据库第一次建立管理员时使用的一次性引导凭证，既不是软件更新公钥，也不是软件更新私钥。更新公钥可以内置和公开，更新私钥只存在于发布侧；两套安全机制、生命周期和用途完全独立。
- 桌面窗口从中性根路由启动，恢复会话或登录成功后再根据 `Document / Sales / Full` 和账号模块权限选择默认工作区。业务员版不会先进入单证 `/dashboard`，因此正常启动不再产生“当前页面不可用”的误导提示。
- API 的本机 CORS 现在包裹早期 `/readyz`、匿名 `/healthz` 探针，使 `http://tauri.localhost` 可以读取探测结果；认证、许可证和数据库业务请求仍保持原安全边界。授权页能读取数据而顶部误报“业务服务暂不可达”的跨域假故障已在该边界修正。

## 2026-07-27 P1/P2 自动化交付收口

- PostgreSQL 多用户主线增加 Unicode collation 下可用的 HS 编码前缀索引和可降级 `pg_trgm` 多列 GIN 索引；GitHub 使用 PostgreSQL 18 真实服务，默认验证 1 万税则/实例和 1 万发票/明细，可切换 10 万或 100 万，覆盖分页、查询、并发、四岗位权限和 SQL 安全。
- 容器版增加 HTTP/HTTPS 双 Compose 生命周期工作流，真实构建 API/Web 镜像，验证 readiness/health、安全响应头、API 端口隔离、PostgreSQL volume 容器重建和 `pg_dump/pg_restore`。正式拓扑仍是一套 PostgreSQL、一个 API 实例和多个浏览器用户，不提前引入 Redis 或伪装多副本能力。
- Tauri 正式发布把公钥固定在安装包内，endpoint 由管理员设置控制并可留空回退到安装包默认值；页面和 IPC 都不能替换公钥。地址支持 GitHub、自建 HTTPS 和可信内网 HTTP，构建时直接内置 HTTP 默认地址必须显式放行；发布仍必须提供公钥、带密码私钥和私钥密码，自动生成 `.sig` 并合并 `latest.json`。
- NuGet 版本集中到根 `Directory.Packages.props`；GitHub 统一 Node 24、Artifact v7/v8，并新增 npm/NuGet/Cargo 漏洞审计、SPDX/CycloneDX 和第三方清单。RustSec 实测后已升级 `plist / wayland-scanner / quick-xml / anyhow` 并消除两个 `quick-xml` 高危公告，Tauri Linux 传递依赖 `event-listener` 也已由 `5.4.1` 更新到修复 `RUSTSEC-2026-0221` 的 `5.4.2`；GTK3 栈唯一 unsound 传递例外被精确记录，门禁拒绝任何新增漏洞、unsound 或 yanked crate。依赖审计工具只存在于仓库 `artifacts/.codex-runtime` 或 GitHub runner，不成为客户机器运行依赖。
- 前端确认框和发票作废原因框统一使用公共焦点/键盘 Hook；HS 状态展示模型从页面拆出。自动响应式矩阵增加 320、375、390 软键盘短高度和 200% 缩放；CSS 硬编码颜色、阴影、渐变、px 字号和 `!important` 建立只减不增基线，继续向语义 token 和公共商业组件收敛。
- 发票留存规则已在服务端和前端形成同一闭环：草稿允许普通物理删除；已核对、已出运、已结汇只能作废；已作废不能删除或反审核回草稿。确需清理时，只有管理员可从独立“发票清理”维护工具查询并永久清理，必须二次核对完整发票号、填写原因，并在同一事务保留 `MaintenancePurge` 审计记录。
- 当前可代码化 P1/P2 约 `99%` 完成。updater 地址已支持管理员在 GitHub、自建 HTTPS 和可信内网 HTTP 之间切换，公钥仍固定在安装包；剩余门禁是 GitHub runner 返回的真实 PostgreSQL/Docker 结果、正式密钥、签名升级/回滚，以及 Windows/macOS/Linux、ARM64、触屏和读屏真机验收。

## 2026-07-26 三批次优化架构收口

- 读取型工作台统一采用“数据库聚合/分页/有限窗口 -> API DTO -> React Query 短期缓存”的链路；仪表盘不再加载全部历史发票，HS 与供应商/装柜列表不再提供无界全量读取。写入型大文件统一交给受控后台任务，任务中心是浏览器版的进度、取消、重试和下载入口。
- 共享参考资料与业务单据继续分层：PostgreSQL 多用户由 API 按账号、公司和部门范围隔离发票等业务数据；HS 标准税则、已审核实例和柜型是部署实例级共享参考数据，但维护/审核/删除要求 `manage`。SQLite 只承担单机单进程，不通过共享文件模拟多人。
- 六组导航和统一 `useModalDialog` 降低页面耦合；商业化视觉优先使用现有 CSS 设计令牌、系统可回退字体和免费开源 React/Lucide/TanStack Query，不引入重量级商业组件或恢复大型 Canvas 动画。响应式页面对手机/平板提供查询、查看和轻量审核，对桌面保留批量导入导出与复杂工作台。
- 单一窗口继续由独立适配层负责官方导入客户端契约。本轮查询导出后台化、路径策略、导航和通用显示精度优化均不改变官方字段映射、字段长度、字符串格式或数值精度。
- 当前重构完成度约 `99%`。最终生产门禁仍包括真实 PostgreSQL/Docker、多岗位权限、横向扩展前的任务租约/共享文件验证、百万级数据压测、备份恢复和三平台真机验收；Redis 只有在未来出现可测量的跨节点热点与失效一致性需求时再评估。
- 本轮最终串行门禁：.NET 8 Release `609/609`（Domain `38`、Application `147`、Infrastructure `177`、API `247`），Web 生产构建 `2156 modules`，视觉基准 `80/80`，缩放场景 `140/140`，Tauri Rust 测试 `34/34`，三个 Rust 锁文件无未处理漏洞，SBOM 共 `830` 个去重组件。该结果是本机代码门禁，不替代真实 PostgreSQL/Docker、多岗位并发和三平台真机验收。

## 2026-07-25 发票与智能 HS 业务闭环

- 发票以“草稿 -> 已核对 -> 已出运 -> 已结汇”为唯一正常状态链，作废与反审核是受权限控制的独立动作。状态不再作为普通表单字段保存，而是通过带 `rowVersion` 的命令接口变更并写审计历史；已核对、已出运和已结汇可按权限反审核回草稿，已作废不可反审核。
- 发票物理删除按留存状态分层：只有草稿允许普通 `DELETE`；正式状态和已作废记录均由服务端拒绝普通删除。管理员如确有错误/测试数据清理依据，必须使用独立数据维护接口和设置页“发票清理”面板，二次输入完整发票号、填写最多 500 字原因；服务端在同一事务保留原始摘要和操作人审计，再清理发票明细、状态历史及关联单一窗口工作区记录。
- 发票复制后的新记录始终从草稿开始，复制页面不再暴露保留源状态的选项，状态链不因复制入口出现旁路。
- 发票保存由服务端承担最终业务真源：限制业务类型，校验字段与明细，重算金额、数量、重量、体积和利润，拒绝同公司范围内相同“发票号 + 类型”，并通过乐观并发阻止旧页面覆盖新版本。前端只编辑原始业务输入，派生汇总字段只读；`.edpkg` 导入也遵循同一公司范围和校验口径，新导入/覆盖回到草稿，锁定发票不能通过覆盖或追加明细绕过反审核。
- 发票内的 HS 回填确认先进入未保存草稿，发票保存成功时才在同一事务中写反馈；最终明细不再使用该编码时不学习。可信年度税则的同一语义累计 3 次确认后才生成 `ConsensusConfirmed` 实例，形成“正式税则 -> 发票查询与回填 -> 保存确认 -> 共识实例 -> 后续推荐”的可审查闭环。
- 联网 HS 查询分为只读 `GET` 与明确留存证据的 capture `POST`。只查看不写数据库；capture 只写待审核候选，人工确认后才进入正式共享实例。知识包导出要求 `manage`，共享查询能力与知识资产维护/导出权限分离。
- SQLite 单机版继续使用当前运行数据库且不承担多人共享；PostgreSQL 多用户版由 API 执行数据范围。管理员可审查全部，普通账号默认只看本人，配置公司/部门且拥有发票 `manage` 的账号可看本人及同公司同部门发票。共享 HS 知识不携带客户、发票号、金额或联系人，原始发票仍按业务范围隔离。
- 本轮不新增 Redis、消息队列或第二数据库；现阶段以 PostgreSQL/SQLite 索引、分页、有界查询、请求取消和前端短期缓存为主。当前回归为 .NET `595/595` 加 Web 类型检查与生产构建通过，完成度约 `99%`；真实 PostgreSQL 多岗位、百万级数据、备份恢复、长时间运行与跨平台真机仍是生产前验收项。

## 2026-07-23 品牌、设备职责与模板版式收口

- 品牌显示统一为登录页风格的“外贸业务综合管理系统（单证员版/业务员版/全功能版）”；浏览器标题、Tauri 窗口、关于页和安装器使用同一版本名称。
- 信息层级按必填任务、可选高级、参考信息、历史/技术信息区分。信用证、利润分析和诊断信息默认按需展开；移动端仅承担查看、搜索、审批、简单回填和提醒，桌面端承担完整录入、报表设计、批量审核和导入导出。
- 报关单商品名称使用中文字段优先回退英文，成分/品牌长文本允许自然换行；装箱单按每页 12 行、货品说明居中，表头灰底、外框和唛头竖线与发票的视觉语言一致。
- UI 表格表头统一左对齐；正式出口模板仍按各自单据规范保留居中表头，不受 Web UI 表格规则影响。

> 前端商业化体验当前基线：业务破坏性操作统一使用应用内确认窗口；登录服务器地址默认收进高级连接设置；登录页采用静态业务结构与可降级的轻量 CSS 微动效，不恢复持续重绘 Canvas；发票编辑显示明确保存状态；860px 以下使用默认关闭的覆盖式抽屉主导航，并具备遮罩、Escape、焦点循环/恢复和背景滚动锁定；复杂发票、单一窗口和报表设计继续以桌面端为完整操作目标，手机端以查询、查看和轻量审核为主。上述行为已进入视觉和静态无障碍门禁。

> 多人/单机运行基线：Web、局域网和容器版只允许完整 PostgreSQL 进入 network mode，并由 API 强制执行模块权限、操作级别和业务数据归属；SQLite 只作为回环、单机、单进程数据库使用，不通过共享 `.db` 承担多人协作。当前多人支持拓扑是一套 PostgreSQL、一个 API 实例和多个浏览器用户。

> 2026-07-24 安全与容量增量：network mode 的 API 文档只向管理员或可信 desktop token 开放；普通账号读取设置时仅获得导出等业务所需的最小配置，不返回数据库、SMTP/WebDAV/AI 连接细节或服务器路径。上传、ZIP、HS JSON 导出均有服务端实际流量配额，HS 导出采用异步枚举写 ZIP，避免随共享知识库增长而一次性物化全部记录。登录限流、首次 PostgreSQL 管理员 bootstrap token、AES-GCM 本地密钥、关联 ID、安全响应头和审计真实主键回写已进入后端基线。

> 报表字体基线：正式 PDF/无头浏览器只使用随程序构建的 Noto CJK 开源字体，专有系统字体仅作 CSS 回退；箱单按每 12 条逻辑分页，品名、款号和订单号分行但不由模板强加固定英文前缀。完整许可证和跨平台验证口径见《字体许可与跨平台报表审查记录》。

## 1. 产品版本

桌面 SQLite 单机版统一使用同一套代码和数据库模型，通过构建参数生成三种产品版本：

| 版本 | Tauri identifier | 独立更新清单 | 面向岗位与默认工作区 |
| --- | --- | --- | --- |
| 单证员版 `Document` | `com.exportdocmanager.desktop.document` | `latest-document.json` / `latest-document-prerelease.json` | 单证员；发票、付款、报表、单一窗口和单证工具 |
| 业务员版 `Sales` | `com.exportdocmanager.desktop.sales` | `latest-sales.json` / `latest-sales-prerelease.json` | 外贸业务员；销售概览、客户跟进、商机、邮件模板、供应商、汇率和邮件工具 |
| 全功能版 `Full` | `com.exportdocmanager.desktop.full` | `latest-full.json` / `latest-full-prerelease.json` | SOHO、管理人员、小团队；单证工作区和销售工作区 |

局域网/Docker Web 版只发布全功能版本，再按账号岗位和权限模板显示并授权模块。`Admin` 使用不可降权的管理员模板，`Sales / User / Finance` 默认绑定各自内置模板，也可由管理员改绑自定义模板；岗位继续负责管理员身份和数据归属，模板负责模块与操作级别，二者不混写。

### 1.1 业务员版导航与权限基线

| 使用身份 | 应显示 | 不应显示 |
| --- | --- | --- |
| `Sales` 产品的本机管理员 | 销售概览、客户跟进、商机跟踪、邮件模板、供应商管理、今日汇率、邮件发送、软件更新、授权注册、系统设置、关于系统 | 单证仪表盘、发票、查询、付款、任务中心、主数据、单一窗口、报表、Excel、OCR、装箱、审计日志、用户与权限 |
| `Full` 产品中的 `Sales` 岗位账号 | 销售工作区、供应商、今日汇率、邮件发送、关于系统 | 全部单证工作区；软件更新、授权注册、系统设置、审计日志、用户与权限等管理员入口 |
| `Full` 产品管理员 | 单证、销售和系统管理全部入口 | 无版本级隐藏项 |

导航隐藏、前端直达路由、设置分类和 API 端点必须使用同一能力口径。销售版不得只隐藏菜单而继续开放单证仪表盘、主数据或自定义候选项 API；用户管理和审计管理只属于全功能版管理员。授权页在试用到期时仍允许系统强制跳转，不把授权恢复路径锁死。

### 1.2 局域网 / Docker 容器版账号矩阵

局域网和容器部署固定使用 `Full` 产品能力，桌面 `Document / Sales` 版本不作为服务器岗位。浏览器导航同时受岗位和运行环境约束：Tauri updater 只在可信桌面运行时显示，浏览器管理员不显示“软件更新”。桌面管理员可在系统设置中维护 `latest.json` 地址；该地址可以指向 GitHub、自建 HTTPS 或受控内网 HTTP，签名公钥仍固定在安装包内且不能由页面或服务器设置替换。

| 岗位 | 业务导航 | 系统导航 | PostgreSQL 数据范围 |
| --- | --- | --- | --- |
| `Admin` | 单证、销售、供应商、汇率、邮件全部工作区 | 授权注册、审计日志、系统设置、关于系统；不显示桌面软件更新 | 可查看和管理全部业务数据 |
| `Sales` | 销售概览、客户跟进、商机、邮件模板、供应商、汇率、邮件发送 | 仅关于系统 | CRM、跟进、商机、供应商和本人模板按本人归属；共享邮件模板可读取 |
| `User` | 单证仪表盘、发票、查询、付款、任务中心、单一窗口、主数据、报表和单证工具，另含汇率、邮件 | 仅关于系统 | 默认只访问本人归属；若授予发票 `manage` 且配置公司/部门范围，可访问本人及同公司同部门发票，付款和其它业务仍按各自范围 |
| `Finance` | 付款报销、单据查询、报表设计、汇率、邮件、OCR、关于；付款/查询所需候选项、客户、收款方和出口商读取作为隐藏基础能力提供 | 仅关于系统 | 只访问本人归属；隐藏模块的 API 同步返回 403；报表模板按模块管理级别授权 |

项目当前不存在财务概览页面、路由、API 或财务专属统计服务，正式清单不得把“财务概览”写成已实现模块。保留 `finance.dashboard` 作为未来模块键，但在模块真正开发并通过权限/数据范围测试前不进入导航和模板编辑器。Finance 第一阶段现复用付款报销、单据查询、报表设计、汇率、邮件、OCR 和关于；发票编辑、单一窗口、主数据维护、Excel、装箱和全部销售模块已从导航、直达路由和 API 同步隐藏/拒绝。

网络安全边界：匿名和无管理权限账号访问 `/healthz` 只得到轻量版本、状态和数据库模式，不扫描浏览器/OCR/PostgreSQL 工具，也不返回服务器绝对路径或依赖位置；管理员 Bearer 或可信 Tauri desktop token 才触发完整运行路径/依赖诊断。通用邮件页在浏览器中只发送正文，任意服务器文件路径附件只允许可信 Tauri 桌面请求，网络版附件必须从受控发票/报表输出入口生成。

### 1.3 Full 多用户版权限模板（第一阶段已实现）

成熟商业 Web/容器版的模块级权限模板已落地，并保持与三种桌面产品版本的边界：

- `Document / Sales` 桌面版继续使用构建版本固定能力，不显示用户和权限模板管理。
- `Full + SQLite` 单机版只向管理员提供同一套高级权限入口，便于小团队预配置账号；普通单机用户无需使用。
- `Full + PostgreSQL` 局域网/容器版由管理员维护内置或自定义模板并分配给账号，是权限模板的主要使用场景。
- 最终有效权限必须取“产品版本上限 ∩ 部署形态 ∩ 权限模板 ∩ 数据归属范围”，导航隐藏不能代替 API 拒绝。

数据契约已经使用独立 `PermissionTemplate / PermissionTemplateModule / User.PermissionTemplateId`，不把大量模块布尔值塞入 `User.Role`，也不为每位用户复制导航 JSON。模块权限提供 `view / operate / manage` 三档：读取请求要求 `view`，新增修改和业务动作要求 `operate`，删除要求 `manage`；管理员身份仍额外保护系统设置、用户管理和审计。内置模板包括管理员、单证人员、业务员和财务人员；管理员模板不可修改或删除，自定义模板被账号引用时不可删除。

账号与权限已经从系统设置分类中拆为独立的“系统维护 → 账号与权限”页面和 `/system/access-control` 路由，仅 `Full` 管理员可见；设置页不再重复放置同一入口。独立页面继续采用“账号管理 / 权限模板”两个清晰页签，避免在同一屏幕堆叠两套大型表单。角色使用系统管理员、单证人员、业务人员、财务人员等中文岗位名称和用途说明；账号状态使用明确的启用/停用标识，新增账号默认单证人员，密码字段区分新增必填与编辑留空不修改。权限模板继续使用“左侧模板目录 + 右侧分组模块权限”布局，支持新建、复制、保存、停用和删除；系统模板代码固定，管理员模板只读，模板代码收进高级信息。

账号开通策略固定为“首次 `admin` 登录初始化首个管理员，管理员再创建团队账号”，默认关闭公开自注册。该系统面向企业内部单证、销售和财务协作，公开注册会引入错误公司归属和权限模板误配；未来如果需要外部客户门户，应单独设计邀请制注册，不在当前内部工作区开放自由注册。PostgreSQL/Web/容器版空数据库第一次登录时，只有用户名 `admin` 可以使用本次输入的密码创建首个管理员，其他用户名不能先行初始化；该流程不增加部署环境变量，适合管理员部署后立即进入系统。普通账号和首个管理员初始/重置密码至少 8 个字符；密码使用 PBKDF2-SHA256、随机盐和定时安全比较。管理员不能停用或删除自己，不能删除最后一个启用管理员，已有业务数据归属的账号只能停用。

模板编辑器只显示普通管理员能理解的业务模块，不显示 `document.reference-data`、`common.product-reference`、候选项和单据输出等技术支撑能力，也不显示预留但未实现的 `finance.dashboard`。管理员只需选择业务模块与“仅查看 / 可操作 / 可管理”，服务端在保存和内置模板计算时自动补齐必要技术依赖，并使用最高显式权限避免降级；发票、商机和供应商会自动获得商品资料只读能力，发票另获得单位等单证基础资料读取，但不会因此开放主数据维护写接口或显示主数据导航。前端提交时同样只提交可见业务模块。SQLite 单机模式使用进程内 token 快照；PostgreSQL 多人模式只把 token 的 SHA-256 摘要写入数据库，每次请求重新读取当前启用账号及完整权限模板，因此停用账号、修改岗位或模板后不会继续沿用旧权限。管理员修改或删除账号后会主动撤销该账号全部会话。登录响应同时返回有效模块和访问级别，侧栏、默认入口、直达路由及付款、查询、OCR、邮件、CRM、商机、邮件模板和供应商页面的只读/操作控件使用同一权限；API 中间件仍独立执行强制授权。浏览器文件上传、服务器路径操作和 Tauri desktop token 属于运行环境权限，不能被普通模块模板自行放开。

权限模板和账号分配只存当前运行数据库，不写 `localStorage`、系统盘用户目录或额外 JSON 权限副本。项目尚未投产，本轮直接采用新数据库模型；已有开发期 SQLite 数据库若缺少权限模板表，应备份需要的样例数据后重建，不增加双 schema、运行期猜表或旧库兼容胶水。

### 1.4 多用户报表模板（2026-07-15 第一阶段已实现）

报表设计与“程序根默认模板”分为两条存储边界：默认/管理员文件模板继续位于程序根 `Templates/`，用户在 Web/容器版新设计的模板写入当前运行数据库 `UserReportTemplates` 表。普通用户只需拥有 `document.reports=operate` 即可新建、编辑、共享和删除自己的模板；`manage` 仍用于管理员文件模板、模板包和存储目录维护。

- 新模板默认私有；用户可选择“仅自己可见 / 同部门可见 / 同公司可见 / 团队成员可见”。
- 他人共享模板只读，可“复制为我的模板”后再修改，不会覆盖原作者内容。
- 模板所有者和管理员可发布/取消团队共享、停用或重新启用模板；停用模板继续保留历史和审计，但不参与预览、PDF 或正式输出。
- 报表类型是强隔离字段：`ExportDocument`（报关单证）与 `PaymentVoucher`（付款/报销）分别列表、复制和校验；服务端拒绝 `Invoice/Customer/Exporter/item` 与 `Payment.*` 交叉字段。
- 从默认模板创建时可传入同类型 `sourceTemplatePath`，由服务端读取程序根模板内容作为初始草稿；不允许跨类型复制。
- SQLite 单机版和 PostgreSQL 容器版都按用户所有权过滤普通账号；管理员可查看全部。模板内容、所属人、共享状态、版本号均只写当前运行数据库，不写系统 C 盘或浏览器持久化。
- 共享范围直接使用当前数据库模型的四值，不增加旧字段兼容、双写或运行期猜测分支；项目尚未投产，数据库按当前结构一次性定型。
- 模板及版本变更进入现有审计日志；`ContentHtml/BodyHtml` 只记录长度和 SHA-256 摘要，密码、密钥和令牌统一脱敏，避免审计库复制完整模板或敏感凭据。

具体接口、数据模型、交互规则和后续“数据库模板直接参与正式 PDF/打印渲染”工作见 `docs/报表模板多用户权限设计与实施记录.md`。

### 1.5 单一窗口权限闭环（2026-07-15 第二阶段已实现）

单一窗口页面统一使用 `document.single-window` 的 `view / operate / manage` 语义：列表、筛选、分页、详情和 GET 预检允许查看；草稿字段、默认值重建、修复、保存、提交包、客户端派发、回执收集/导入和目录档案保存要求 `operate`。COO、ACD 编辑器将可编辑字段集中在权限 `fieldset` 内，回执和预检结果保持只读；操作中心列表、批次详情、协作看板和回执包工作区均在只读时保留查询/打开路径能力并禁用写操作。COO/ACD 详情深链路由也要求 `operate`，避免通过地址栏进入可编辑页面。

参考词典继续属于系统设置管理员维护能力（`canManageSettings`），普通单一窗口用户可以查询和使用词典，不获得保存、恢复、JSON/Excel 导入或批量编辑权限；该边界有意独立于普通业务操作权限，避免把全局代码表维护误授予业务人员。

### 1.6 审计日志维护交互（2026-07-15 已实现）

审计日志管理明确分为两种动作：删除筛选结果只处理已提交筛选命中的记录，且至少需要一个筛选条件；按保留期清理忽略当前筛选，只删除早于保留天数的历史记录。两者都使用显示数量、范围和影响说明的确认弹窗，不再要求普通管理员输入 `DELETE`。导出按运行形态分为 Tauri 的 `save-to-path` 和浏览器/容器的 `download`：前者受 desktop token 保护并写入用户选择路径，后者返回标准 Excel attachment，不接收服务器路径。服务端使用 `confirmed` 布尔契约并拒绝无条件删除，前端维护区拆分为导出、保留期清理和折叠高级删除，保持桌面/浏览器响应式布局。

详细改动、验证证据和 Web/容器版受控浏览器下载验收边界见 `docs/审计日志维护交互重构与跨平台审查记录.md`。

### 1.7 全项目导出下载平台契约（2026-07-15 已实现）

全项目导出统一为两个平台适配方向：Tauri 使用原生保存对话框和受 desktop token 保护的 `save-to-path`；Web/容器使用标准 attachment，或对耗时输出采用“创建后台任务 -> 查询进度 -> 下载任务结果”。浏览器不再输入服务器绝对路径，上传文件只进入运行数据根 `Cache/BrowserUploads`，任务结果只进入运行数据根 `Exports/Browser`。普通 Web 任务响应只返回文件名，任务下载按用户归属授权；旧 `/export` URL、旧 operationId、旧 DTO 和兼容适配器已删除。

该契约已覆盖查询、审计日志、发票/付款 PDF、批量 ZIP、单据包、Excel 工具、PDF 合并、发票转移包、报表模板包、支持包和单一窗口包。CRM/供应商 Excel 保持既有 Blob 下载，WebDAV 云备份下载保持服务器备份管理语义。详细矩阵、路径、权限、缓存清理和验证证据见 `docs/全项目导出下载契约重构与多平台审查记录.md`。

### 1.8 商业化界面与装柜现场 PDF（2026-07-16 已实现）

普通业务页面不再直接展示程序根、`App_Data`、storage policy、服务器路径边界等开发审查文字；技术路径集中在管理员“运行检查”。维护工具拆为团队库、数据归属、发票清理、运行检查和问题诊断五个任务页签，英文确认口令改为中文影响确认，服务端安全契约保持不变；发票清理单独限制为管理员的已作废数据维护，不把危险删除混入普通发票编辑页；响应式表单采用自动换列，普通窗口和小窗口不再因旧 owner、付款报销等字段产生页面级横向溢出。

装柜模拟 PDF 定型为现场作业单，只包含方案/柜型、装载摘要、现场提示、货物清单、伪 3D 效果图、俯视/侧视/柜门图和颜色图例；可旋转 Three.js 3D 与操作控件不进入 PDF。浏览器使用内存 Blob 下载，Tauri 使用原生保存对话框并由受限 PDF 写入命令保存用户显式路径，不新增数据库、服务器导出副本、系统临时目录或 C 盘默认落点。专项记录见 `docs/商业化界面与装柜现场PDF重构记录.md`。

### 1.9 SQLite 单机与 PostgreSQL 多人运行模式（2026-07-18 已加固）

数据库模式不再按“同一种部署随意切换”理解，而是固定为两条清晰产品边界：

- `SQLite` 只允许单机、单进程、回环地址使用。数据库文件位于当前运行数据根 `Database/`；连接启用外键、10 秒 busy timeout、连接池和 shared cache，初始化启用 `WAL + synchronous=NORMAL + foreign_keys=ON`。API 使用运行数据根 `Cache/Locks/sqlite-single-instance.lock` 持有排他租约，Tauri 桌面壳也启用单窗口插件；重复启动会恢复并聚焦现有窗口，不能让两个进程同时写同一数据库。它适合一名用户在一台电脑上使用，不允许把 `.db` 文件放到 NAS/共享文件夹后由多台电脑同时打开，也不允许网络监听时继续使用 SQLite。
- 单机版使用标准开源 SQLite，不使用 SQLCipher，也不再保留“密码已加密数据库”的伪逻辑。`.db` 文件本身是标准 SQLite 明文格式；需要静态数据保密的电脑应使用 BitLocker、FileVault、LUKS 或等效整盘/目录加密，并结合系统账号权限和备份介质加密。该选择保持依赖精简、跨平台一致和普通用户免安装额外数据库组件。
- `PostgreSQL` 用于局域网、Docker 和浏览器多人共享。用户会话写 `ApiUserSessions`，只保存不可逆 token 摘要；后台任务写 `ApiBackgroundJobs`，按单任务 upsert/删除，不再整表覆盖。API 重启后未完成任务会明确标记失败并保留可重试描述，已完成历史继续可查询。连接池默认上限为 30、最小预热 2、空闲连接 300 秒回收，可用 `EXPORTDOCMANAGER_DB_MAX_POOL_SIZE` 在 5–200 范围内按数据库规格调整，避免大量浏览器请求把 PostgreSQL 连接数无限放大。
- 跨数据库业务日期在共享仓储边界统一规范化：浏览器 `YYYY-MM-DD`、桌面表单和后台导出传入的无时区日期保留原日历字段并标记为 UTC，再与 PostgreSQL `timestamp with time zone` 比较；SQLite 继续使用同一查询模型。不得在各前端或各 API 端点分别复制日期转换，也不得用数据库 Provider 分支掩盖不一致语义。
- 数据库建表、结构检查和种子数据由进程级初始化协调器只成功执行一次；PostgreSQL 额外使用 advisory lock 防止并发初始化。认证中间件、持久会话签发/校验/撤销均走异步数据库调用，不在请求线程执行同步 PostgreSQL I/O。
- CRM 客户/联系人/跟进、商机、供应商/联系人/供货关系/评价、邮件模板、用户报表模板和付款记录均携带版本号或 RowVersion；前端保存时回传当前版本，服务端在数据库更新条件中校验并在冲突时返回 HTTP 409，避免多人编辑静默覆盖。批量状态更新也会递增版本，使已打开的旧编辑页必须刷新。
- 后台任务固定保存提交账号 ID；任务真正开始前再次读取当前启用账号和有效权限。账号在排队期间被停用或删除时，任务委托不会执行，并以明确失败原因结束。
- 单个 API 服务实例默认最多同时运行 4 个后台任务、每个用户 2 个、浏览器/PDF 重任务 2 个；可用 `EXPORTDOCMANAGER_JOB_GLOBAL_CONCURRENCY`、`EXPORTDOCMANAGER_JOB_USER_CONCURRENCY`、`EXPORTDOCMANAGER_JOB_BROWSER_CONCURRENCY` 按服务器资源调整。排队任务先取得用户/浏览器容量再占全局容量，避免某一用户的等待任务阻塞其他用户。
- 后台任务另有“运行并发”和“排队容量”两层门禁：全局默认最多保留 64 个未结束任务、单用户 12 个，可用 `EXPORTDOCMANAGER_JOB_GLOBAL_QUEUE_LIMIT`、`EXPORTDOCMANAGER_JOB_USER_QUEUE_LIMIT` 调整。超过上限立即返回 429 和普通用户可理解的等待提示，不再无限创建等待任务。用户任务信号量在该用户最后一个任务结束后删除，避免长期运行时按历史用户名持续增长。
- 任务历史默认保留 30 天、全局 2000 条、单用户 200 条，可用 `EXPORTDOCMANAGER_JOB_HISTORY_DAYS`、`EXPORTDOCMANAGER_JOB_HISTORY_LIMIT`、`EXPORTDOCMANAGER_JOB_USER_HISTORY_LIMIT` 调整；清理只处理已结束任务及其受控浏览器输出，不删除排队/运行任务。运行中进度仍实时保存在内存，但持久化最多约每秒一次，降低 SQLite JSON 整体重写和 PostgreSQL 高频 upsert 竞争。
- 受控 Playwright 宿主允许多个 BrowserContext 共用一个登记进程。单任务超时先关闭自己的 Page/Context；如同时还有其它页面，进程回收延迟到最后一个页面结束，避免一个用户超时中断另一个用户。宿主按次数/时间回收，并在启动时清理已退出 API 进程遗留的 `Cache/BrowserRuntime/automation-*`；PDF Chromium 仍为每任务独立进程，取消、超时和异常都终止进程树并重试清理临时目录。
- 管理员的数据归属改派已覆盖发票、付款、CRM 客户/跟进、供应商、商机、邮件模板、用户报表模板和装柜方案；停用账号不能成为目标，同一来源/目标会拒绝。大批量改派每 500 条保存并释放 EF 跟踪缓存，同时保持一个数据库事务和审计记录，避免一次性加载全部业务数据。
- 当前多人目标是“一套 PostgreSQL + 一个 API 服务实例 + 多个浏览器用户”，适合普通小团队和单台服务器。数据库记录已避免不同服务实例互相整表删除，但执行中取消令牌、任务领取和浏览器容量仍属于本 API 进程；在增加多个 API 副本前必须再实现数据库租约或队列领取，不能把当前版本直接声明为无状态横向扩展。
- 当前阶段不强制引入 Redis、Kafka、RabbitMQ 或商业数据库组件。若以后达到多 API 副本、数十个并行 PDF/OCR 任务或共享文件集群，再按实际负载增加 Redis/消息队列和 NAS/MinIO；不要为尚不存在的规模提前增加部署复杂度。
- 当前工作区最新 .NET 8 Release 回归为 `609/609`（Domain `38`、Application `147`、Infrastructure `177`、API `247`）。`PostgreSqlIntegrationTests` 只有在设置 `EXPORTDOC_TEST_POSTGRES_CONNECTION_STRING` 时才连接真实服务器，未设置时直接结束。因此这些数字证明当前代码回归通过，不证明真实 PostgreSQL、容器 volume、备份恢复或多人压力已经验收；这些项目必须在专用数据库/容器环境单独记录结果。

### 1.10 PP-OCRv6 Linux x64 与 Docker x64（2026-07-19 已接入）

- OCR 已统一为 `.NET IOcrService -> 长驻 Rust Sidecar -> PP-OCRv6 ONNX`。Sidecar 使用 `ort + image + ndarray` 完成解码、预处理、检测后处理和 CTC 解码；旧 C# OpenCvSharp OCR、原生运行包和运行时回退均已删除。
- 信用证 PDF 仅在无法提取有效文本时进行逐页栅格化并调用 Rust OCR，这属于 OCR 输入适配，不扩展为通用图片/PDF编辑模块。
- Excel批量结构分析优先使用随包 Rust `calamine` 分析器，.NET保留旧 `.xls` 和异常文件的兼容读取与业务映射；Rust只生成预览报告，不直接写数据库。
- Windows x64/ARM64、Linux x64/ARM64 与 macOS x64/ARM64 均构建对应 Rust Sidecar；发布包携带 PP-OCRv6 模型和 ONNX Runtime native，不安装 Python、OpenCV、PaddlePaddle Python 环境、系统 OCR 服务或商业 OCR SDK。
- ONNX Runtime 统一锁定为官方仍同时覆盖上述六种 RID 的最后版本 `1.23.2`；Rust `ort` 只使用该版本提供的 C API 23，因此同一 Sidecar 源码也可加载更新平台上的兼容运行库。上游从后续版本起停止发布 Intel macOS native，项目不使用来源不明的第三方二进制，也不在构建时临时编译一套未治理的替代运行库。
- 独立浏览器服务器包和 Docker 包在构建阶段执行 `--verify-ocr-runtime` 完成真实识别，失败则不生成发布包；可用 `EXPORTDOCMANAGER_OCR_RUNTIME=disabled` 显式关闭 OCR。
- 发布资源按 `Desktop / Server / Container` 配置生成：Rust模块共享程序根唯一ONNX Runtime；桌面和服务器只携带当前RID浏览器；Container使用发行版Chromium且不携带 `Browsers/`；Release包不包含PDB、`.lib`或未使用的ONNX托管/provider文件。
- 权限导航以API返回的 `moduleAccess` 为唯一现代契约：明确空权限不显示任何业务导航并进入无权限说明页；模块页面按view/operate/manage控制只读和写操作，后端中间件继续执行同级别强制授权，前端隐藏不能替代后端校验。
- Docker API 镜像在 Linux amd64 构建层执行同一真实 OCR 验证，并安装 `libgomp1`；运行时默认 `EXPORTDOCMANAGER_OCR_RUNTIME=enabled`。Linux arm64 镜像仍可构建其它功能，但明确跳过 OCR 验证并由运行时诊断返回架构不支持。
- PP-OCRv6 det/rec 模型与 PaddleOCR 为 Apache-2.0；ONNX Runtime 为 MIT；Rust依赖按各 crate 的 MIT/Apache-2.0 许可发布。具体来源记录随模型放在 `OcrModels/PaddleOCR/V6/THIRD_PARTY_NOTICES.md`。

## 2. 数据边界

- 原单证 `Customer`、`Invoice`、`Item`、报表 DTO、Scriban 字段和打印模板保持不变。
- 销售模块使用独立的 `CrmCustomer / CrmContact / CrmFollowUp`，不把销售字段堆入单证客户表。
- 供应链资料使用独立的 `SupplierCompany / SupplierContact / SupplierProductLink`；供货关系通过显式关联表连接现有产品，只读取产品编号和名称，不向产品表写供应商字段。
- `CrmCustomer.LinkedDocumentCustomerId` 只是可选桥接键；CRM 不反向修改单证客户。
- SQLite 未投产，数据库按当前模型直接重建，不增加旧结构兼容、双读双写或历史迁移胶水。
- SQLite 只作为单机单进程数据库；多人 Web/容器部署必须使用 PostgreSQL，不能通过共享 `.db` 文件绕过该边界。
- 项目尚未正式投产时，API、生成客户端、前端模型和运行配置统一以当前代码为唯一契约；字段调整直接同步修改并重新生成，不保留旧 sidecar、旧前端状态或旧配置猜测分支。
- PostgreSQL/局域网模式沿用现有用户归属范围；普通账号默认只能访问本人 CRM 数据，管理员可查看全部；会话和后台任务状态也进入共享数据库，不依赖某个浏览器或系统盘用户目录。

## 3. 外贸综合工具迁移取舍

- 发票报表预览与输出采用“API/路径调度协调器、标题状态区、模板快捷工具区、单据包配置、邮件附件、预览画布、纯输出模型”分层。2026-07-14 已将原 1874 行组件拆为最大 998 行的 7 个职责文件；PDF、托单 Excel、ZIP/目录单据包、打印和邮件仍复用当前 API 与显式路径规则，并通过真实报表桌面 smoke。
- 报表打印采用“iframe 生命周期协调器 + 纯打印文档构造器”分层。纯构造器只根据内存 HTML 注入标准 CSS `@page` 空页边距盒，清除 Chromium 日期、标题、URL 和页码槽位，不覆盖模板的纸张方向、四边页边距、`body` 版式或分页类；因此发票/装箱单显式分页、合同自然分页、报关单横向分页和新版设计器结构化分页统一继承各自模板规则。打印不改模板文件，导出 PDF 继续走后端 Chromium 链路，两条输出共享模板而不共享平台适配实现。
- 发票商品明细编辑器按“交互协调器、商品库弹窗、单元格输入、编辑工具栏、辅助候选区、虚拟化表格、剪贴板/选区/金额纯模型”分层。2026-07-14 已将原 2314 行组件完整拆为最大 998 行的 9 个职责文件；发票保存、金额重算、商品库、键盘导航和虚拟化行为保持当前唯一契约，并通过真实桌面发票明细 smoke。
- 装箱模拟器前端采用“状态/API 协调器 + 类型化工作区 + 请求模型 + 可视化编排 + 伪 3D 几何模型 + 正投影视图 + Three.js 场景”的分层结构。方案与柜型持久化仍只通过现有 API 写运行数据根数据库；分析和全部 2D/3D 渲染只处理内存请求/响应，不创建额外缓存或系统盘目录。2026-07-14 已将原 2258 行单文件完整拆为最大 886 行的 8 个职责文件，并通过 Web 构建、三层专项测试和真实桌面装箱 smoke。

| 功能 | 决策 | 原因 |
| --- | --- | --- |
| 客户、联系人、跟进 | 分阶段迁入，独立 CRM 模块 | 简单、常用，适合业务员；不影响单证主数据 |
| 供应商、供应商联系人 | 已建立独立模块，继续渐进完善 | 与采购/产品来源相关，但不混入客户 CRM 或单证客户 |
| 邮件模板 | 已建立独立业务模块 | 模板 CRUD、变量、预览、单封邮件套用、团队共享和追加式版本历史已落地；不直接发送、不承担营销活动 |
| 邮件活动/群发 | 暂缓 | 必须同时具备退订、抑制名单、频控、发送审计和合规说明，否则不符合商业软件要求 |
| 线索采集、Meilisearch、Valkey | 不迁入简版 | 增加部署和使用复杂度，当前收益不足 |
| 外部 Python/OpenCV OCR 运行时 | 不迁入 | 全平台统一使用 Rust Sidecar + ONNX Runtime，不包含 Python 或 OpenCV 运行时 |

## 4. 运行与依赖原则

- 数据库、日志、缓存、WebView、备份和业务文件继续放在程序运行数据根或用户显式目录，不默认写系统 C 盘。Tauri 在运行数据根解析成功后，启动失败与 panic 诊断也优先写该数据根 `Logs/`；只有数据根尚未解析或日志目录不可写时才尝试程序根 `logs/` 诊断例外，全部候选不可写时直接在错误弹窗说明未生成日志，不再显示虚假文件路径。
- 程序根 `Templates/Resources/Browsers/Tools/OcrModels` 路径只做解析和存在性诊断，不因健康检查、设置页浏览或依赖发现自动创建；`Templates/` 是唯一报表模板根，内置模板、新建模板、编辑/重命名/删除和模板包导入继续统一使用 `Templates/Export` 或 `Templates/Internal`，不增加第二套模板目录。管理员可在设置页显式执行可写性检查；检查只在 `Templates/` 创建并立即删除短生命周期探针。API `/healthz` 的匿名/普通请求走轻量公开响应，只有管理员或可信桌面连接才通过结构化 `runtimePaths` 与 `runtimeDependencies` 说明程序资源、运行数据、当前数据库文件，以及浏览器、OCR、PostgreSQL 工具的真实就绪状态；依赖检查只读取程序目录，不启动外部工具、不自动下载。
- 构建缓存继续使用仓库 `.codex-runtime` 与 `artifacts`；Rust/Cargo 路径由脚本执行非系统盘校验。
- 发布脚本按版本化目标整体替换，不把上次构建的浏览器平台、Playwright 开发 UI、未知字体、重复 ONNX Runtime 或内部注册机带入客户包。单版与三版 Windows 便携输出都自动执行载荷审计；未请求内部注册机时清理旧 `KEY/`，安装器只替换本次请求版本的旧文件。浏览器 PDF 验证工作目录固定在仓库 `.codex-runtime/browser-pdf-check`，Linux ARM64 Chromium 只有在 manifest、架构、版本、相对可执行路径和实际启动全部通过后才复用。
- .NET CLI、NuGet、npm 与构建 `TEMP/TMP` 统一优先进入仓库 `.codex-runtime/`；Tauri bundle 不再静默使用系统用户 `~/.cargo`，而是读取显式 `CARGO_HOME` 或从 `PATH` 中的 Cargo 推导工具链根，Windows 推导到系统盘时明确失败。开发机已安装的 `dotnet.exe`、系统 `curl.exe` 可作为工具入口，但缓存、临时文件、依赖下载和构建产物不随之写入系统盘用户目录。
- 三种桌面版本通过 `scripts/run-tauri-local.ps1 -ProductEdition Document|Sales|Full` 构建；Windows 便携目录脚本按版本输出独立目录。
- 局域网/容器 API 默认仍只允许回环监听；只有显式启用 `EXPORTDOCMANAGER_NETWORK_MODE=true` 且数据库为完整 PostgreSQL 配置时才允许 `0.0.0.0` 等网络监听。基础 Compose 正式支持受控内网 HTTP，不要求证书或 HTTPS overlay；公网、跨不可信网络或有合规要求时再启用 `docker-compose.https.yml`，或由外部网关终止 TLS。Compose 的 Nginx（`nginx:1.30.4-alpine3.24`）在 HTTP 和 HTTPS 两种模式都提供 React 静态文件、SPA fallback、同源 `/api` 代理和安全响应头，因此不能把“内网不强制 HTTPS”理解为直接删除 Web/代理层；非 Docker 浏览器服务器包才是无需 Nginx 的正式替代方案。跨域前端必须进入 `EXPORTDOCMANAGER_ALLOWED_ORIGINS` 白名单，API 端口不直接公开。容器数据库镜像固定 `postgres:18.4-trixie`，使用 Debian/glibc 多架构镜像；初始化显式选择 PostgreSQL 内置 `PG_UNICODE_FAST` Unicode locale，避免默认排序依赖 glibc/musl。PostgreSQL 18 数据卷挂载到 `/var/lib/postgresql`，跨大版本升级必须先完成备份恢复演练。
- 不新增 Python、Go、Next.js、Valkey、Meilisearch 或第二套数据库运行栈。
- 新增 UI 依赖必须优先选择免费开源、跨平台、无云服务绑定且仍在维护的库；现有 React、Lucide 和项目组件可以满足时不为视觉效果重复引入依赖。
- GitHub 公开仓库只包含客户端公钥验签，不包含 `apps/license-keygen-tauri/`、签发私钥、`KEY/` 内部工具或注册机产物。Windows 客户程序默认不构建注册机，私有源码存在且显式传入 `-IncludeLicenseKeygen` 时才生成内部工具。公开源码在本机脚本和 GitHub Actions 两层执行敏感路径、签发材料和 95 MiB 大文件门禁。
- Docker 公开发布使用 GitHub Actions 构建 API/Web 的 Linux amd64/arm64 镜像并保存到 GHCR；另提供 Windows/Linux 非 Docker 浏览器服务器包，由单个 ASP.NET Core 进程同源托管 React 与 API，连接部署机原生 PostgreSQL。GitHub Pages 不能运行 ASP.NET Core/PostgreSQL，不视为完整网页版托管。容器内 Chromium 来自 Debian 开源软件包并显式使用 `/usr/bin/chromium`；非 Docker 服务器包在构建期下载当前平台 Chrome Headless Shell 并打入发布包，源码仓库不提交数百 MB 浏览器副本。
- 产品版本、岗位和管理员能力必须在导航过滤、设置分类、前端路由和 API 中间件四层对齐；新增功能先声明所属 `document / sales / common / full-admin` 能力，再接入页面和端点，禁止仅靠侧栏隐藏形成表面权限。
- 跨工作区只读基础资料必须通过隐藏技术模块声明依赖，不能为了读取商品等选项直接授予整个主数据维护模块。当前商品读取统一使用 `common.product-reference`，发票、商机和供应商按需自动获得 `view`；商品新增、修改、删除仍只属于 `document.master-data`。
- 设置中心采用“状态协调壳 + 分类级独立组件 + 按需加载”结构：主页面只管理统一读取、保存、校验、消息和分类导航，运行/数据库、模板、Excel 导入、汇率、邮件/备份、AI/单一窗口和维护工具分别维护自己的展示与交互；进入分类前不加载对应代码，也不提前查询模板、签证机构或运行诊断数据。账号与权限是独立管理员工作区，不再嵌入设置分类。
- OpenAPI 以当前未投产契约为唯一事实源，文档工厂只负责顶层信息、服务器、安全方案、路径/Schema 合并和共享构造 helper；路径与 Schema 按系统、工具、单据、单一窗口、销售、主数据及公共模型分区维护，不在单个巨型工厂中继续追加，也不为旧客户端保留重复 Schema 或适配目录。
- Excel 发票导入采用“导入协调器 + 发票头 + 客户/出口商 + 主体块识别 + 普通商品明细 + 明细布局分析 + 订舱小表 + 字段查找/文本归一化 + 工作簿读取适配器”结构；协调器只负责分析器调用、工作表选择、取消和结果组装，`.xlsx`/`.xls` 读取、真实业务表头识别与各业务对象解析分别维护。字段类型校验与保守派生独立于单元格位置：目的国必须是可识别国家文本，不能接受金额；缺少独立目的国时只从明确国家名称的目的地或客户地址推断。导入草稿进入发票编辑器后由前端纯模型统一把 ASCII 小写字母转为大写，并提供相同规则的手动按钮；中文内容和数值不改。新增格式应扩展对应解析域和回归样本，不得重新把标签、主体、商品、订舱表和两种工作簿实现堆回单一服务文件。
- 单一窗口大型编辑器遵循“页面协调壳 + 专项编辑组件 + 纯业务规则模型”结构：页面只负责查询、mutation、草稿状态、撤销和跨组件编排，商品/附件/第三方企业等复杂工作区独立维护，条件显示、保存归一化、描述生成和代码转换保持可独立测试；不得重新把 JSX、文件交互与纯规则堆回单一页面文件。
- 单一窗口操作中心采用“极薄路由出口 + 独立导航 + 批次列表页 + 协作页 + 批次详情/客户端桥 + 回执包工作区 + 结果展示 + 通用表格 + 状态/路径模型”结构；COO、ACD 和参考词典只依赖轻量导航，不反向加载整个操作中心。提交包、客户端 OutBox/SentBox/InBox/FailBox、回执收集/导入和批次记录分别维护，新增操作中心动作不得重新堆回统一页面文件。
- 单一窗口参考词典采用“页面协调器 + 纯目录/校验/批量编辑模型 + 摘要展示”结构。六类词典定义、别名归一化、批量粘贴、重复校验、去重合并与 Excel 映射请求由纯模型统一维护，页面只协调 API、未保存保护和表格交互；模型可在 Windows/Linux/macOS 的 Node 构建环境独立回归，不创建本地词典副本或浏览器持久化。
- 报表设计器属性系统采用“主协调器 + 类型专项属性编辑器 + 明细表子域编辑器 + 共享控件 + 纯归一化模型”结构：主面板只负责当前页面、版区和组件类型的选择编排，页面/打印、网格、行、条件、图片和明细表分别维护；明细表继续按版式分页、分组小计、列内容、表尾汇总拆分。所有归一化与空单元格构造集中在纯模型，字段路径、列宽、边框和文字样式使用共享控件，不得把类型分支、列编辑和归一化规则重新堆回单一属性面板。
- 报表模板工作区采用“页面状态协调壳 + 查询/业务 mutation hook + 选择同步/选择动作 hook + 纯选择模型 + 纯派生状态模型 + 模板选择面板 + 工作区头部 + 用户/管理员/模板包面板 + 新版/源码编辑工作区 + 独立预览工作区 + 反馈组件”结构。预览 HTML、busy 聚合、内容有效性和全部操作可用性由纯派生函数计算；类型/模板/预览源切换动作独立维护；新版设计器和源码编辑器共享独立工作区。主协调页只保留状态所有权、成功回调和跨域流程编排，文件保持 `999` 行。后续新增功能应扩展对应模型、hook 或面板，不重新向协调页堆叠业务分支。
- 基础资料工作区采用“路由壳 + 通用列表 + 通用编辑器 + 字段控件 + 实体配置目录 + 纯辅助模型 + HS 专项工具”结构：客户、出口商、收款对象、商品、港口、单位和 HS 编码共享一致的搜索、分页、编辑、删除和键盘操作，不为每个实体复制页面；商品/单位辅助、HS 联网解析与清空保护保持独立，新增基础资料实体应优先扩展配置目录和必要的专项组件，不得重新把全部 CRUD、字段和工具堆入单一页面。
- HS 编码专项采用“本地有效性库 + 智能 Excel 预检/提交 + 联网参考查询”三段边界。HS Excel 只进入 HS 专项分析器，不复用或修改商业发票 Excel 分析链；增量资料不得因文件缺失停用本地编码，只有用户明确选择完整年度库才可标记疑似作废。历史商业发票保留原编码，本地商品候选只使用有效编码。联网 Provider 的静态解析优先按可信详情链接选择结果表，并兼容 i5a6 当前 `hscasefind` 申报实例结构；静态无结果或详情失败才进入受控浏览器降级。前端列表、导入向导和联网结果按任务分离，不在一个页面同时铺开完整表单、差异表和远程结果。
- HS Excel语义覆盖中国第三方税则常见变体：`规格型号`可作为申报要素、`许可证代码`作为监管条件，普通/优惠或最惠国/出口/消费/增值税率与出口退税率分别存储；同一Excel列只允许映射一个标准字段。新增供应商格式应以真实工作簿预检结果补充别名和回归样本，不把特定文件列号硬编码进导入器。
- 浏览器能力采用“共享治理、进程隔离”：`BrowserRuntimeManager / BrowserExecutableResolver` 统一负责运行目录浏览器定位、全局资源预算、PID归属、取消超时和退出清理；PDF使用每任务独立命令行Chromium，HS动态网页降级使用受控Chromium + Playwright CDP宿主。两类业务不共享Browser进程或页面状态，避免PDF打印参数与网页自动化会话互相污染。新增浏览器业务必须接入运行时管理器，不得自行扫描/结束Chrome进程。
- 面向普通用户的销售页面遵循“单页单主要任务”：目录、编辑、导入导出、关联、预览和历史通过清晰分区切换，不在一个屏幕同时铺开全部功能；供应产品等子模块内部也必须把关系目录和新增/编辑表单分开，不能因为已经位于上层任务页就再次把列表与完整表单堆在一起。
- 销售工作区任务分区使用统一页签语义和共享视觉规则；普通窗口保持双列表单，小于 `860px` 自动改为单列，小于 `560px` 时操作按钮纵向铺满，任务页签横向滚动而不把所有功能挤入同一屏幕。页签支持键盘方向键、Home 和 End 切换。
- 销售概览只在客户、联系人、待跟进和进行中商机均为空时显示三步首次使用引导；已有业务数据后自动回到统计和待办视图。概览指标可直达对应任务，客户与商机页面通过轻量 `?view=` 参数打开指定分区，不保存额外用户状态。
- 商业化界面优先使用一致的状态语言、主次按钮、搜索分页、确认提示和空状态；业务状态统一按积极、跟进提醒、风险、停用和普通信息显示，保存成功、输入提醒与接口错误使用不同反馈语义；目录中的客户、商机、供应商、产品和邮件主题等长文本保持紧凑单行并提供完整悬停提示，避免普通窗口被异常数据撑宽；目录筛选不得导致刚保存的记录从编辑上下文中消失，必要时清除会隐藏新记录的筛选并保持当前记录选中；高级能力逐步显示，不要求普通用户理解内部模块或数据库结构。

## 5. 精简后的文档入口

日常开发和审查先看四份核心文档：

1. 本文：产品版本、长期架构约束、模块边界、迁移取舍和文档导航。
2. `docs/程序改进重构进度文档.md`：每轮代码变更、验证证据、完成度和下一步。
3. `docs/运行目录与路径存储审查清单.md`：数据库、依赖、缓存、构建产物和非系统盘审查。
4. `docs/整体项目核查与程序改进重构规划-2026-07-23.md`：整体商业化核查、风险分级和发布前门禁。

按专项需要再查阅：

- `docs/单一窗口对接代码级设计.md` 与 `docs/单一窗口字段映射清单.md`：官方客户端交接、XML、回执和字段映射真源；两份文档职责不同，不强行合并。
- `docs/新版报表设计器进度参考文档.md`：结构化 schema、画布、打印模型、实施状态和剩余计划的唯一真源。
- `docs/Tauri正式更新签名与发布配置.md`：管理员更新地址、固定公钥、发布私钥、签名和轮换。
- `docs/GitHub Actions工作流用途与运行手册.md`、`docs/GitHub开源发布与Docker镜像说明.md`、`docs/多平台与多架构支持矩阵.md`：CI、发布、镜像和平台支持。
- `docs/全项目导出下载契约重构与多平台审查记录.md`、`docs/审计日志维护交互重构与跨平台审查记录.md`、`docs/商业化界面与装柜现场PDF重构记录.md`、`docs/默认报表模板与账号管理商业化复审记录.md`、`docs/报表模板多用户权限设计与实施记录.md`：仍有效的专项审计证据。
- `docs/字体许可与跨平台报表审查记录.md`、`docs/Windows构建脚本与前端多平台审查记录.md`：字体许可、报表输出和 Windows 构建验收。

后续增量默认只写入“进度文档 + 路径清单”；只有长期架构、产品边界或专项契约发生变化时才更新对应文档。已经被现行真源覆盖的阶段性方案不继续保留，避免审查时同时读到相互冲突的旧口径。

## 6. 当前完成度

| 范围 | 完成度 | 说明 |
| --- | ---: | --- |
| 原单证功能 Tauri/Web 迁移 | 99% | 现有构建与测试基线保持，仍待真实多平台设备验收 |
| 发票管理与智能 HS 闭环 | 99% | 状态命令与历史、草稿删除/正式状态作废/已作废审计留存、管理员独立清理审计、发票乐观并发、重复单号保护、服务端派生重算、PostgreSQL 发票范围、保存事务内 HS 反馈、三次共识实例、远程只读/capture 分流和知识包导出权限已落地；仍待真实 PostgreSQL 多岗位、百万级数据与跨平台真机验收 |
| 多版本能力与账号工作区 | 99% | 三版本导航/API 隔离、桌面/浏览器运行形态显隐、Full 权限模板 CRUD/分配、业务权限自动补齐技术依赖、共享商品只读能力、独立“账号与权限”导航/路由、中文岗位与账号/模板页签、管理员增删改停用和密码重置、首管理员部署侧初始化、登录期模块访问级别、模块与操作级服务端授权，以及发票、付款、查询、OCR、邮件、CRM、商机、邮件模板、供应商、主数据、任务中心、Excel 工具、装箱模拟和单一窗口全页面只读控件态已落地；默认关闭公开自注册。发票列表、编辑、商品库写入、Excel 托单、COO/ACD 编辑器、操作中心、协作、回执包、参考词典和报表输出按关联模块分别授权，Finance 默认只进入付款模板类型。主数据模板会自动获得基础资料、商品读取和候选项技术依赖，Excel 不会在缺少发票权限时发起无关查询。显式 network mode、PostgreSQL/Nginx 容器部署骨架、网络诊断脱敏和邮件附件路径隔离保持不变。仍待安装 Docker 的真实 PostgreSQL 四岗位浏览器验收、代码签名、更新通道和多平台多架构实机验收 |
| 多用户运行基础设施 | 97% | SQLite/API/Tauri 单实例门禁、一次化初始化、PostgreSQL 持久异步会话和有界连接池、账号变更会话撤销、停用账号排队任务阻断、全业务归属改派、数据库任务历史、任务排队上限/历史收敛/进度写入节流、全局/用户/浏览器任务限流、浏览器并行页面安全回收，以及主要可编辑业务实体和共享主数据乐观并发已完成；当前面向单 API 服务实例的小团队多人使用。剩余 3% 是连接真实 PostgreSQL/Docker 后的四岗位并发、压力、重启、备份恢复和反向代理验收，以及未来多 API 副本所需的数据库租约/队列领取和共享文件存储，不提前计为已完成 |
| 销售 CRM 第一阶段 | 93% | 客户/联系人、跟进、概览、导入导出、批量状态和邮件变量草稿已落地；跟进列表与新增/编辑已拆成独立任务，保存、长摘要、待跟进/逾期/完成状态和真实窄屏闭环已验收，更完整销售分析待补 |
| 商机与报价跟踪 | 83% | CRUD、漏斗、币种金额、近期提醒和追加式阶段/报价版本历史已落地；编辑表单已按业务顺序分组、可选记录默认折叠并完成真实窄屏验收，正式报价文件仍不在本模块范围 |
| 供应商模块 | 91% | 独立 CRUD、联系人、导入导出、批量状态、产品受控关联、四维供应商评价、单体趋势/分布分析及跨供应商只读采购概览已落地；概览只按每家最新人工评价展示覆盖率、结论和四项均分，不虚构准时率、合格率或自动排名，真实安装版业务验收和采购订单事实分析待补 |
| 邮件模板 | 82% | 独立 CRUD、分类、变量安全替换、预览、CRM 客户草稿、单封邮件套用、模板复制、未保存保护、最小团队共享及追加式版本历史/恢复已落地；PostgreSQL 普通账号可读本人及共享模板但只能维护本人模板，共享模板可查看历史/套用/复制但不能恢复，更多真实邮件样本与安装版人工验收待补 |
| 邮件活动/群发 | 暂缓 | 完成合规设计后再评估 |
| 新商业化总体目标 | 约 97% | 普通业务页技术文案清理、维护工具任务化、普通/窄窗口溢出治理、手机抽屉导航、静态业务结构与轻量 CSS 动效登录页、移动端任务优先级及装柜现场 PDF 已落地；当前主要差距集中在真实 Docker/PostgreSQL、多平台实机、签名更新链和复杂业务样本人工验收 |
| GitHub 与容器发布工程 | 90% | 本地 Git 仓库、公开源码守卫、注册机/私钥排除、Docker build context、GHCR 多架构工作流和镜像 Compose 已完成；自动依赖 PR 已关闭，依赖升级改为人工审查后集中处理。仍待在 GitHub Actions/GHCR 和装有 Docker 的真实主机完成首轮稳定构建、拉取与四岗位验收 |

后续整体规划按风险和收益排序：第一阶段完成发布工程稳定性、真实 Docker/PostgreSQL 四岗位验收、签名和更新链；第二阶段继续拆分超过千行的业务协调页与 1 万行全局样式表，优先处理发票编辑、单一窗口 COO/ACD、报表 schema 校验和 CSS 领域化；第三阶段补销售分析、供应商事实指标和复杂业务样本验收。前端继续复用 React、TanStack Query、Lucide、Three.js、html2canvas 和 jsPDF 等免费开源库，不为了视觉效果引入重型商业组件。
## HS 编码知识中心（2026-07-20）

HS 模块采用“当前年度标准编码 + 已审核申报实例 + 替代关系 + 用户反馈”四层模型。标准编码用于判断当前可用性；正式实例只保存归类所需的普通商品名称和规格；替代关系处理年度变更；反馈在当前数据库部署内形成可审查的推荐证据。联网 Provider 是可替换的补充来源，不是本地查询的强依赖。单机知识可通过 `.edmhs` 在两台电脑间迁移，且不携带商业发票等业务数据。

前端入口为 `/master-data/hs-knowledge/:section`，侧栏归入“申报与归类”、子菜单为“HS 编码知识”，任务分为 `search / examples / history / annual / transfer / online`；原 `/master-data/hs-codes` 保持标准编码列表和人工编辑职责。知识中心路由不会再同时激活“主数据维护”。
知识中心页面不重复渲染应用工作区顶部已经提供的英文眉题、中文标题、总说明和返回按钮，内容区直接从任务标签开始。六个任务标签在非选中状态也使用清晰边框和实体底色，避免把一级任务导航表现成弱化文字链接；年度税则和联网补充的任务提示采用收窄短说明，不横跨整张工作卡片。
历史资料学习属于“候选发现层”，只读取 `Products / Items / CustomsCooItems` 并返回待审核视图，不直接写 `HsCodeDeclarationExamples`。人工确认是候选进入正式知识层的唯一入口，确保系统推测不会被当作事实。
“同义词”与“相关词”分开处理：全棉/纯棉等可安全归一的表达进入同义词层；针织/钩编等相关但不等价的工艺只参与相关度加分，不改写知识事实。
联网 Provider 输出先进入 `HsCodeRemoteCandidates` 候选层；`HsCodeDeclarationExamples` 只保存人工确认实例。候选层不能被搜索服务当作正式证据，审核服务是两层之间唯一写入通道。
Linux ARM64浏览器运行时采用随包 `Browsers/ChromiumArm64`，来源和产品标识与Google官方Chrome Headless Shell分离。浏览器解析器不关心发行品牌，只通过统一可执行文件契约向PDF和网页自动化提供能力。Linux桌面仍同时交付deb/AppImage；AppImage构建跳过 `linuxdeploy` 的旧版重复strip，并保留verbose子进程日志，这属于临时runner打包兼容配置，不改变程序运行目录、数据库或浏览器资源落点。

### 共享知识库、业务候选与数据治理（2026-07-24）

正式 `HsCodes`、年度替代关系、公开申报实例以及人工审核通过的学习结果属于同一部署实例的共享参考知识，不设置个人所有者。具备 HS 查询权限的用户可以共同检索，避免每个账号重复导入上万条税则或各自积累互不相通的归类经验；2026-07-26 起导入、审核、修改和删除统一要求 `manage`，查询共享不等于所有用户都能维护知识。

共享知识与原始业务历史必须保持分层。商品主数据沿用既有共享主数据权限；PostgreSQL 多用户模式下，发票明细和由发票派生的报关资料只在当前用户可访问的业务范围内参与“历史资料学习”。候选审核页面可以在该权限范围内展示近期来源和款式样例，人工确认后写入共享知识层的仅有规范商品名称、成分/规格、申报要素语义、HS 编码、来源类别、年度、可信状态和统计信息，不写客户、发票号、合同号、金额、联系人、业务记录 ID 或款号。原始发票和报关资料不会因为参与候选发现而变成全局可见。

这一边界符合企业商用软件的常见“共享主数据 + 私有业务单据 + 审核发布”模式。当前所称共享范围是同一个公司部署/数据库实例，不自动等同于未来 SaaS 的跨租户平台共享；如果以后引入多个企业租户，官方公开税则仍可平台共享，企业自有学习知识应默认停留在租户内，只有经过明确脱敏、审核和来源合规确认后才允许提升为平台公共知识。

正式税则一万级规模不需要为查询单独引入 Redis。当前优先使用数据库索引、服务端过滤与分页、数字前缀查询、有界候选窗口、前端短期查询缓存和请求取消；只有未来形成多 API 节点、跨节点热点缓存和明确失效一致性需求时，才评估 Redis。SQLite 单机版继续把共享知识保存在本机运行数据根；PostgreSQL 多用户版由同一数据库向授权用户共享，不把 SQLite 文件放到网络共享盘模拟多人知识库。

### i5a6 网页证据边界（2026-07-21 审查补充）

i5a6 搜索页存在三种不可混淆的结构：`resultfind` 后的标准编码结果表、`hscasefind`/“申报实例查询结果”直接实例表，以及标准编码详情页的 `#sbsl` 申报实例汇总表。标准编码结果行中的“实例汇总”链接通常指向 `/hscode/detail/{code}#sbsl`；详情页上部还包含申报要素、单位、税率、监管条件、检验检疫、CIQ 和分类层级，`#sbsl` 首屏通常只展示约 20 条代表实例，链接显示的“534条”等是汇总数量而不是可直接遍历的全部行。

联网 Provider 现已返回类型化证据：`StandardCode`、`DeclarationExample` 和 `ReplacementEvidence` 分开传递，并保留 `summaryUrl / instanceCount / evidenceUrl / observedAt`。详情解析覆盖 `#hscode-detail`、`#sbsl`、个人行邮税号、CIQ 和分类层级；旧实例的商品名称/规格仍可作为历史语义样本，但旧编码推荐只产生待审核映射。网页观察时间不会写成实例年份，i5a6 标准行也不会自动写成当前年度有效税则。

联网查询明确采用两条数据流：上方结果表只展示按编码去重的非作废 `StandardCode`；最初搜索页的全部 `DeclarationExample` 按“原始编码 + 商品名称 + 商品规格”整行去重后进入 `HsCodeRemoteCandidates`。当“男T恤”等搜索页只有历史实例、没有当前标准行时，协调服务按每个唯一旧编码访问一次详情页，读取“已作废 / 推荐查询”，再查询推荐前缀并合并当前标准行；自动详情访问全请求最多 12 个唯一编码、推荐递归最多 3 层，单个失败不影响其它编码。详情页 `#sbsl` 和推荐查询页产生的额外实例不会自动混入本次直接候选，避免证据范围无控制扩张。无法解析当前标准编码时，上方保持为空并明确提示候选已保存，不再用旧实例伪装标准结果。

本地有效税则选择器对至少 4 位的纯数字条件采用 `NormalizedCode.StartsWith` 前缀语义；例如 `6109` 只匹配 `6109100000` 等以 `6109` 开头的编码，不匹配 `2846109010` 这类中间包含值。商品名称等文本条件继续使用名称/描述检索，两个语义不互相污染。

解析层继续采用跨平台 `.NET 8 + HtmlAgilityPack`，动态 DOM 使用统一 `.NET 8 + Microsoft.Playwright` 浏览器宿主降级。解析器按表头语义、字段标签和结构质量评分选择区域，不依赖单一 `id/class/XPath`；已覆盖包裹层、表头行、列顺序和移动卡片类名变化。该任务属于网络 I/O 和语义校验，不存在值得引入 Rust Sidecar 的已证实性能瓶颈；Rust 仍用于 Excel 结构分析和 OCR，避免为同一 API 增加第二语言进程边界。

知识中心已经落实当前年度有效性门禁：只有本地 `Active` 年度税则编码可由人工确认进入正式实例；第三方详情补全不自动写主库，显式保存也标记为 `ReferenceOnly`，后续年度税则导入才可升级为 `Active`。智能查询增加性别、材质、织造方式和品类冲突惩罚，界面显示匹配理由/冲突并把分数明确命名为“文本匹配分”。本模块定位仍是“可审查的辅助检索”，不能作为无人审核的自动报关归类决策器。

### 可信 Active、发票回填与审核生命周期（2026-07-21）

`Active` 不再只是字符串状态，而是必须同时满足“可信来源 + 2000—2100 适用年度 + 最近验证时间”的正式税则事实。手工新建 `HsCode` 默认 `ReferenceOnly`，普通编辑不能把参考或作废编码提升为 `Active`；建立和更新正式有效编码只允许走年度税则导入或经过完整性校验的 `.edmhs` 迁移。智能查询、历史确认、联网候选确认、反馈确认和发票一键回填共同使用同一 `HsCodeValidityPolicy`，避免各入口出现不同有效性口径。

发票商品明细新增“智能 HS”侧边面板：查询词由当前行中文/英文品名、材质和品牌组合，结果展示税则来源、年度、验证时间、文本匹配分、正式实例数和属性冲突；只有可信 `Active` 可一键回填当前行。回填编码时可补齐空白中文单位和退税率，并记录本次人工确认反馈，形成“年度税则 -> 正式实例 -> 发票使用 -> 反馈学习”的闭环。面板使用 `/api/invoices/hs-*` 发票专用只读/反馈别名并由 `document.invoices=operate` 授权，不要求额外显示“主数据维护”，也不把主数据新增、编辑或删除权限隐式授给发票用户。

商品主数据编辑器不再预载固定 200 条 HS 编码。用户输入至少 4 位编码后采用 250ms 防抖向服务端查询前 20 条，结果缓存 1 分钟；失焦确认时再做精确校验，只套用完全一致且可信的 `Active`。因此浏览器内存和首次加载时间不再随年度税则总量线性增长，大型税则库仍通过数据库分页、数字前缀查询和服务端过滤工作。

申报实例库和联网候选池都采用服务端分页。实例库支持选择和管理级批量删除；候选池保留待审核、已确认、已忽略三类审核历史，支持批量确认、批量忽略和恢复待审核。恢复已确认候选时同步删除其 `RemoteConfirmed:*` 正式实例，保证“撤销审核”不会留下继续参与推荐的隐性知识。`view` 只显示查询和历史，`operate` 显示新增、编辑、确认、忽略和撤销，删除只对 `manage` 显示；API 中间件仍按同一档位强制授权。

远程联网服务只保留 `IHsCodeRemoteProvider` 编排。`HsCodeService` 不再拥有第二套 i5a6 HttpClient、HTML 表格和详情解析；当前 `I5a6HsCodeProvider + I5a6PageParser` 负责来源特有行为，协调服务只负责 Provider 优先级、证据合并、旧编码推荐追踪和故障降级。新增数据源应实现 Provider，不修改知识服务、发票面板或候选审核流程。

### 发票保存反馈与联网证据写入边界（2026-07-25）

发票侧栏的一键回填不再立即写学习记录。前端只把确认反馈保存在当前编辑草稿，保存时与最终商品明细一起提交；服务端验证建议编码仍存在于最终明细、编码属于可信 `Active` 税则后，才在发票事务内累计反馈。发票保存、明细保存或反馈校验任一步失败都会整体回滚，不形成“发票没保存但知识已经学习”的半成功状态。同一规范查询、编码、商品和规格累计 3 次人工确认后才生成 `ConsensusConfirmed` 正式实例。

联网来源采用显式读写分离：`GET /api/master-data/hs-codes/search-remote` 只返回在线证据，不修改候选池；`POST /api/master-data/hs-codes/search-remote/capture` 才把申报实例写入待审核候选。查询、留存证据、审核发布和知识包导出因此分别受查询、操作和管理权限控制，不再把普通查看行为隐式解释为数据采集授权。

## 权限、业务归属与正式多人运行边界（2026-07-21）

权限采用前后端双层但不互相替代的模型。React 根据会话返回的 `moduleAccess` 控制工作区、路由和按钮显隐，公共业务组件的 `canOperate / canManage` 等参数必须显式传入，缺少参数会在 TypeScript 编译阶段暴露；API 中间件依据模块和 HTTP 方法再次执行 `view / operate / manage` 校验，端点内部再处理使用 POST 表达的批量删除等特殊管理动作。因此隐藏按钮只改善普通用户体验，真正的越权防护始终位于服务端。

PostgreSQL 多用户模式下，发票、付款、CRM、供应商、商机、用户模板、后台任务和装柜方案均有明确账号归属或作用域。装柜方案使用 `OwnerUserId / DepartmentId / CompanyScope` 隔离普通账号，并用 `VersionNumber` 防止同一记录在多个浏览器会话中被旧版本覆盖；管理员保留全局审查能力。删除账号前必须确认其不再拥有主要业务实体，否则只能停用。共享型参考数据（例如柜型和标准主数据）继续由模块权限控制，不伪装成个人数据。

当前可承诺的正式多人拓扑是“一套 PostgreSQL、一个 API 服务实例、多个浏览器用户”。SQLite 仅面向单机、单进程并绑定回环地址，不支持把数据库文件放到 NAS 后供多台电脑并发打开。多个 API 副本仍需要数据库任务租约、跨实例取消、共享文件存储和压力测试后才能进入支持范围；Docker/PostgreSQL 多岗位压力、备份恢复、HTTPS 反向代理、大文件/PDF/OCR 容量以及真实 Windows/macOS/Linux 设备与签名更新链也仍需生产前实机验收。

前端继续采用 React、TypeScript、TanStack Query、React Router、Lucide、Three.js、html2canvas、jsPDF 和 Vite 等免费开源库。页面已具备统一导航、权限态、空/忙/错状态、分页和窄屏适配，生产构建按路由拆包；后续视觉和维护性优化优先拆分大型业务协调页和全局样式表，而不是引入重量级商业组件。当前 Web 锁定 `react-router-dom/react-router 7.18.1`；官方 npm 审计报告同一上游 `GHSA-qwww-vcr4-c8h2` 的两个依赖路径，该公告仅涉及未使用的 unstable RSC API。源码只使用声明式 `HashRouter / Routes / Route` 等客户端 API，并由 `npm run test:router-contract` 防止 RSC/SSR API 回流；因此这里不再笼统写成“npm 全依赖 0 漏洞”，待兼容的上游修复包发布后再升级复验。NuGet 本机还原/测试通过，但漏洞源不可达时不把 `NU1900` 误报为联网审计通过。
## 2026-07-22 多用户共享主数据并发边界

- 客户、出口商、商品、收款对象、港口、单位和 HS 编码统一采用 API `rowVersion` 乐观并发令牌。读取详情时返回令牌，保存时携带原令牌；数据库写入成功后由审计拦截器生成新令牌。
- 两个浏览器同时编辑同一条共享资料时，先保存者成功，后保存的旧页面返回 HTTP 409，并提示加载最新数据，不再使用“最后保存者覆盖前者”的隐式策略。
- HS 编码普通编辑受版本令牌保护；年度税则导入和联网候选写入仍走各自的批处理/审核边界。批量任务不会伪装成用户打开的旧编辑页面，也不会把第三方参考资料直接升级为当前有效编码。
- 权限边界仍为“前端显隐与禁用 + 路由守卫 + API 最终校验”：`view` 只读、`operate` 新建/编辑/执行、`manage` 删除和批量破坏性操作；管理员系统页另受管理员身份、产品版本和桌面运行环境约束。
- 当前正式多人拓扑仍限定为“一个 PostgreSQL 数据库 + 一个 API 实例 + 多个浏览器用户”。本轮并发令牌解决同一 API 下的共享资料编辑冲突，但不等同于多 API 副本能力；横向扩容仍需数据库任务租约、跨实例取消和共享文件存储。

## 2026-07-24 安全边界与容器代理设计补充

- 登录限流的客户端地址来源经过显式可信代理链：内置 Compose Web/Nginx 使用初始化脚本根据宿主接口、路由表和 Docker 网络自动选择不重叠的紧凑私有容器 IPv4（默认 `/28`，也可显式规划 `/24` 至 `/28`），API 通过 `EXPORTDOCMANAGER_TRUSTED_PROXIES`、`KnownProxies` 和有限 `ForwardLimit` 逐跳处理 `X-Forwarded-*`。仓库不再把 `172.30.238.0/24` 作为部署默认值；没有配置的代理不会因为发送任意转发头而改变 API 看到的地址；外层 HTTPS/CDN 代理必须以明确 IP 加入 `EXPORTDOCMANAGER_ADDITIONAL_TRUSTED_PROXIES`。非 Docker 浏览器服务器版不使用容器网段，若置于 IIS/Caddy/Nginx 后面则在本地环境文件中配置直接代理 IP。
- 代理链配置属于部署边界而非业务代码耦合。单机 SQLite/回环模式不需要代理；浏览器服务器可配置一个明确代理或直接保留框架回环默认值；Compose 通过 `.env` 同时管理容器网段、Nginx 固定地址和可选上游代理，避免把反向代理实现细节散落在登录端点。
- HS 知识库的共享层仍是部署实例级参考主数据，业务发票按权限隔离；知识包导入使用批量键预取和有界参数列表，不引入 Redis 或搜索引擎。未来只有在多 API 节点出现跨节点热点缓存与失效一致性需求时，才单独评估 Redis，不能用缓存替代 PostgreSQL 权威数据。
- 浏览器版可切换 API 地址，CSP 的 `connect-src` 允许 HTTP/HTTPS 目标，但服务端 CORS、Bearer 会话、模块权限和审计边界仍独立生效。该取舍支持局域网部署的普通用户连接体验，不意味着开放匿名跨域 API。
- 当前质量门禁以代码和本机测试记为约 `99%`，生产前仍需真实 PostgreSQL 多机、外层代理、四岗位、多平台和长时间运行验收；不以静态测试或 Windows 单机结果替代这些部署证据。

## 2026-07-25 发票价格精度与金额权威口径

- 发票明细明确区分“单价驱动”和“行金额驱动”。普通录入以单价为准并按数量重算行金额；用户直接录入行金额时，行金额成为该行权威值，单价按数量反算，避免整数总金额除不尽时被舍入误差反向改写。
- 单价存储和计算上限统一为 5 位小数；界面、报表和单一窗口映射采用“能用 2 位准确表达就显示 2 位，否则显示 5 位”的规则。行金额、发票总金额、FOB/总值统一保留 2 位，发票总金额始终等于明细行金额合计，不再设置前端可见的金额尾差字段。
- Excel 导入会比较数量×单价与行金额：一致时采用单价驱动，不一致或只有行金额时采用行金额驱动；保存 API、复制清空金额、单据包导入和服务端校验复用同一规则。
- “2—6 位可配置”暂列为后续扩展，不在当前未生产部署版本增加全局设置、跨用户同步和数据库迁移复杂度。若未来确有低单价/大数量行业需求，应以受限的 `2—6` 配置同时贯通前端输入、服务端重算、数据库、Excel、报表和单一窗口导出，不能只改一个页面的显示格式。

## 2026-07-26 发票显示精度与单一窗口适配边界

发票编辑和普通报表采用统一的商用显示口径：单价通常保留 2 位，只有 2 位不能准确表达时才显示最多 5 位并去掉无意义尾零；毛重/净重固定 2 位，体积固定 3 位。该口径由领域层策略统一提供，前端、Excel 导入、商品主数据、保存校验和普通报表复用，避免每个页面自行格式化导致金额或计量不一致。

单一窗口不是普通报表。它必须匹配官方单一窗口导入客户端，本项目继续把 `SingleWindowFieldMappers` 视为独立适配层：本轮未修改其字段映射、字段长度和官方字符串格式，也不把发票编辑页的视觉格式化规则强行套入官方导出。后续如官方客户端规则变化，只在该适配层及其契约测试中单独评估，不能通过修改通用领域显示策略“顺带”改变导入文件。

发票编辑的破坏性操作按钮采用高对比度语义色，保证普通用户在浅色/深色背景下可读；动效保持轻量 CSS 过渡，不引入大型 Canvas 或额外运行时依赖。当前本机门禁为 Web 构建 `2154 modules`、视觉基准 `80/80`、.NET Release `605/605`，生产完成度仍以部署环境验收为准。
