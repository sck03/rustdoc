name: Reusable browser server package

on:
  workflow_call:
    inputs:
      platform:
        required: true
        type: string
      runner:
        required: true
        type: string
      runtime_identifier:
        required: true
        type: string
      rust_target:
        required: true
        type: string
      chrome_platform:
        required: true
        type: string
      version:
        required: true
        type: string
      publish_release:
        required: true
        type: boolean

concurrency:
  group: browser-server-${{ inputs.platform }}-${{ inputs.runtime_identifier }}-${{ inputs.version }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  package:
    runs-on: ${{ inputs.runner }}
    env:
      DOTNET_CLI_HOME: ${{ github.workspace }}/.codex-runtime/dotnet-cli
      NUGET_PACKAGES: ${{ github.workspace }}/.codex-runtime/nuget-packages
      NUGET_HTTP_CACHE_PATH: ${{ github.workspace }}/.codex-runtime/nuget-http-cache
      npm_config_cache: ${{ github.workspace }}/.codex-runtime/npm-cache
      CARGO_HOME: ${{ github.workspace }}/.codex-runtime/cargo-home
      CARGO_TARGET_DIR: ${{ github.workspace }}/artifacts/cargo-target-ocr

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Verify public repository boundary
        shell: pwsh
        run: ./scripts/github/verify-public-source.ps1

      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/export-doc-web/package-lock.json

      - name: Set up Rust target
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ inputs.rust_target }}

      - name: Install Web dependencies
        run: npm ci --prefix apps/export-doc-web

      - name: Provision and verify open-source report fonts
        shell: pwsh
        run: |
          node scripts/provision-report-fonts.mjs
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          node scripts/verify-font-license-policy.mjs --require-files
          exit $LASTEXITCODE

      - name: Validate and apply requested version
        id: version
        shell: pwsh
        env:
          REQUESTED_VERSION: ${{ inputs.version }}
        run: |
          $version = $env:REQUESTED_VERSION.Trim()
          if ($version.StartsWith("v", [System.StringComparison]::OrdinalIgnoreCase)) {
            $version = $version.Substring(1)
          }
          if ($version -notmatch '^\d+\.\d+\.\d+(?:-[0-9A-Za-z.-]+)?$') {
            throw "版本格式无效：$($env:REQUESTED_VERSION)"
          }
          "value=$version" | Out-File $env:GITHUB_OUTPUT -Encoding utf8 -Append
          node scripts/sync-version.mjs $version

      - name: Provision Chrome Headless Shell
        if: inputs.chrome_platform != 'chromium-linux-arm64'
        shell: pwsh
        run: >-
          ./scripts/provision-chrome-for-testing.ps1
          -Product ChromeHeadlessShell
          -Platform ${{ inputs.chrome_platform }}

      - name: Provision Chromium ARM64
        if: inputs.chrome_platform == 'chromium-linux-arm64'
        shell: pwsh
        run: ./scripts/provision-playwright-chromium-arm64.ps1

      - name: Build shared browser frontend
        run: npm --prefix apps/export-doc-web run build

      - name: Restore browser server runtime graph
        shell: pwsh
        run: >-
          dotnet restore src/ExportDocManager.Api/ExportDocManager.Api.csproj
          --runtime ${{ inputs.runtime_identifier }}
          --configfile NuGet.Config

      - name: Publish self-contained browser server
        shell: pwsh
        run: >-
          dotnet publish src/ExportDocManager.Api/ExportDocManager.Api.csproj
          --configuration Release
          --runtime ${{ inputs.runtime_identifier }}
          --self-contained true
          --no-restore
          --output artifacts/browser-server/package
          /p:PublishSingleFile=false
          /p:PublishReadyToRun=false
          /p:DebugType=None
          /p:DebugSymbols=false
          /p:ExportDocPackageProfile=Server

      - name: Build Rust OCR sidecar
        shell: pwsh
        run: cargo build --manifest-path apps/exportdoc-ocr-rs/Cargo.toml --release --target ${{ inputs.rust_target }}

      - name: Build Rust Excel analyzer
        shell: pwsh
        run: cargo build --manifest-path tools/excel-analyzer-rs/Cargo.toml --release --locked --target ${{ inputs.rust_target }}

      - name: Assemble browser server package
        shell: pwsh
        run: |
          $packageRoot = Join-Path $env:GITHUB_WORKSPACE "artifacts/browser-server/package"
          $webRoot = Join-Path $packageRoot "wwwroot"
          New-Item -ItemType Directory -Path $webRoot -Force | Out-Null
          Copy-Item -Path "apps/export-doc-web/dist/*" -Destination $webRoot -Recurse -Force
          Copy-Item -LiteralPath "deploy/browser-server/appsettings.json" -Destination $packageRoot -Force
          if ('${{ inputs.platform }}' -eq 'windows') {
            Copy-Item -LiteralPath "deploy/browser-server/start-windows.ps1" -Destination $packageRoot -Force
          } else {
            Copy-Item -LiteralPath "deploy/browser-server/start-linux.sh" -Destination $packageRoot -Force
          }
          $ocrRoot = Join-Path $packageRoot "sidecar/ocr"
          New-Item -ItemType Directory -Path $ocrRoot -Force | Out-Null
          $ocrName = if ('${{ inputs.platform }}' -eq 'windows') { 'exportdoc-ocr.exe' } else { 'exportdoc-ocr' }
          Copy-Item -LiteralPath (Join-Path $env:CARGO_TARGET_DIR "${{ inputs.rust_target }}/release/$ocrName") -Destination (Join-Path $ocrRoot $ocrName) -Force
          Copy-Item -LiteralPath "apps/exportdoc-ocr-rs/README.md" -Destination $ocrRoot -Force
          $toolsRoot = Join-Path $packageRoot "Tools"
          New-Item -ItemType Directory -Path $toolsRoot -Force | Out-Null
          $analyzerName = if ('${{ inputs.platform }}' -eq 'windows') { 'exportdoc-excel-analyzer.exe' } else { 'exportdoc-excel-analyzer' }
          Copy-Item -LiteralPath (Join-Path $env:CARGO_TARGET_DIR "${{ inputs.rust_target }}/release/$analyzerName") -Destination (Join-Path $toolsRoot $analyzerName) -Force
          Copy-Item -LiteralPath "tools/excel-analyzer-rs/THIRD_PARTY_NOTICES.md" -Destination (Join-Path $toolsRoot "EXCEL_ANALYZER_NOTICES.md") -Force

          $browser = Get-ChildItem -LiteralPath (Join-Path $packageRoot "Browsers") -File -Recurse |
            Where-Object { $_.Name -in @("chrome-headless-shell.exe", "chrome-headless-shell", "chrome") } |
            Select-Object -First 1
          if ($null -eq $browser) { throw "Chrome Headless Shell was not included in the server package." }
          if (-not (Test-Path -LiteralPath (Join-Path $webRoot "index.html"))) { throw "Web index.html was not included." }
          if ("${{ inputs.platform }}" -eq "linux") {
            @(
              (Join-Path $packageRoot "libonnxruntime.so"),
              (Join-Path $packageRoot "OcrModels/PaddleOCR/V6/det/inference.onnx"),
              (Join-Path $packageRoot "OcrModels/PaddleOCR/V6/rec/inference.onnx"),
              (Join-Path $packageRoot "OcrModels/PaddleOCR/V6/THIRD_PARTY_NOTICES.md")
            ) | ForEach-Object {
              if (-not (Test-Path -LiteralPath $_ -PathType Leaf)) {
                throw "Linux PP-OCRv6 package file was not included: $_"
              }
            }
            & chmod +x -- (Join-Path $packageRoot "ExportDocManager.Api") (Join-Path $packageRoot "start-linux.sh") $browser.FullName (Join-Path $ocrRoot "exportdoc-ocr") (Join-Path $toolsRoot "exportdoc-excel-analyzer")
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }

      - name: Verify bundled browser PDF generation
        shell: pwsh
        run: ./scripts/verify-bundled-browser-pdf.ps1 -BrowserRoot ./artifacts/browser-server/package/Browsers

      - name: Verify lean server payload
        shell: pwsh
        run: ./scripts/verify-package-payload.ps1 -PackageRoot ./artifacts/browser-server/package -Profile Server -RuntimeIdentifier ${{ inputs.runtime_identifier }}

      - name: Verify bundled Rust PP-OCRv6 runtime
        shell: pwsh
        env:
          EXPORTDOCMANAGER_OCR_RUNTIME: enabled
        run: |
          $packageRoot = Join-Path $env:GITHUB_WORKSPACE "artifacts/browser-server/package"
          $dataRoot = Join-Path $packageRoot "App_Data/OcrBuildCheck"
          & (Join-Path $packageRoot "ExportDocManager.Api") --app-root $packageRoot --data-root $dataRoot --verify-ocr-runtime
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          Remove-Item -LiteralPath $dataRoot -Recurse -Force -ErrorAction SilentlyContinue

      - name: Create downloadable archive
        id: archive
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.value }}"
          $releaseRoot = Join-Path $env:GITHUB_WORKSPACE "artifacts/browser-server/release"
          $packageRoot = Join-Path $env:GITHUB_WORKSPACE "artifacts/browser-server/package"
          New-Item -ItemType Directory -Path $releaseRoot -Force | Out-Null
          if ("${{ inputs.platform }}" -eq "windows") {
            $archive = Join-Path $releaseRoot "export-doc-manager-browser-server-windows-x64-$version.zip"
            Compress-Archive -Path (Join-Path $packageRoot "*") -DestinationPath $archive -CompressionLevel Optimal
          } else {
            $architecture = if ('${{ inputs.runtime_identifier }}' -eq 'linux-arm64') { 'arm64' } else { 'x64' }
            $archive = Join-Path $releaseRoot "export-doc-manager-browser-server-linux-$architecture-$version.tar.gz"
            & tar -czf $archive -C $packageRoot .
            if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          }
          "path=$archive" | Out-File $env:GITHUB_OUTPUT -Encoding utf8 -Append

      - name: Upload browser server artifact
        uses: actions/upload-artifact@v5
        with:
          name: export-doc-manager-browser-server-${{ inputs.platform }}-${{ inputs.runtime_identifier }}-${{ steps.version.outputs.value }}
          path: ${{ steps.archive.outputs.path }}
          if-no-files-found: error
          retention-days: 14
          compression-level: 0

      - name: Publish to GitHub Release
        if: inputs.publish_release
        shell: pwsh
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          $tag = "v${{ steps.version.outputs.value }}"
          gh release view $tag *> $null
          if ($LASTEXITCODE -ne 0) {
            gh release create $tag --title "ExportDocManager ${{ steps.version.outputs.value }}" --notes "testing"
          }
          gh release upload $tag "${{ steps.archive.outputs.path }}" --clobber
