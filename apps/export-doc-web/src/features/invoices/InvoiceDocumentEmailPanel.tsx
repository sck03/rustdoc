import { Mail, Settings } from "lucide-react";
import { TextAreaField } from "../../ui/FormFields.tsx";
type Props={body:string;canSend:boolean;includeMergedPdf:boolean;isBusy:boolean;selectedTemplateCount:number;subject:string;toAddress:string;onBodyChange(value:string):void;onIncludeMergedPdfChange(value:boolean):void;onOpenSettings():void;onSend():void;onSubjectChange(value:string):void;onToAddressChange(value:string):void};
export function InvoiceDocumentEmailPanel(p:Props){return <div className="document-email-panel"><div className="document-package-heading"><h3>邮件附件</h3><div className="toolbar-actions"><span>{p.selectedTemplateCount} 个模板</span><button className="command-button secondary" type="button" title="管理邮件设置" disabled={p.isBusy} onClick={p.onOpenSettings}><Settings size={17} aria-hidden="true"/><span>邮件设置</span></button></div></div><div className="document-email-grid"><label><span>收件人</span><input type="email" value={p.toAddress} disabled={p.isBusy} onChange={e=>p.onToAddressChange(e.target.value)}/></label><label><span>主题</span><input value={p.subject} disabled={p.isBusy} onChange={e=>p.onSubjectChange(e.target.value)}/></label><TextAreaField label="正文" value={p.body} disabled={p.isBusy} onChange={p.onBodyChange}/></div><div className="document-email-actions"><label className="toggle-field"><input type="checkbox" checked={p.includeMergedPdf} disabled={p.isBusy||p.selectedTemplateCount<2} onChange={e=>p.onIncludeMergedPdfChange(e.target.checked)}/><span>合并 PDF</span></label><button className="command-button secondary" type="button" disabled={!p.canSend} onClick={p.onSend}><Mail size={17} aria-hidden="true"/><span>发送邮件</span></button></div></div>}

